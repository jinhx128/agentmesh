# 2026-05-15

## 11:30 - Codex CLI Reasoning 参数修复

- 修复 `codex-cli` adapter 仍使用旧 `--reasoning-effort` 参数的问题，改为通过当前 Codex CLI 支持的 `-c model_reasoning_effort="..."` 传递 reasoning effort。
- 同步 `prepareAdapterInvocation` 和 CLI surface 回归断言，覆盖 Codex adapter 的命令拼装行为。
- 影响范围：只改变 `codex-cli` 内置 adapter 的调用参数；`claude-code-cli`、`gemini-cli`、`opencode-cli` 和 generic `command` adapter 不变。

### 验证

- `npm run build && node --test dist-node/tests-node/adapter-invocation.test.js dist-node/tests-node/cli-surface.test.js` 通过：12 tests passed。

## 12:00 - Studio 增加中英文切换

- 在 AgentMesh Studio 侧栏标题区新增紧凑语言选择器，默认英文，可无刷新切换到简体中文，并通过 `localStorage` 持久化语言选择。
- 增加前端内置 i18n 字典，覆盖 Studio 静态文案、按钮、空状态、计数、mutation 状态、review/release 标题和预览标题；run id、status、workflow id、artifact 内容、raw review 和命令输出保持原样不翻译。
- 扩展 `studio-ui` 测试，覆盖非法本地语言值回退英文、英文到中文切换、中文再切回英文、预览标题本地化和 packet 内容不被翻译。
- OpenCode/ds-v4-pro follow-up review 确认前序 Should Fix / Nit 已处理，结果为 LGTM。

### 验证

- `npm run build && node --test dist-node/tests-node/studio-ui.test.js dist-node/tests-node/studio.test.js` 通过：9 tests passed。
- 浏览器烟测确认默认英文、切换中文、刷新后保持中文、`预览: request` 和 artifact 内容不翻译。
- 完整 `npm test` 未完成：当前项目 `.agentmesh/config.toml` 会让 readiness 测试拉起真实 Gemini auth probe 并挂住，已按环境限制记录在 AgentMesh handoff。

## 12:07 - AgentMesh Skill 补齐 Codex frontmatter

- 给 canonical `agentmesh-skill/SKILL.md` 增加 Codex 可加载的 YAML frontmatter，解决 Codex CLI 跳过 `agentmesh` skill 时提示缺少 `---` frontmatter 的问题。
- 调整 Skill 版本 metadata 注入逻辑，保留 frontmatter，并继续把 `Version Metadata` 块插入到 `# AgentMesh Skill` 标题后。
- 同步 `skill-output` contract，明确 canonical Skill markdown 必须带 `name` / `description` frontmatter；Cursor、Gemini 等 host wrapper 可以在外层使用自己的格式。
- 扩展 readiness 回归，覆盖 canonical markdown frontmatter，以及 `skill install --target codex` 写出的最终 `SKILL.md` 可被 Codex 加载并通过 `skill verify`。

### 验证

- `npm run build && node --test --test-name-pattern "Codex-loadable" dist-node/tests-node/readiness.test.js` 通过：1 test passed。
- `node --test --test-name-pattern "skill" dist-node/tests-node/readiness.test.js` 通过：6 tests passed。

## 12:34 - 内置 Review Gate workflow 重命名

- 将内置 `review-center` workflow 重命名为 `review-gate`，展示名同步为 `Review Gate`，保留原有 `review -> decide` stage 形状和 reviewer evidence / decider classification 语义。
- 将 canonical recipe 从 `docs/workflows/review-center.toml` 重命名为 `docs/workflows/review-gate.toml`，并同步 runtime `recipeSource`、workflow registry 回归、README、AgentMesh skill 源文件、contracts、roadmap 和静态页面引用。
- 刷新本机 Codex 安装版 AgentMesh skill，确保 `~/.codex/skills/agentmesh/SKILL.md` 也指向 `review-gate`。
- 兼容性影响：`agentmesh workflows show review-gate` 返回内置 workflow；旧 id `review-center` 现在会按 unknown workflow 处理。

### 验证

- `node dist-node/packages/cli/src/cli.js skill verify --target codex --json` 通过，Codex 安装版 skill 状态为 `ok`。
- `npm run build` 通过。
- `node --test dist-node/tests-node/workflow-registry.test.js` 通过：12 tests passed。
- `node dist-node/packages/cli/src/cli.js workflows show review-gate --json` 返回 `workflowId = "review-gate"` 和 `recipeSource = "docs/workflows/review-gate.toml"`。
- `node dist-node/packages/cli/src/cli.js workflows show review-center --json` 返回 unknown workflow。
- `git diff --check` 通过。

## 12:44 - Agent / Workflow 管理默认用户级

- 补齐 `agentmesh agents remove`、`agentmesh workflows add` 和 `agentmesh workflows remove` CLI 入口；`agents add` 和 `workflows add` 默认写用户级 registry，只有显式 `--scope project` 才写项目级。
- `agents remove` 和 `workflows remove` 必须显式指定 `--scope user|project`，只删除请求的 registry 层，并拒绝删除内置 workflow。
- 修复显式传入空值 `--scope` 时被误当作默认 user 的问题；现在缺值或非法值会返回 usage 错误，不会创建用户级配置或 workflow 文件。
- 同步 README、AgentMesh skill 源文件和 `config-layering` contract，明确用户级默认、项目级显式 opt-in 和 scoped 删除语义。

### 验证

- `npm run build && node --test dist-node/tests-node/management-cli.test.js` 通过：5 tests passed。
- `node --test dist-node/tests-node/management-cli.test.js dist-node/tests-node/cli-surface.test.js dist-node/tests-node/config-layering.test.js dist-node/tests-node/workflow-registry.test.js` 通过：40 tests passed。

## 12:56 - Workflow 管理命令 Review 修复

- 修复 `workflows add` 在目标 registry 已存在同名 `.toml` 文件时会覆盖旧 workflow 文件的问题；现在即使旧文件内部 `id` 不同，也会拒绝写入并保留原文件。
- 修复 `workflows remove --scope project` 未继承 `--config` 外部项目 workflow registry 发现路径的问题；现在删除逻辑与 `workflows list/show` 使用同一套 project registry 派生规则。
- 同步 `config-layering` contract 和 `python-decommission` 命令替换矩阵，补充 scoped 管理命令和 `--config` 下的 project workflow 写删语义。

### 验证

- `npm run build && node --test dist-node/tests-node/management-cli.test.js dist-node/tests-node/cli-surface.test.js dist-node/tests-node/workflow-registry.test.js dist-node/tests-node/config-layering.test.js && git diff --check` 通过：42 tests passed。
- `node --test dist-node/tests-node/decommission.test.js` 通过：2 tests passed。

## 14:22 - Plan / Decide Fanout 调度落地

- 支持 `plan` 和 `decide` 多 agent fanout：每个 agent 写入 `outputs/<stage>/<agent>.md`，再由首个 agent 通过 `prompts/<stage>/synthesis.md` 合成 canonical `plan.md` 或 `decision.md`；`execute` fanout 继续明确延期。
- 修复 fanout retry 语义：已成功登记 artifact 的 agent 输出会复用，失败 agent 即使留下 partial 文件也会重跑；retry 后重新执行 synthesis。
- 将 release-check 的 verdict 更新改为只读取 canonical `decision.md`，覆盖 `ready`、`not_ready`、`needs_decision` 和缺失 verdict 的失败路径。
- 同步 `stage-dispatch`、`status-json`、`config-layering` contract，并保留 review artifact 命名兼容 wrapper。
- DeepSeek review 后修复 retry / verdict / synthesis 覆盖缺口；Mimo review 未发现 Must Fix，建议后续可补 `decide` fanout retry 和 3+ agent fanout 覆盖。

### 验证

- `npm run build` 通过。
- `node --test dist-node/tests-node/flow-dispatch.test.js dist-node/tests-node/release-check-flow.test.js` 通过：16 tests passed。
- `node --test $(find dist-node/tests-node -name '*.test.js' ! -name 'readiness.test.js' | sort)` 通过：162 tests passed。
- `readiness.test.js` 未纳入完整回归：当前本机配置会触发真实 Gemini auth probe 并挂住，沿用已知环境限制。

## 14:40 - Studio 默认切换为中文

- 将 AgentMesh Studio 首屏静态 HTML 默认语言从英文改为简体中文，包含 `html lang="zh-Hans"`、默认选中 `中文`、侧栏、操作区、review/release、事件、产物和预览空状态文案。
- 调整 Studio 语言读取逻辑：只有已保存的 `agentmesh.studio.language = "en"` 会保留英文，其余空值、非法值或 storage 异常都回退到中文。
- 扩展 `studio-ui` 回归，覆盖静态首屏中文、非法语言值回退中文、已保存英文偏好首载不被覆盖、`documentElement.lang` 同步，以及 `zh -> en -> zh` 往返切换。
- 通过 AgentMesh `review-gate` 让 `opencode`/DeepSeek 和 `mimo` 评审本次改动；两者均未发现 Must Fix，采纳了英文偏好保留与语言属性断言的测试覆盖建议。

### 验证

- `npm run build && node --test dist-node/tests-node/studio-ui.test.js dist-node/tests-node/studio.test.js` 通过：10 tests passed。

## 17:25 - Plan D 重复 Workflow 节点落地

- 增加稳定的 workflow stage node 模型：`stages` 保留 stage type 序列，运行态写入 `stage_nodes`，重复节点派生为 `execute_2`、`review_2` 等 node id。
- 调整 flow run / dispatch / retry / resume / prompt / attach，使状态、artifact、prompt、review 输出和事件都按 node id 读写，避免重复 stage 覆盖或误跳过。
- Workflow registry 允许 `plan`、`execute`、`review` 重复，限制 1-15 个节点，并要求 `decide` 至多一次且只能在最后。
- 同步 `status-json`、`stage-dispatch`、`prompt-assembly`、`artifacts-toml` 和 `workflow-toml` contract，补充重复节点路径、事件字段和前置节点约束。

### 验证

- `npm test` 通过：197 tests passed。

## 19:21 - Run Timing 与 Studio 可观测性收口

- `status.json` 新增 `created_at`、`updated_at`、`stage_timing` 和 `agent_timing`，flow create / dispatch / attach / retry / resume 会记录阶段与 agent 的开始、完成、失败、耗时、尝试次数和 exit code。
- `doctor --agent <id-or-alias>` 支持按 id 或 alias 单独检查 agent readiness，可重复传入多个 `--agent`，避免全量 probe 影响单个 agent 诊断。
- Studio 增加 run 创建/更新时间、阶段耗时、事件时间格式化、左侧更新时间、事件分页，以及 agents / workflows 配置概览；事件时间显示为 `yyyy-mm-dd hh:mm:ss`。
- 自审修复 `/api/catalog` 同步调用 CLI 可能长时间卡住的问题：catalog 子进程现在有超时保护，并在超时后返回 diagnostics。
- 同步 README、AgentMesh skill、`status-json` contract、fixture 和 packet / flow / doctor / Studio 回归测试。
- Mimo review 因本机 auth probe 在 30s 内超时未执行，未绕过 AgentMesh readiness 规则。

### 验证

- `npm test` 通过：203 tests passed。
- `git diff --check` 通过。

## 21:19 - Agent 子进程继承系统环境

- 新增统一的 agent 子进程环境构建逻辑：`agentmesh call`、doctor auth probe、help probe 和 version probe 都继承宿主进程环境，并在 macOS 下从 `scutil --proxy` 补齐系统 HTTP / HTTPS / SOCKS / NO_PROXY 设置。
- 支持 agent 配置 `env = ["KEY=value"]` 作为显式覆盖；非法 env key 会在 invocation / doctor 中报告配置错误，空字符串值可用于清空继承的代理变量。
- 同步大小写代理变量别名，过滤 inherited env 中的 `undefined` 项，并缓存 macOS 代理读取结果，避免每次 probe 重复调用系统命令。
- 同步 `adapter-invocation` contract，记录 AgentMesh worker subprocess 的 env 继承、agent env 覆盖和 macOS 代理映射规则。
- Mimo 复查后处理了 malformed env 断言、doctor 重复解析 env、undefined env 过滤和缓存边界测试。

### 验证

- `npm test` 通过：215 tests passed。
- `git diff --check` 通过。
- `node dist-node/packages/cli/src/cli.js doctor --agent codex --json` 返回 ready。
- `node dist-node/packages/cli/src/cli.js doctor --agent mimo --json` 返回 ready。
- `node dist-node/packages/cli/src/cli.js doctor --agent gemini --probe-timeout-secs 90 --json` 返回 ready。
- 真实 `agentmesh call` 验证 Codex 和 Gemini 均写出预期健康标记。

## 22:11 - AgentMesh CLI 入口权限与 Help 修复

- 修复 `npm link` 后直接执行 `agentmesh` 可能因 `dist-node/packages/cli/src/cli.js` 缺少执行位而 `permission denied` 的问题；build 后会显式给 CLI bin 目标设置 `0o755`。
- 将 build 脚本中的 POSIX `rm -rf` / `chmod +x` 替换为 Node `fs.rmSync` / `fs.chmodSync`，降低 Windows shell 兼容风险。
- 修复顶层 `agentmesh --help` / `agentmesh -h` 打印 usage 但退出码为 2 的问题，现在顶层 help 作为成功路径返回 0。
- 安装并验证 Claude 宿主侧 AgentMesh skill；诊断确认 `mimo` readiness 正常，Claude agent 真实调用失败的当前原因是 Claude Code 额度限制，而非 AgentMesh 配置或入口权限。
- Mimo review 后采纳跨平台 build 脚本建议；其余子命令 help 和测试输出流建议暂不改变行为。

### 验证

- `agentmesh skill verify --target claude --json` 返回 `ok`。
- `agentmesh doctor --agent mimo --json --probe-timeout-secs 8` 返回 ready。
- `agentmesh --help` 返回退出码 0 并打印 usage。
- `npm test` 通过：216 tests passed。

## 22:21 - CLI 使用手册与 Mimo Review 修复

- 将静态首页的“命令速查”升级为 CLI 使用手册，补齐当前 CLI 命令面、关键参数说明和本机配置 / run / current stage / release-check 示例。
- 同步顶层 `agentmesh --help`，补齐已支持的扩展参数，并把 `doctor --agent` 标注为可重复传入。
- 收紧 `skill export --format` 语义：当前只接受 `markdown`，`skill show` 不再接受无效 `--format` 参数；首页同步注明 markdown 是当前唯一导出格式。
- 采纳 Mimo review 的 2 个 Nit，并新增回归覆盖 `skill export` 格式校验和 help 文案。

### 验证

- `git diff --check` 通过。
- `npm test` 通过：217 tests passed。

## 22:37 - Plan 执行规则与阶段顺序校准

- 调整 `plan.md` 的执行规则：每个小任务必须完成范围确认、实现、验证、review、accepted findings 处理、同步 `changelog/` 和 commit 后，才能进入下一个小任务。
- 明确每个 `P<n>` 大任务收尾都要重新校准 `README.md` 和根目录 `index.html`，同步产品说明、安装入口、命令入口和当前限制。
- 将 P2 已完成的配置分层产品化决策移为 P2.0 前置决策，顺延未完成的 P2 分层任务编号，并给 capability profile 预留 `verify` stage 扩展入口。
- 将 P3 的 `verify` stage 工作拆成 contract、core/runtime、workflow/Studio 三个小 slice，再顺延 runtime timing、MCP 复用、async adapter 和 fanout 调度。
- 将 `agentmesh studio` CLI 入口从 P4 前置依赖移入 P1，消除 P4 自依赖，并让 P4 剩余任务聚焦桌面分发。

### 验证

- `git diff --check -- plan.md` 通过。

## 22:53 - Plan Review Findings 收口

- 处理 `run-1778856165817` 中 Mimo/Gemini review 的 accepted findings，补齐 `plan.md` 小任务与大任务 checkpoint 的语义一致性。
- 明确一个 `P<n>` 下所有小任务完成后，必须执行大任务收尾校准 slice，完成后才能把该大任务标为完成。
- 为 P2/P3/P4/P5 增加或校准大任务收尾任务，要求重新校准 `README.md` 和根目录 `index.html`，再做总体 review、同步日志和提交。
- 将 P2/P3/P4/P5 的未完成 slice 调整为独立闭环：每个 slice 都包含验证、review、同步 `changelog/` 和 commit，不再依赖后续汇总任务统一收尾。
- 将 P3.10 的 fanout 并发上限改为读取 `status.json.resolved_execution_policy.max_fanout_concurrency`，显式依赖 P2.12 固定到 packet 的 execution policy。

### 验证

- `git diff --check -- plan.md changelog/2026-05-15.md` 通过。

## 23:11 - Studio 产品化边界决策

- 新增 `docs/decisions/studio-productization.md`，锁定 P1 Studio 本地工作台定位、首屏三分区、主要用户任务、数据来源、mutation 规则、API 边界、移动端降级和非目标。
- 在 Studio HTML 中补充稳定的 P1 workbench landmarks，覆盖 workspace bar、run navigator、当前 run 总览、安全操作、配置入口、review/release evidence、时间线/事件、packet 产物和 artifact preview。
- 扩展 `studio-ui` 与 `studio` 回归：断言首屏结构检查点存在，且 Studio server 继续只暴露窄 API，不提供 generic command/filesystem 入口，也不 import runtime。
- 更新 `plan.md`，将 P1 状态调整为进行中，并标记 P1.1 完成及证据。

### 验证

- `npm run build && node --test dist-node/tests-node/studio-ui.test.js dist-node/tests-node/studio.test.js` 通过：15 tests passed。
- `npm test` 通过：219 tests passed。
- `git diff --check` 通过。

## 23:22 - Studio 工作台三栏布局

- 将 Studio 页面重排为顶部 `workspace-bar`、左侧 `run-navigator`、中间 `run-workspace`、右侧 `inspector`，左侧只保留 run 列表和刷新，中间聚焦当前 run 总览、事件和产物，右侧承载预览、review/release、配置和安全操作。
- 重写 Studio CSS 布局骨架，桌面使用三栏 grid，1120px 以下降为两栏，720px 以下降为单列；保留工作台风格、低装饰边框、按钮换行、长 run id / artifact path 换行和 375px 无横向滚动约束。
- 扩展 `studio-ui` 测试，先用失败断言锁定 P1.2 console skeleton，再验证中文默认文案和英文切换仍可用。
- 更新 `plan.md`，标记 P1.2 完成并补充 RED/GREEN、布局和移动端自审证据。

### 验证

- `npm run build && node --test dist-node/tests-node/studio-ui.test.js` 通过：5 tests passed。
- `git diff --check` 通过。

## 23:26 - Studio Run Navigator 增强

- Run navigator 现在每个 run 显示 run id、workflow、status、latest event 和格式化更新时间，并给当前选中 run 标记 `aria-current="true"`。
- 新增本地 `run-filter` 输入，按已加载 runs 的 run id / workflow / status 做前端筛选，不引入后端搜索或 packet browser API 变更。
- 区分 run 列表三种状态：无 runs、过滤后无匹配、`/api/runs` 加载失败；中英文文案同步更新。
- 扩展 `studio-ui` 回归，覆盖 run 列表信息、workflow 筛选、空筛选结果、加载失败和语言切换下筛选框占位文案。

### 验证

- `npm run build && node --test --test-name-pattern="Studio UI" dist-node/tests-node/studio-ui.test.js` 通过：3 tests passed。
- `npm run build && node --test dist-node/tests-node/studio-ui.test.js dist-node/tests-node/studio.test.js && git diff --check` 通过：17 tests passed。

## 23:31 - Studio 当前 Run 工作区重构

- 将当前 run summary 拆成 overview metrics 和 `stage-timeline`，展示 run id、workflow、status、completed/total stages、latest event、创建/更新时间。
- Stage timing 改为可扫描列表，逐 stage 显示开始/结束/失败时间、耗时、尝试次数和 planned/running/completed/failed 文本状态，不只依赖颜色区分。
- Event timeline 保留分页，并为每条事件补充关键字段摘要，例如 stage、agent 等简单标量字段；事件时间继续格式化为 `yyyy-mm-dd hh:mm:ss`。
- Artifact 列表和点击预览行为保持稳定，不修改 packet-browser API。

### 验证

- `npm run build && node --test dist-node/tests-node/studio-ui.test.js dist-node/tests-node/studio.test.js && git diff --check` 通过：17 tests passed。
