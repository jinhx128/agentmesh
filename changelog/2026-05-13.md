# 2026-05-13

## 14:55 - AgentMesh Skill 与中文 README

- 将 `README.md` 改为中文说明，保留命令、路径、workflow id、adapter id 等专有名称原文。
- 同步说明 AgentMesh Skill、`current` 入口 agent 参与者、`flow prompt` / `flow attach`、`flow status --json`、`skill install` 和 `.agentmesh/` gitignore 行为。
- 新增项目级 `changelog/` 变更记录目录，用于后续按日期记录 AgentMesh 产品、CLI、文档和协作协议变化。
- 兼容性影响：`changelog/` 现在是项目文档目录，不再作为旧个人配置树排除项。

## 15:20 - 公共 AgentMesh Skill 安装路径

- 将 `agentmesh skill install` 调整为默认写入 `~/.agents/skills/agentmesh/SKILL.md`，符合一台电脑安装一次的公共 skill 语义。
- 保留 `agentmesh skill install --target codex`，作为同一公共路径的兼容别名，不再写入 `$CODEX_HOME/skills/...`。
- 明确 `agentmesh skill install --target cursor` 是当前项目 `.cursor/rules/agentmesh.mdc` 的入口适配器，不是 AgentMesh Skill 的主安装位置。

## 15:21 - 外审修复与路线图同步

- 修复 `flow attach --agent <worker-id>` 可伪造 worker 阶段输出的问题；`flow attach` 现在只允许 `current` 入口 agent 写回输出，worker 阶段必须走 `flow dispatch`。
- 调整 `agentmesh init` 的 `.gitignore` 行为：仅对当前项目内的输出路径更新项目 `.gitignore`，项目外配置路径会跳过；同时兼容 `.agentmesh` / `/.agentmesh/` 等已有 ignore 写法。
- 为 scoped git diff 增加超时降级，避免 `agentmesh run --scope ...` 在慢 git/filter 场景下无限等待。
- 用户提供的 task、context、diff、verification、attach 文件读取改为替换非法 UTF-8 字节，避免非 UTF-8 文件直接导致命令崩溃。
- 同步 `docs/roadmap.md` 最新短期目标与长期目标，并补充 `flow status --json` 可空字段和项目级 `.gitignore` 语义说明。
- 补充对应 shell / pytest 回归，覆盖外部配置路径、`current` 大小写保留字、显式 worker attach、非 UTF-8 输入和 scoped diff timeout。

## 15:35 - 二审问题修复

- 根据 Claude 4.7 与 Gemini 二审结果，进一步调整 `agentmesh init` 的项目边界：同一 git worktree 内的输出路径会更新当前工作目录的 `.gitignore`，不会从子项目误改 worktree root。
- 现有 `.gitignore` 若不是合法 UTF-8，`init` 会跳过 ignore 写入并保留原文件字节，不再用替换字符重写项目文件。
- `flow attach` 在 run 已完成后再次写回 `current` 阶段输出时，不再把顶层 status 从 `completed` 回退到中间阶段状态。
- 区分 scoped git diff 的 `git` 缺失与 timeout 提示，并为超大 `--task-file` 增加创建 run 前的大小保护。
- 补充回归覆盖：git worktree 子目录初始化、非法 UTF-8 `.gitignore`、完成后 re-attach、缺失 git 提示和超大 task file。

## 15:45 - `.agentmesh/config.toml` 可提交

- 根据最终复审反馈，将自动写入和仓库内 `.gitignore` 规则收窄为 `.agentmesh/runs/`，只忽略 runtime run packet。
- 保留 `./.agentmesh/config.toml` 作为可提交的项目级配置路径，避免公共 agent registry 被 `.gitignore` 意外吞掉。
- 同步 README、AgentMesh Skill 和 roadmap 中关于 `.gitignore` 的说明。
- 修复空 `.gitignore` 写入时多出前导空行的小问题。

## 16:35 - 长期方案：入口宿主显式安装

- 撤回 `~/.agents/skills/agentmesh/SKILL.md` 作为默认公共安装路径的假设；`agentmesh skill install` 现在必须显式指定入口宿主。
- 新增并统一 `codex`、`claude`、`cursor`、`gemini`、`copilot` 安装 target，均从同一份 AgentMesh Skill 内容生成宿主适配文件。
- `--target codex` 改为写入 `$CODEX_HOME/skills/agentmesh/SKILL.md` 或 `~/.codex/skills/agentmesh/SKILL.md`，不再作为公共路径别名。
- `--target gemini` 生成 `~/.gemini/extensions/agentmesh/` extension wrapper；Cursor 仍写入当前项目 `.cursor/rules/agentmesh.mdc`。
- 同步 README、AgentMesh Skill、roadmap 和 pytest，明确长期方向是“一个 Skill 源头 + 多入口 host adapter”，不是虚构的全工具公共目录。
- 根据外审反馈补充 Gemini 多文件安装冲突预检回归，并把安装过程中的文件系统异常包装为清晰的 `AgentMeshError`。

## 17:17 - Resume / Retry 与更多 MVP workflow

- 新增 `agentmesh flow retry <run-id>` 和 `agentmesh flow resume <run-id>`，支持从 `failed_stage`、显式 stage 或首个未完成 stage 恢复本地 run packet。
- 成功重跑失败 stage 后会清理 stale `failed_stage`，并在 `events.jsonl` 记录 retry / resume 请求。
- 将 `handoff` 和 `release-check` workflow 从 planned 升级为可执行 MVP workflow；`research-spike` 仍保持 planned。
- 补充 pytest 覆盖：retry 清理失败状态、resume 从失败 stage 推进后续 stage、handoff / release-check 可创建 workflow packet。
- 同步 README、AgentMesh Skill 和 roadmap，补充恢复命令与新的短期里程碑。

## 17:49 - Guided Delivery 与用户抉择门

- 新增 `guided-delivery` MVP workflow，用通用 `review` 阶段审查交付物，不引入 `code-review` 专用 stage。
- 新增 `--user-gate` run metadata，写入 `assignment.toml`、`status.json` 和 `flow status --json`；开启时要求 `--decide current`。
- user-gated decide prompt 会提示入口 agent 汇总 accepted / rejected / needs user decision，并等待用户明确抉择。
- 补充 pytest 覆盖 guided-delivery packet、user-gate metadata、decide prompt 和非法 `--decide` 组合。
- 同步 README、AgentMesh Skill 和 roadmap 的 guided-delivery 示例与语义说明。

## 17:57 - opencode CLI agent 支持

- 新增公开 `opencode-cli` adapter，可通过 `agentmesh agents add ... --adapter opencode-cli` 注册本机 opencode CLI agent。
- `opencode-cli` 使用 `opencode run --model <model> --variant <effort> <prompt>` 调用；`xhigh` 会映射为 opencode CLI 的 `--variant max`。
- 注册层新增 `opencode` / `opencodecli` 简写，并在 `agentmesh adapters list` 中展示 `opencode-cli`。
- 同步 README、AgentMesh Skill 和 roadmap 中的支持范围、示例命令和登录态说明。
- 补充 shell 回归覆盖 adapter 列表、opencode prompt 位置参数、`--variant` 映射和非 stdin dry-run。

## 18:10 - 本地 Workflow TOML Registry

- 新增 `.agentmesh/workflows/*.toml` 项目本地 workflow recipe 读取能力，`agentmesh workflows list/show` 会同时展示内置 workflow 与本地 workflow。
- `agentmesh run --workflow <id>` 现在可以直接使用本地 `status = "mvp"` 的 recipe 创建 run packet，并复用已有 stage、role flag、user gate、resume / retry 机制。
- 本地 workflow TOML 需要 `schema_version = 1`、合法 workflow id、`plan` / `execute` / `review` / `decide` 内的阶段序列和 description；本地 id 不能覆盖内置 workflow id。
- 新增 `examples/workflows/docs-check.toml` 作为本地 recipe 模板。
- 同步 README、AgentMesh Skill 和 roadmap，补充本地 workflow registry 的路径、模板和边界说明。
- 补充 shell / pytest 回归覆盖本地 workflow list/show/run、重复 id 防护和非法 stage 校验。

## 18:17 - Flow Events 轻量事件视图

- 新增 `agentmesh flow events <run-id>`，以紧凑文本展示 run packet 的 `events.jsonl` 时间线。
- 支持 `agentmesh flow events <run-id> --json` 和 `--limit <n>`，方便入口 agent 读取最近事件而不直接解析 raw JSONL。
- 同步 README、AgentMesh Skill 和 roadmap 中关于 packet 可观测性的说明。
- 补充 pytest 覆盖文本事件视图、JSON 输出和 limit 行为。

## 19:26 - Doctor Readiness 与 Skill 安装校验

- 新增 `agentmesh doctor --json`，输出稳定的 readiness 报告，区分 `ready`、`unknown` 和 `not_ready`，并保留 auth probe 状态。
- 文本版 `agentmesh doctor` 为每个 agent 增加 readiness 标识，同时保持原有状态文案可读。
- `agentmesh skill install --target <host>` 写入后会校验目标文件内容，发现缺失、无法读取或内容不匹配时返回清晰错误。
- 新增 `agentmesh skill verify --target <host>`，用于单独检查已有入口宿主安装是否与打包的 AgentMesh Skill 一致。
- 同步 README、AgentMesh Skill 和 roadmap，并补充 shell / pytest 回归覆盖 doctor JSON、安装后校验、缺失安装和内容 mismatch。

## 19:35 - 移除异步 Review 产品口径

- 移除 `review-center` workflow 中 “asynchronous review” 的触发描述，避免把现有分阶段 dispatch / attach 能力误读成后台异步任务系统。
- 保留 reviewer 输出落盘、手动 attach 和 decider 汇总机制；产品口径统一为 reviewer evidence 与决策前质量门。

## 20:19 - 短期方案收口与外审修复

- `release-check` 新增并维护 `release-summary.md`，自动汇总 diff / verification、review findings、recent events、缺失证据和 residual risk；review / decide prompt 会携带这份 evidence summary，并要求最终 verdict 使用 `ready` / `not_ready` / `needs_decision`。
- `agentmesh flow events` 新增 `--follow`、`--follow-poll-secs` 和 `--follow-timeout-secs`，支持本地 tail run packet 的新增事件；`--follow-timeout-secs 0` 改为非法参数，避免误解为无超时。
- `agentmesh doctor --json` 增加 `help_probe` 和 `hints`，文本输出也显示 help probe 与修复提示；help probe 现在按 `agent["command"] + agent["args"] + --help` 检查实际 worker invocation。
- 根据 Gemini 与 Claude 4.7 外审结果修复 release evidence 判定：`Scoped Git Diff` 只有 `Command:` 和 `(no scoped diff)` 时不再被误判为存在 diff evidence。
- 补充真实 `flow events --follow` 增量输出测试，通过后台追加 event 覆盖 polling、`seen_count` 和新增事件打印路径。
- 同步 README、AgentMesh Skill 和 roadmap 的 release-check summary、events follow、doctor hints 说明。

### 验证

- `make check` 通过：shell 测试 `PASS`，pytest `55 passed`。
- `git diff --cached --check` 通过，且确认无 `__pycache__` 残留。

## 20:32 - MCP Client Context Ingestion MVP

- 新增最小 MCP stdio client，按 MCP lifecycle 执行 `initialize` /
  `notifications/initialized`，并通过 `resources/read` 拉取文本资源。
- `agentmesh run` 和 `agentmesh flow run` 新增可重复的
  `--mcp-resource <server-id>:<resource-uri>`，读取结果会写入 run packet
  的 `context.md`，后续 dispatch prompt 会自动携带。
- `agentmesh.toml` 新增可选 `[mcp_servers.<id>]` 配置，支持 `command` 和
  `args`；AgentMesh 不保存 MCP 登录态，只作为 client 读取外部上下文。
- 根据 Claude 4.7 与 Gemini 外审结果，MCP client 现在会把非 object
  JSON-RPC 响应包装为 `AgentMeshError`，并在初始化失败、timeout、非法响应等
  错误中附带 bounded stderr tail，方便定位 MCP server 启动配置问题。
- 同步 README、AgentMesh Skill 和 roadmap，明确 MCP client 是长期方案第一
  块 MVP，MCP server mode 仍留到后续。
- 补充 pytest 覆盖 legacy `flow run`、workflow-backed `run --workflow` 和未知
  MCP server 的错误路径，以及非法 JSON-RPC 响应和 server stderr 诊断。

### 验证

- `make check` 通过：shell 测试 `PASS`，pytest `60 passed`。
- 确认无 `__pycache__` 残留。

## 23:05 - Release Verdict Gate 与 Doctor Verify 收口

- `release-check` 的 `decide` stage 现在必须在 `decision.md` 中给出且只给出一条 verdict：`ready`、`not_ready` 或 `needs_decision`；缺失、重复或非法 verdict 会失败在 `decide`，写入 `release_verdict` diagnostic，并记录 `release.verdict.invalid`。
- `release_verdict` 会同步进入 `status.json`、`flow status` / `flow status --json`、`events.jsonl` 和 `release-summary.md`；`flow retry` / `flow attach` 会重新解析并覆盖 stale verdict，同时保护已完成 artifact。
- verdict parser 根据外审反馈支持常见 LLM 输出格式，包括 list item、bold label、inline-code value、重复空格 / 连字符 / 下划线、尾随说明，以及 blockquote 中的 verdict；fenced code block 示例仍会被忽略。
- 将用户可见的 `opencode` 文案统一为标准写法 `OpenCode` / `OpenCode CLI`，同时保留机器可读 id 和命令名 `opencode-cli`、`opencode`。
- `agentmesh doctor --json` 增加 `version_probe` 和独立的 `non_interactive` 字段；AI CLI 通过 auth probe 证明非交互可用，generic command adapter 因没有内置认证探针而保持 `non_interactive = unknown`。
- `agentmesh doctor` 文本输出显示 help / version probe；`agentmesh skill verify --target <host> --json` 输出机器可读的 Skill 安装状态，覆盖 `ok`、`missing` 和 `content_mismatch`。
- 同步 README、AgentMesh Skill、root skill 副本和 roadmap，补充 release verdict gate、doctor readiness、Skill verify JSON、OpenCode 命名和短期里程碑状态。
- 先后送 Claude 4.7 / Gemini / MiMo 外审；已修复成立的 Should Fix，4.7 后续复审受额度限制，Gemini 复审遇到模型容量 429 后按用户要求改由 MiMo review。

### 验证

- `make test` 通过：shell 测试 `PASS`，pytest `67 passed`。
- `git diff --check` 通过，且确认无 `__pycache__` 残留。

## 23:40 - TS-on-Node Runtime 第一片

- 新增 TS/Node 并行入口 `agentmesh-ts`，当前先支持 `packet validate <run>`，
  用于验证 Python CLI 生成的 run packet。
- 新增 `src-node/`、`tests-node/`、`package.json`、`tsconfig.json` 和
  `package-lock.json`，构建产物输出到不提交的 `dist-node/`。
- `packet validate` 会校验 `status.json` 必填字段、`artifacts.toml` artifact
  文件存在性、`events.jsonl` JSONL 合法性，并支持 `--json` 输出。
- `make test` / `make check` 现在会同时运行 Python 回归和 TS/Node 测试。
- 同步 README 与 root `plan.md`，明确 TS/Node 是长期 runtime 方向，但迁移采用
  并行 slice，不一次性替换 Python CLI。

### 验证

- `npm test` 通过：TS build 通过，Node test `4 passed`。
- `make check` 通过：shell 测试 `PASS`，pytest `67 passed`，Node test
  `4 passed`。
- Python CLI 临时生成的真实 run packet 可被 `agentmesh-ts packet validate
  --json` 验证为 `ok: true`。
- `git diff --check` 通过，且确认无 `__pycache__` 残留。

## 23:51 - TS-on-Node Workflow Registry

- `agentmesh-ts` 新增 `workflows list` 和 `workflows show <id>`，支持文本输出和
  `--json` 机器可读输出。
- 新增 TS 侧内置 workflow catalog，并实现 `.agentmesh/workflows/*.toml` 本地
  workflow 读取。
- 本地 workflow 校验与 Python 侧保持一致：`schema_version = 1`、合法 id、
  `mvp` / `planned` 状态、非空且不重复的合法 stage、本地 id 不能覆盖内置 id。
- 新增轻量 TOML root parser，覆盖当前 workflow recipe 使用的字符串、数字、
  布尔值和字符串数组。
- 同步 README 与 root `plan.md`，把下一片推进到 status / events / artifact
  读写迁移。

### 验证

- `npm test` 通过：TS build 通过，Node test `9 passed`。
- `agentmesh-ts workflows list/show --json` dogfood 通过，能读取内置
  `release-check` workflow。
- `make check` 通过：shell 测试 `PASS`，pytest `67 passed`，Node test
  `9 passed`。

## 23:59 - TS-on-Node Packet IO

- 新增 TS packet IO 模块，支持 `status.json` 读取 / 写回、`events.jsonl`
  追加 / 读取、`artifacts.toml` 读取 / 写回和 artifact 登记。
- `agentmesh-ts` 新增 `packet status`、`packet events` 和 `packet artifacts`，
  支持文本输出和 `--json` 机器可读输出。
- `packet validate` 改为复用同一套 TS packet IO parser，避免校验和实际读写
  走两套协议解析。
- 事件追加使用 `schema_version = 1`，artifact 写回保持 packet-relative path，
  兼容 Python CLI 生成的 run packet。
- 同步 README 与 root `plan.md`，把下一片推进到 release-check evidence
  aggregation。

### 验证

- `npm test` 通过：TS build 通过，Node test `13 passed`。
- Python CLI 临时生成的真实 run packet 可被 TS CLI 读取 `status`、`events`、
  `artifacts`；TS IO 追加 event 并登记 artifact 后，`packet validate --json`
  仍返回 `ok: true`、`artifactCount: 7`、`eventCount: 2`。
- `make check` 通过：shell 测试 `PASS`，pytest `67 passed`，Node test
  `13 passed`。
