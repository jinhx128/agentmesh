# 2026-09-04

## 22:07 - 准备 AgentMesh 0.2.0 发布

- 将 root、全部 workspace、内部 exact dependencies、Tauri/Cargo、stable/beta updater 示例、运行时版本常量和版本测试同步到 `0.2.0`。
- 汇总 Packet v2、Call 结果处理、Run/Call 详情与产物展示、Studio 活动导航和设置版本更新能力，新增 `docs/distribution/v0.2.0-release-notes.md`。
- 保留旧版本发布说明和历史计划，不修改历史记录；本机生成的 `.agents/` Skill 安装文件不纳入仓库提交。

### 验证

- 版本占用检查：远端不存在 `v0.2.0`，npm registry 不存在 `@jinhx128/agentmesh@0.2.0`。
- 发布凭据检查：GitHub CLI 与 Tauri updater 签名凭据可用；npm 当前未登录，npm 发布待登录后执行。

## 22:34 - 发布 AgentMesh 0.2.0 并完成本机同步

- Release commit `36cbc28` 与 annotated tag `v0.2.0` 已推送到 `main` / `origin`；GitHub Release 已发布为 latest、非 draft、非 prerelease。
- GitHub Release 七项资产已上传并通过远端校验：CLI tgz、Apple Silicon DMG、Tauri updater archive/signature、`latest.json`、Skill markdown 与 `SHA256SUMS`。
- `@jinhx128/agentmesh@0.2.0` 已发布到 npm `latest`；registry shasum `92f3661803beeb10ae81d536f6b51f66bc88f0ba` 与发布 tgz 一致。
- ai-config 更新脚本已按 Release 校验并同步 fnm CLI、`/Applications/AgentMesh.app` 与 `~/.agents/skills/agentmesh/SKILL.md`；Homebrew CLI 也已更新。两套 CLI、桌面 App、共享 Skill 均报告 `0.2.0`。

### 最终验证

- `npm test`：709 passed，0 failed。
- `cargo test --manifest-path apps/studio-desktop/src-tauri/Cargo.toml`：4 passed，0 failed。
- `npm run check:boundaries`、`npm run studio-desktop:package:dev` 与 GitHub Release 远端资产校验均通过。
