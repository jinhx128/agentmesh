# 2026-08-25

## 17:08 - 准备 AgentMesh 0.1.15 发布

- Studio 写操作统一使用 toast 反馈：成功后关闭或重置当前操作，失败时保留弹窗、输入和上下文；覆盖 Agent、Workflow、Preset、设置、CLI/Skill 安装、活动删除、Call 采纳、Reviewer Session 与 Run 安全操作。
- 修复 Agent 编辑模型列表加载和 Codex 非 Git 顶层工作区调用，缩短模型缓存与 Codex 保存就绪检查等待；增强活动列表未选中项的分隔层级。
- root、全部 workspace、内部 exact dependencies、SDK/runtime version、MCP client、Tauri/Cargo、stable/beta metadata examples、README 与版本测试同步到 `0.1.15`。
- 新增 `docs/distribution/v0.1.15-release-notes.md`，记录兼容性、升级渠道和七项发布资产。

### 验证

- `npm test`：703/703 通过；版本夹具定点回归 9/9 通过。
- `npm run check:boundaries`、`cargo check --manifest-path apps/studio-desktop/src-tauri/Cargo.toml`、`npm run studio-desktop:package:dev` 与 `npm run cli:install-smoke` 通过；Desktop 分发 smoke 为 `ok=true`、`issues=[]`。
- `npm audit --audit-level=low` 为 0 vulnerabilities；`git diff --check` 通过。
