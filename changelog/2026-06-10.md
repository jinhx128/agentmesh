# 2026-06-10

## 19:30 - Studio agent lifecycle provider discovery 与手册补充

- Studio 手册的核心概念新增 `Stage 类型` 小节，说明 `plan`、`execute`、`verify`、`review`、`decide` 五类 stage 及其规范产物：`plan.md`、`handoff.md`、`verification.md`、`findings.md`、`decision.md`。
- Runtime agent create/update lifecycle 新增 `providerToolDiscovery` 透传，model resolution 与 readiness probe 统一使用以当前 workspace 为默认 `workspace` 的 provider CLI discovery。
- Studio agent update 新增回归测试：当 provider CLI 不在 `PATH`、login shell 不可用，但 CLI 位于 well-known user bin 路径时，agent lifecycle update 仍可发现并完成 readiness。
- 调整新增测试的环境恢复路径，确保 `PATH` / `SHELL` 修改后即使 server 启动失败也会在 `finally` 中恢复。

### 验证

- `npm run build:node`
- `node --test --test-name-pattern "Studio agent lifecycle discovers provider CLIs outside PATH during update" dist-node/tests-node/studio.test.js`
- `npm run build:studio-frontend`
- `git diff --check`
