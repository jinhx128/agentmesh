# 2026-07-19

## 00:40 - 增加五宿主 Reviewer Scope 续传协议

- Canonical AgentMesh Skill 统一 Codex、Cursor、Claude、Antigravity、OpenCode 的普通连续 review 调用方式：安全 native conversation identity 优先，否则首次创建一个 `amscope_v1` token，同一宿主对话后续原样传递。
- Scope token 只保留在 entry-agent 对话上下文，不写入 workspace 或 packet；token 丢失/无效时 fresh，禁止从 repository、worktree、旧 packet、provider state 或其他宿主对话恢复 scope。
- 其他宿主仅替换 `--host-kind`，不得复制 provider/native session ID；正式 review/release gate 固定使用 `independent`，continuous non-hermetic evidence 不能静默满足正式门禁。
- 新增 contract test 锁定 `correlation_token`、`--host-kind`、scope 原样复用、missing/invalid fresh、native precedence、no workspace fallback 与 formal independent gate 语义。

### 验证

- TDD RED 1/1 预期失败、GREEN 1/1；fresh build 后 package-structure + management-cli 34/34，skill verify/install/output 定向 11/11，`git diff --check` 通过。
- AgentMesh 初审的 4 个 Should 全部关闭；最终 gate `workflow-20260719003652` 为 LGTM、0 Must/0 Should/0 Nit 且 `decide_completed`。

## 01:14 - 增加 Studio Reviewer Session 脱敏管理

- SDK 新增 run-level Reviewer Session 安全投影，只输出 `session_ref`、`host_kind`、`agent_id`、`mode`、`last_used_at`、`expires_at`、`hermetic`；与 registry metadata 按安全 ref、completed attempt 和 actual agent 交叉校验，SDK 仍不依赖 runtime。
- App Server 使用 runtime safe list/close/purge 原语，为 run detail 补充 active session metadata；关闭与过期清理复用本机 origin/auth、registry lock/CAS，成功响应也逐字段白名单投影。
- Studio 详情展示 reviewer、host、mode、独立上下文、上次使用与过期时间，可关闭单个 session 或清理 expired；操作成功后刷新当前运行。八类 `reviewer_session.*` event 已本地化。
- SDK 和 App Server 均验证夹带的 provider/native session 字段不能进入 API/UI；旧 packet、无 active session 或 registry 缺失仍保持可读。

### 验证

- Fresh Node build 后 SDK/App Server/Studio UI 三文件 77/77；Studio frontend build、`npm run check:boundaries`、`git diff --check` 通过。
- Claude 外审被 provider safety false-positive 拒绝且无技术 finding；Cursor 初审的 2 Should/1 Nit 全部关闭，最终 gate `workflow-20260719011119` 为 LGTM、0 Must/0 Should/0 Nit。

## 01:42 - 完成跨宿主 Reviewer Session 管理收尾

- 隔离 browser 与 debug desktop `.app` 完成跨宿主 session 管理 smoke：Claude/OpenCode run 不串线，八类 session 事件中文化，过期清理与逐个关闭均生效，最终 CLI registry 为空。
- SDK 继续只投影七个安全字段；App Server close/purge 复用 loopback、认证、registry lock/CAS；Studio 不接收或展示 provider/native session ID。smoke 使用临时 HOME/workspace，未触碰真实登录态和 reviewer registry，结束后清理全部临时进程与目录。
- debug `.app` 可运行；外围 Tauri debug bundle 命令缺少 updater signing key，不作为 P4 功能缺陷，也不声明已生成正式发布产物。

### 验证

- Fresh `npm run build` 通过；SDK/Studio/package/management/readiness 六文件 154/154；`npm run check:boundaries` 与 `git diff --check` 通过。
- AgentMesh P4 总 gate `workflow-20260719013846` 返回 LGTM、0 Must/0 Should/0 Nit，并完成 `decide`。

## 09:31 - 补齐 Reviewer Session 用户文档

- README 新增完整的 Reviewer Session 复用说明；落地页增加会话模式、支持矩阵、生命周期、安全边界和管理命令；Studio Manual 明确区分 Run resume 与 reviewer provider-session resume，并补充首次 scope、列表、详情、关闭和过期清理。
- 文档统一 `interactive_continuous`、`auto`、`independent`、non-hermetic、2 小时空闲/12 小时绝对/8 次 resume、一次有界 fresh recovery 和 auth/permission/trust hard-fail 语义。
- Claude Code 与 OpenCode 仅标记为 experimental；Codex、Cursor、Antigravity 保持 fresh-only。入口宿主可以传递 conversation scope，不代表对应 reviewer provider 已启用 resume；不得从 workspace、repository、worktree、旧 packet、provider state 或其他宿主对话恢复 scope。

### 验证

- TDD 首轮 RED 命中三处缺失；GREEN full build 后 package/UI 48/48。接受外审 6 Should/2 Nit 后新增定向 RED，修复后 focused 2/2、fresh full build + 48/48、`npm run check:boundaries`、CLI help/文案 grep 与 `git diff --check` 通过。
- 初审 `workflow-20260719015224` 的发现全部关闭；定向 follow-up `workflow-20260719092738` 为 LGTM、0 Must/0 Should/0 Nit，两个 gate 均完成 `decide`。

## 10:14 - 收紧 Reviewer Session 默认启用门禁

- 使用同一个临时 Git fixture、同一 diff/prompt/scope 和同一授权绕过缺陷实测 Claude Code 与 OpenCode。两者 continuous structured start 均失败，没有产生合格 resumed arm；OpenCode independent 对照又在 240 秒超时，不能证明 latency、tool-read 或质量收益。
- Claude independent fresh 与默认 gate 后的 hermetic fresh 均输出有效、检出注入缺陷且无 false-LGTM。基于真实 A/B 而非早期精确回复 probe，所有五个内置 reviewer provider 现在默认保持 fresh-only。
- 底层 Claude/OpenCode argv/parser probe 继续保留；只有 disposable fake-CLI 集成测试显式打开实验环境变量，生产 dispatch 默认绕过 session registry。README、落地页、Studio Manual 和 capability matrix 已同步事实变化。

### 验证

- TDD RED 命中默认 runtime 仍启用 Claude；GREEN adapter 5/5、fake session flow 3/3。Fresh build 后 10 文件 session/release/package/Studio 定向 211/211，fake-CLI E2E cleanup=true 且 provider ID/scope token 泄漏为 0，`npm run check:boundaries` 与 `git diff --check` 通过。
- 四个真实 A/B disposable packet root 的敏感字段扫描为 0，随后全部清理。P5.2 gate `workflow-20260719100857` 返回 LGTM、0 Must/0 Should/0 Nit，并完成 `decide`。

## 10:33 - 完成 Reviewer Session 发布门禁

- 完成 `3da31d3..bfeaa69` 的项目总收尾，聚合 85 个文件的交付范围、阶段 gate、跳过项、残余风险和回滚路径；release evidence 使用 compact summary，未向 reviewer 注入 1.4 万行完整 diff。
- README、落地页、Studio Manual、canonical Skill、capability matrix、CLI help、Studio copy 与 changelog 已统一：所有五个内置 reviewer provider 默认 fresh-only，正式 gate 固定 `independent`，会话生命周期与管理命令保持一致。
- 真实 resumed A/B 缺失、`tool_reads` 不可观测，以及签名/公证/updater 产物未构建均保留为显式边界；本次不升级版本、不 push、不发布 npm/GitHub/Desktop，也不安装本机正式产物。

### 验证

- Fresh `npm run check:boundaries && npm test && git diff --check` exit 0；Node 与 Studio 重新构建，全量 699/699、0 fail、0 skipped、0 todo。
- Independent Release Check `workflow-20260719103029` 返回 LGTM、0 Must/0 Should/0 Nit；diff、verification、review output 均 present、无 missing evidence，最终 `Verdict: ready` 且 `decide_completed`。
