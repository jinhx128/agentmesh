# 2026-06-16

## 10:31 - Studio 产物侧栏与抽屉预览修复

- 在运行详情页增加当前节点产物侧栏，点击产物后通过右侧抽屉展示内容，并调整运行区左右布局与抽屉边距。
- 统一产物展示名称，预览标题、抽屉标题和产物信息优先使用中文名称；产物计数文案同步进入 `copy.ts`。
- 修复产物抽屉在切换到非运行页面时仍可能覆盖界面的问题，并避免 run detail 临时 reload 时直接关闭已打开的抽屉。
- 补充 Studio UI 测试，覆盖产物侧栏、中文产物名、抽屉打开条件和布局选择器。

### 验证

- `npm run build:node && node --test dist-node/tests-node/studio-ui.test.js`

## 10:37 - CLI 运行后自动登记工作区

- `agentmesh run <preset>` 和 `agentmesh flow run` 在成功创建运行包后，会 best-effort 记录当前工作区到用户 workspace registry，Studio 后续可自动看到对应项目记录。
- 抽出 CLI 共享的工作区活动登记 helper，`agentmesh call` 继续复用同一套容错逻辑；registry 损坏或写入失败不会影响运行包创建。
- `recordWorkspaceActivity` 增加保留禁用态选项，CLI 自动登记不会把用户手动禁用的工作区重新启用。
- 补充 registry、preset run、workflow run 测试，覆盖成功登记、校验失败不登记、registry 损坏不阻塞、禁用项不自动启用。

### 验证

- `npm run build:node && node --test dist-node/tests-node/workspace-registry.test.js dist-node/tests-node/flow-preset-run.test.js dist-node/tests-node/flow-run.test.js`

## 10:48 - CLI 推进运行后刷新工作区记录

- `agentmesh flow dispatch`、`flow retry`、`flow resume` 和 `flow attach` 成功修改运行包后，会刷新当前工作区在 workspace registry 中的活动时间。
- 保持 `status`、`events`、`prompt`、`packet` 等只读入口不写 registry；参数校验失败的写入口也不会刷新。
- 补充 flow dispatch 测试，覆盖 dispatch、retry、resume、attach 四类推进入口的自动记录刷新。

### 验证

- `npm run build:node && node --test dist-node/tests-node/flow-dispatch.test.js`

## 14:25 - 发布 0.1.6 并规范自动记录命名

- 默认运行记录命名改为按入口类型区分：preset 执行生成 `preset-YYYYMMDDHHmmss`，workflow 执行生成 `workflow-YYYYMMDDHHmmss`，直接调用生成 `call-YYYYMMDDHHmmss`。
- 同一秒重复创建时追加数字后缀 `-1`、`-2`，并通过原子目录预留避免并发创建时抢到同一个记录名。
- 版本统一提升到 `0.1.6`：同步根包、workspace 包、内部依赖、package lock、Tauri/Cargo、SDK/runtime 默认版本、MCP client version、README、Desktop updater 示例元数据和测试期望。
- 补充生成 ID、preset run、workflow run 和 call history 回归，覆盖紧凑本地时间、重复后缀、默认前缀和 CLI 输出。

### 验证

- `npm run build:node`
- `node --test dist-node/tests-node/generated-id.test.js dist-node/tests-node/flow-preset-run.test.js dist-node/tests-node/flow-run.test.js dist-node/tests-node/call-history.test.js`
- `npm run build:studio-frontend`
- `npm run build && node --test dist-node/tests-node/*.test.js`：513/513 通过
- `npm run release:assets`：通过，生成 `dist-release/` 下 4 个 `0.1.6` 发布资产
- `node dist-node/packages/cli/src/cli.js --version`：`0.1.6`
- `hdiutil verify dist-release/AgentMesh_0.1.6_aarch64.dmg`：VALID
- `dist-release/SHA256SUMS`：
  - `6e1c21c5a4f9596d107e1febe98b640930de3aacf7b699416787d8bcd877ba1d  dist-release/agentmesh-0.1.6.tgz`
  - `7f46f777e93d89741e6538b23b5b36b67735da60b0629b9a4fc261d2dd1619d0  dist-release/AgentMesh_0.1.6_aarch64.dmg`
  - `d262ae3cbe9d32b87fa4f496073eda2d78f3e7a983a639da880fbfb818aad147  dist-release/agentmesh-skill-0.1.6.md`
