# 2026-05-17

## 23:02 - 校准 Plan C 全量执行状态

- `plan.md` 补齐 P2 phase gate 的完成状态；P2.1-P2.8d 与 P2.Z 已覆盖 Runs、Calls、artifacts、events、review/release、safe actions、agent lifecycle 和 direct call adoption 的 React parity 证据。
- P4 明确保持 optional capability additions，不作为 P0-P3 Plan C required execution path 的完成阻塞项；只有出现产品证据时才启动 React Flow、Monaco、xterm.js、专用状态库或高级 table 能力。
- Current next step 改为 P0-P3 required Plan C execution 已关闭到 internal unsigned DMG handoff；public signed/notarized distribution 仍是后续独立 release gate。

### 验证

- `npm test`（443/443）
- `git diff --check`

## 22:46 - 完成 P3.4-P3.Z internal unsigned DMG 与 co-install 收尾

- 通过 rustup 安装并验证本机 Rust/Cargo 后，重新执行 Tauri unsigned DMG build，产出
  `apps/studio-desktop/src-tauri/target/debug/bundle/dmg/AgentMesh_0.1.0_aarch64.dmg`。
- 修复 Tauri `src-tauri` 到根目录 `dist-node` 的相对路径；新增标准 Tauri icon 文件；保留源 SVG。
- `sidecar-bundle` 现在同时支持本地 `dist-node/.../sidecar` 和 packaged
  `AgentMesh.app/Contents/Resources/dist-node/...` 布局，并把 production runtime
  dependencies 复制到 `dist-node/apps/studio-desktop/runtime-node_modules`，并在 Tauri 包内映射为
  `Resources/dist-node/node_modules`，避免包内 Node 依赖仓库根 `node_modules`。
- `tsconfig.json` 排除 Tauri `gen/` 和 `target/` 构建产物，避免 DMG resource copy 里的
  dependency `.d.ts` 被 TypeScript 主项目误扫描。
- 新增 mounted-DMG co-install smoke 证据：空 `PATH` 启动包内 sidecar，app 通过 bundled CLI
  写 artifact，source/global CLI 读取结果，detach DMG 后 CLI 仍可读 run status。
- 新增 `docs/reviews/studio/p3-internal-dmg-2026-05-17.md`、
  `docs/reviews/studio/p3-cli-dmg-coinstall-2026-05-17.md` 和
  `docs/reviews/studio/p3-desktop-packaging-phase-2026-05-17.md`；`plan.md` 标记
  P3.4、P3.5、P3.Z 完成，当前下一步切到 P4 optional capability additions gating。

### 验证

- `npm run build && node --test dist-node/tests-node/studio-desktop-distribution.test.js`（6/6）
- `npm run studio-desktop:package:dev`
- `npx --yes @tauri-apps/cli@latest build --config apps/studio-desktop/src-tauri/tauri.conf.json --bundles dmg --debug`
- `shasum -a 256 apps/studio-desktop/src-tauri/target/debug/bundle/dmg/AgentMesh_0.1.0_aarch64.dmg`：
  `5ca5a3901c2f05d5d4ab1f64c82c9ef282cad7e5a22df3854d1be36d86e972c7`
- `hdiutil verify apps/studio-desktop/src-tauri/target/debug/bundle/dmg/AgentMesh_0.1.0_aarch64.dmg`
- mounted-DMG sidecar smoke：空 `PATH` readiness 通过，`runtime_cli_path` 位于
  `AgentMesh.app/Contents/Resources/dist-node/packages/cli/src/cli.js`
- mounted-DMG co-install smoke：app mutation 使用 bundled CLI，source/global CLI 读取 artifact，
  detach DMG 后 source/global CLI 仍可读 status
- `npm test`（443/443）

## 22:16 - P3.4 internal unsigned DMG build blocked

- 已尝试执行 P3.4 unsigned DMG build；`npm run build` 和 `sidecar-bundle.js --verify` 通过。
- `npx --yes @tauri-apps/cli@latest --version` 可运行，版本为 `tauri-cli 2.11.2`。
- Tauri build 在 `cargo metadata` 阶段失败，因为本机没有 `cargo` / `rustc`。
- 新增 `docs/reviews/studio/p3-internal-dmg-blocker-2026-05-17.md`，记录环境、尝试命令、失败原因和未产出的 DMG / checksum / install smoke / WKWebView smoke。
- `plan.md` 将 P3.4 标为 blocked progress，P3.5 / P3.Z 暂不推进，避免把缺失的真实 DMG 证据伪装成完成。

### 验证 / 阻塞证据

- `cargo --version`：command not found
- `rustc --version`：command not found
- `npx --yes @tauri-apps/cli@latest --version`：`tauri-cli 2.11.2`
- `npm run build && node dist-node/apps/studio-desktop/src/sidecar-bundle.js --verify && npx --yes @tauri-apps/cli@latest build --config apps/studio-desktop/src-tauri/tauri.conf.json --bundles dmg --debug`：失败于 `cargo metadata`

## 22:08 - 完成 P3.3 CLI Studio path preservation

- 新增 `docs/reviews/studio/p3-cli-studio-path-preservation-2026-05-17.md`，记录 CLI Studio 与 desktop Studio 的 session / binary path 分离证据。
- `tests-node/studio-distribution-coexistence.test.ts` 新增同 workspace 同时启动 CLI Studio 与 desktop Studio 的回归测试。
- 测试确认 CLI Studio 与 desktop Studio 使用不同动态端口；CLI path 保持本地 browser wrapper 行为，desktop path 继续要求 launch token。
- 测试确认 CLI Studio mutation 使用 `dist-node/packages/cli/src/cli.js`，desktop mutation 使用 app-bundled runtime CLI path。

### 验证

- `npm run build && node --test dist-node/tests-node/studio-distribution-coexistence.test.js --test-name-pattern "CLI Studio and desktop Studio"`
- `node --test dist-node/tests-node/studio-distribution-coexistence.test.js dist-node/tests-node/studio-cli.test.js`（10/10）

## 21:59 - 完成 P3.2 sidecar packaging proof

- 新增 `apps/studio-desktop/src/sidecar-bundle.ts`，生成 target-triple sidecar launcher，并复制 app-bundled Node runtime 到 `dist-node/apps/studio-desktop/sidecar/node`。
- Tauri `externalBin` 改为 `../../dist-node/apps/studio-desktop/sidecar/agentmesh-studio-sidecar`，Rust 壳改为启动 `agentmesh-studio-sidecar`。
- Tauri `bundle.resources` 纳入 `../../dist-node`，让 launcher、desktop host entrypoint、React assets 和 runtime CLI 保持 app-managed bundle layout。
- `studio-desktop:package:dev` / `studio-desktop:package:signed` / `studio-desktop:update:metadata` 现在先生成 sidecar bundle，再跑 distribution smoke。
- 新增 `docs/reviews/studio/p3-sidecar-packaging-proof-2026-05-17.md`，记录 sidecar bundle、no-PATH mutation smoke 和本机未跑真实 Tauri app bundle 的限制。

### 验证

- red `npm run build && node --test dist-node/tests-node/studio-desktop-distribution.test.js`
- green `npm run build && node --test dist-node/tests-node/studio-desktop-distribution.test.js`（5/5）
- sidecar no-PATH smoke：空 `PATH` 下启动 target-triple launcher，App Server mutation 使用 bundled sidecar Node 作为 `process.execPath`。
- `npm run studio-desktop:package:dev`

## 21:48 - 完成 P3.1 Tauri shell 到 App Server 接线

- 新增 `apps/studio-desktop/src-tauri/Cargo.toml`、Rust entrypoint、Tauri capability 和 `apps/studio-desktop/shell/index.html`，Tauri 先加载本地 bootstrap page，再由 Rust 壳启动 app-managed sidecar。
- Desktop sidecar 支持 `--launch-json`，输出 `agentmesh_studio_ready` JSON；Tauri Rust 壳解析该事件并导航到动态 `127.0.0.1` tokenized App Server URL。
- Desktop host 默认传入 built Vite React asset directory，避免 packaged desktop path 继续落到旧 embedded Studio fallback。
- Distribution smoke 增加 `frontendDist="../shell"` 和 `bootstrapPage="index.html"` 输出与校验。
- 新增 `docs/reviews/studio/p3-tauri-shell-wiring-2026-05-17.md`，记录 P3.1 的 source wiring、验证证据和本机无法运行真实 WKWebView 的限制。

### 验证

- red `npm run build:node && node --test dist-node/tests-node/studio-desktop-options.test.js`
- green `npm run build && node --test dist-node/tests-node/studio-desktop-options.test.js dist-node/tests-node/studio-desktop-distribution.test.js`（13/13）
- sidecar launch smoke：`--launch-json` 输出 ready event，动态 tokenized URL 返回 built React assets。
- `npm run studio-desktop:package:dev`
- desktop shell source scan：Rust/Tauri shell 与 bootstrap page 未命中 runtime / packet / workflow / `.agentmesh` 逻辑。
- `git diff --check`

## 21:32 - 完成 P3.0 Tauri desktop shell spike

- 新增 `docs/reviews/studio/p3-tauri-shell-spike-2026-05-17.md`，记录 Tauri/Electron 决策、macOS 版本、CPU target、sidecar packaging notes、验证证据和本机未跑项。
- `apps/studio-desktop/distribution/macos.json` 明确首个桌面目标为 `darwin-aarch64`，macOS minimum version 继续为 `12.0`，shell spike 决策为 `continue-tauri`。
- Desktop host 新增 restart helper，覆盖 sidecar restart 后旧 server 停止、launch token 轮换、旧 token 401、新 token 200 的行为。
- Distribution smoke summary 增加 shell spike 和 target architecture 校验，确保 app-managed `externalBin` 路线、WKWebView 后续 smoke gate、Electron fallback threshold 都有机器可读记录。
- 本机可用 macOS arm64 + codesign，但没有 Rust/Cargo/Tauri CLI；真实 Tauri WKWebView smoke 与 unsigned DMG build 记录为 P3.1/P3.4 的打包机验证门槛，不把它们伪装为已完成。

### 验证

- red `npm run build:node && node --test dist-node/tests-node/studio-desktop-options.test.js dist-node/tests-node/studio-desktop-distribution.test.js`
- green `npm run build:node && node --test dist-node/tests-node/studio-desktop-options.test.js dist-node/tests-node/studio-desktop-distribution.test.js`（11/11）
- `npm run studio-desktop:package:dev`
- `node dist-node/apps/studio-desktop/src/distribution-smoke.js --mode signed --dry-run`
- `node dist-node/apps/studio-desktop/src/distribution-smoke.js --mode metadata`
- 工具链探测：`cargo` / `rustc` / local `tauri` CLI 不可用；`xcrun --find codesign` 可用。

## 21:16 - 完成 P2.Z Studio React workbench phase review

- 新增 `docs/reviews/workflow/p2-studio-react-workbench-2026-05-17.md`，汇总 P2.1-P2.8d 的 React workbench 迁移、验证证据、review 结论和 residual risks。
- P2 覆盖 Runs、timeline、artifacts、events、review/release、safe actions、agent lifecycle、Calls list/detail 和 call adoption；React frontend 仍只走 App Server API。
- Chrome headless smoke 使用 built React assets + launch token，桌面宽度覆盖 Runs / Calls / artifacts / events / review-release / safe actions / agent lifecycle，375px 宽度确认无水平溢出。
- 旧 embedded fallback 暂不在 P2.Z 删除；它仍作为无 `assetDir` CLI Studio fallback 保留，后续应在 P3.0/P3.1 证明桌面和 CLI serving path 后单独清理。
- `plan.md` 已标记 P2.Z 完成，并把下一步切到 P3.0 Tauri shell spike。

### 验证

- `npm run build`
- `npm test`（437/437）
- Chrome browser smoke：1365px desktop + 375px mobile，覆盖核心 workbench workflows。
- frontend source-boundary scan：无 runtime / SDK / Node / direct workspace access 命中。
- Calls cleanup scope scan：无 archive / delete / cleanup action 命中。

## 21:05 - 决定 Calls tab 首版不做 cleanup

- P2.8d 明确首版 Calls tab 只包含 direct call 的读取、adoption state、stale / dangling / read-only / superseded 状态展示。
- 当前 scope 不提供 archive / delete / cleanup 操作，也不会移动或删除 `.agentmesh/calls/*` 目录。
- 后续如果要加入清理能力，必须单独切片实现 runtime helper、App Server endpoint、确认 UI、`_archive` 行为，并用测试证明 linked committed files 不会被删除。
- `plan.md` 已记录该决策并把下一步切到 P2.Z phase review。

### 验证

- `rg -n "archive|delete|cleanup|remove|清理|删除|Archive|Delete" apps/studio/src/frontend/features/calls apps/studio/src/frontend/api/calls.ts apps/studio/src/calls-browser.ts`
- `node --test dist-node/tests-node/studio-ui.test.js --test-name-pattern "React call detail"`
- `git diff --check`

## 18:57 - 完成 P1.Z React baseline phase review

- 完成 P1.1-P1.3：新增 Vite + React build baseline、typed Studio API client / bootstrap、React catalog pilot view，并保持旧 embedded Studio shell 可达。
- 新增 `docs/reviews/workflow/p1-react-baseline-2026-05-17.md`，记录 P1 diff summary、验证证据、browser smoke、source scan、review finding 和 residual risks。
- Browser smoke 发现 React 默认 fetch 在 Chrome 中可能触发 `Network request failed`，已改为通过 `globalThis.fetch(...)` 调用，并补回归断言。
- `plan.md` 将 P1.3 标记完成，P1.Z review 结论为 ready for P2。

### 验证

- `npm run build`
- targeted `node --test dist-node/tests-node/studio-ui.test.js dist-node/tests-node/studio.test.js dist-node/tests-node/package-structure.test.js`
- `npm test`（414/414）
- Chrome browser smoke：built React asset path + launch token auth；URL token 被清理，React catalog pilot 渲染 agents / workflows / MCP。
- frontend source scan：无 runtime package import、无 direct packet mutation、无 local/session storage、无 fixed port。
- `git diff --check`

## 18:21 - 完成 P0.Z runtime contract phase review

- 新增 `docs/reviews/workflow/p0-runtime-contract-prerequisites-2026-05-17.md`，汇总 P0.1-P0.6 六个提交的 diff summary、验证证据、review 结论和 residual risks。
- P0.Z review verdict 为 `ready`，open Must Fix 为 0；记录未跑独立外部 reviewer / subagent review、provider preference UI 后续接入、direct call redaction 复核作为 residual risks。
- `plan.md` 标记 P0 phase gate 与 P0.Z 完成，并把 Current next step 切到 P1.1 Vite + React infrastructure。

### 验证

- `npm run agentmesh -- doctor --json --skip-auth-probe`（ok；6 个 configured agents command-present，auth intentionally skipped）
- `npm run build`
- targeted `node --test --test-name-pattern "workspace compatibility|legacy workspace|run mutation lock|agentmesh call records|call record reader|call adoption|bootstrap endpoint|tokenized App Server|provider discovery|skill target matrix|legacy Cursor|opencode" dist-node/tests-node/flow-run.test.js dist-node/tests-node/flow-dispatch.test.js dist-node/tests-node/call-history.test.js dist-node/tests-node/studio.test.js dist-node/tests-node/studio-desktop-options.test.js dist-node/tests-node/readiness.test.js`（22/22）
- `npm test`（407/407）
- `git diff --check`

## 18:16 - 完成 P0.6 entry skill 安装目标现代化

- `SkillTarget` 新增 `opencode`，CLI `skill install/verify --target opencode` 可用。
- `codex`、`cursor`、`gemini`、`opencode` 和 `copilot` 现在统一写入 / 校验项目级共享 `.agents/skills/agentmesh/SKILL.md`。
- `claude` 改为项目级 `.claude/skills/agentmesh/SKILL.md`，不再写入用户级 `~/.claude/skills`。
- `skill verify --target cursor` 会把旧 `.cursor/rules/agentmesh.mdc` 报告为 `legacy_only` evidence；`install --force` 只刷新共享 Skill，不删除 legacy Cursor rule。
- README、AgentMesh Skill、landing command reference 和 `docs/contracts/skill-output.md` 同步新 target matrix 与 migration 说明。
- `plan.md` 标记 P0.6 完成，并把 Current next step 切到 P0.Z runtime contract phase review。

### 验证

- red `npm run build && node --test --test-name-pattern "skill target matrix|legacy Cursor|shared project SKILL|opencode|skill install writes host files|skill verify reports missing" dist-node/tests-node/readiness.test.js`（`opencode` 不是 SkillTarget）
- green `npm run build && node --test --test-name-pattern "skill target matrix|legacy Cursor|shared project SKILL|opencode|skill install writes host files|skill verify reports missing" dist-node/tests-node/readiness.test.js`（6/6）
- `npm run build && node --test dist-node/tests-node/readiness.test.js dist-node/tests-node/core-contracts.test.js dist-node/tests-node/cli-surface.test.js`（71/71）
- `npm test`（407/407）
- `git diff --check`

## 18:06 - 完成 P0.5 bootstrap 与 provider discovery

- Studio App Server 新增受保护的 `/api/bootstrap`，返回最小 session context；缺少 launch auth 或未配置 authToken 时拒绝，避免 same-origin unauthenticated fallback。
- Desktop host 的初始 token URL 继续用于 WebView 启动，根页面写入 `HttpOnly; SameSite=Strict` 的 `agentmesh_studio_token` cookie，支持 reload 后用 cookie 访问 API。
- Desktop 启动日志改为通过 `redactStudioUrlForLog` 输出，避免把 `?token=...` 原样写到日志或终端。
- Doctor readiness 增加 provider tool discovery 诊断字段，支持 configured path、app preference、well-known location 和受控 login-shell probe；GUI 启动场景可在没有继承 `PATH` 时找到 user-local provider CLI。
- Login-shell probe 从用户 home 目录、最小环境运行，只接受单个 absolute executable path，并拒绝 alias/function 样式输出、relative path、多行输出和 workspace-local path。
- `plan.md` 标记 P0.5 完成，并把 Current next step 切到 P0.6 entry skill install targets。

### 验证

- red `npm run build && node --test --test-name-pattern "bootstrap|desktop launch URLs|provider discovery" dist-node/tests-node/studio-desktop-options.test.js dist-node/tests-node/readiness.test.js`（缺少 provider discovery / URL redaction API）
- green `npm run build && node --test --test-name-pattern "bootstrap|desktop launch URLs|provider discovery" dist-node/tests-node/studio-desktop-options.test.js dist-node/tests-node/readiness.test.js`（3/3）
- red `npm run build && node --test --test-name-pattern "bootstrap endpoint" dist-node/tests-node/studio.test.js`（无 authToken 的 `/api/bootstrap` 仍返回 200）
- green `npm run build && node --test --test-name-pattern "bootstrap endpoint|tokenized App Server|desktop launch URLs|provider discovery" dist-node/tests-node/studio.test.js dist-node/tests-node/studio-desktop-options.test.js dist-node/tests-node/readiness.test.js`（5/5）
- `npm run build && node --test dist-node/tests-node/studio-desktop-options.test.js dist-node/tests-node/studio.test.js dist-node/tests-node/studio-cli.test.js dist-node/tests-node/readiness.test.js dist-node/tests-node/agent-registration.test.js`（76/76）
- `npm test`（404/404）
- `git diff --check`

## 17:55 - 完成 P0.4 direct call adoption

- 新增 call adoption helper：`appendCallAdoptionEvent` 以 append-only `adoption.jsonl` 记录 `accepted` / `rejected` / `superseded` 本地采纳事件。
- Adoption transition 仅允许从 `unreviewed` 进入最终状态；非法二次 transition 不会追加事件，也不会改写既有 event log。
- `superseded` 事件要求 `superseded_by_call_id`，但 related run / call link 可保持 dangling，本地记录不要求引用对象已经存在。
- 新 schema call record 保持 read-only：reader 可展示 warning，adoption mutation 会拒绝，避免旧 runtime 改写未知 schema。
- `call.json` 只更新当前 `adoption_status` 与 related run/call id 等 bounded metadata，不改写原始 prompt / output artifacts。
- `plan.md` 标记 P0.4 完成，并把 Current next step 切到 P0.5 bootstrap / provider discovery contracts。

### 验证

- red `npm run build && node --test --test-name-pattern "call adoption" dist-node/tests-node/call-history.test.js`（缺少 adoption helper 导出）
- green `npm run build && node --test --test-name-pattern "call adoption" dist-node/tests-node/call-history.test.js`（3/3）
- `node --test dist-node/tests-node/call-history.test.js dist-node/tests-node/adapter-invocation.test.js dist-node/tests-node/cli-surface.test.js`（39/39）
- `npm test`（400/400）
- `git diff --check`

## 17:48 - 完成 P0.3 direct call history

- 新增 `packages/runtime/src/calls/history.ts`，为 `agentmesh call` 写入 `.agentmesh/calls/<call-id>/call.json`、`prompt.md`、`output.md` / external output ref 和 `stderr.txt`。
- `agentmesh call` 默认要求在 AgentMesh workspace 内运行，provider 调用前先校验 workspace 与 `--output-file` containment；workspace 外必须显式传 `--no-record`，并提示该调用不会出现在 Studio。
- Call record 覆盖 `running -> success|failed|timeout` 状态、nullable model/token/cost 字段、`unreviewed` adoption 初始状态、stale running reader 识别和 newer schema read-only warning。
- 外部 `--output-file` 记录 workspace-relative authoritative output ref 和 sha256；逃逸 workspace 的 output path 在 provider 调用前拒绝。
- `plan.md` 标记 P0.3 完成，并把 Current next step 切到 P0.4 direct call adoption state machine。

### 验证

- red `npm run build && node --test dist-node/tests-node/call-history.test.js`（缺少 call-history module）
- green `npm run build && node --test dist-node/tests-node/call-history.test.js`
- red `npm run build && node --test --test-name-pattern "call record reader" dist-node/tests-node/call-history.test.js`（newer schema 尚未 read-only 标记）
- green `npm run build && node --test dist-node/tests-node/call-history.test.js dist-node/tests-node/cli-surface.test.js dist-node/tests-node/adapter-invocation.test.js`（36/36）
- `npm test`（397/397）
- `git diff --check`

## 17:37 - 完成 P0.2 entrypoint-aware mutation locks

- `packages/runtime/src/packet/lock.ts` 的 lease owner record 增加 `lock_id`、`workspace`、`scope`、`entrypoint`、`runtime_version`、`operation_id`、`command` 和 `heartbeat_at`，保留旧 `operation` / `owner_id` 兼容字段。
- Lock refusal diagnostic 现在显示 entrypoint、runtime、pid、operation id、command、heartbeat 和 expiry；旧 lease 缺失字段时显示 `unknown`，不自动删除或重写。
- `withRunMutationLockAsync` 支持 heartbeat refresh，长运行 mutation 持锁期间会刷新 `heartbeat_at`，成功后仍统一推进 workspace compatibility writer metadata。
- 同步更新 `docs/contracts/run-lock.md` 和 `plan.md`，P0.2 完成后 Current next step 切到 P0.3 direct call record schema。

### 验证

- red `npm run build && node --test --test-name-pattern "run mutation lock" dist-node/tests-node/flow-dispatch.test.js`（lock helper 尚不支持 owner options / 新 lease 字段）
- green `npm run build && node --test --test-name-pattern "run mutation lock" dist-node/tests-node/flow-dispatch.test.js`（4/4）
- `node --test dist-node/tests-node/flow-dispatch.test.js dist-node/tests-node/review-artifacts.test.js dist-node/tests-node/release-check.test.js dist-node/tests-node/studio-distribution-coexistence.test.js`（52/52）
- `npm test`（392/392）
- `git diff --check`

## 17:32 - 完成 P0.1 workspace compatibility metadata

- 新增 runtime-owned `.agentmesh/compatibility.json` 读写与诊断 helper，记录 packet schema、最低读写 runtime、最近写入 runtime / entrypoint 和更新时间。
- 新建 workflow run 成功后写入兼容元数据；缺失元数据的 legacy workspace 保持可读，首次成功 mutation 才回填，失败 mutation 不推进 writer metadata。
- Packet 读取与 run mutation lock 接入兼容检查：unsupported packet schema / min-read runtime 拒绝读取，newer min-write runtime 保持只读并拒绝写入。
- 新增 CLI `agentmesh packet compatibility --json`、SDK read surface 和 Studio `/api/compatibility`，让 CLI 与 App Server 调用方能看到 runtime version、entrypoint、metadata state 和 read/write decision。
- `plan.md` 标记 P0.1 完成，并把 Current next step 切到 P0.2 mutation lock owner metadata。

### 验证

- red `npm run build && node --test dist-node/tests-node/flow-run.test.js`（缺少 compatibility module）
- green `npm run build && node --test dist-node/tests-node/flow-run.test.js`
- red `npm run build && node --test --test-name-pattern "workspace compatibility|narrow API boundary" dist-node/tests-node/studio.test.js`（缺少 `/api/compatibility`）
- green `npm run build && node --test --test-name-pattern "compatibility diagnostics refuse|workspace compatibility|narrow API boundary" dist-node/tests-node/flow-run.test.js dist-node/tests-node/studio.test.js`
- `node --test dist-node/tests-node/flow-run.test.js dist-node/tests-node/flow-dispatch.test.js dist-node/tests-node/packet-io.test.js dist-node/tests-node/studio.test.js dist-node/tests-node/studio-distribution-coexistence.test.js`（81/81）
- `npm test`（391/391）
- `git diff --check`

## 17:13 - 校准 entry skill 安装目标计划

- `plan.md` 新增 P0.6 `Modernize entry skill installation targets`，把 entry-agent Skill 安装目标纳入 P0 runtime contract prerequisites。
- 根据官方路径校准，计划将 `.agents/skills/agentmesh/SKILL.md` 设为 Codex、Cursor、Gemini CLI、OpenCode 和 GitHub Copilot CLI 的共享项目级默认安装路径。
- 保留 Claude Code 例外：计划要求 `claude` target 使用 `.claude/skills/agentmesh/SKILL.md`，避免误把 `.agents/skills` 当作 Claude Code 已确认扫描路径。
- 同步更新 P0 phase gate、P0.Z、验证策略和风险控制，要求后续实现覆盖 skill target matrix、legacy `.cursor/rules/agentmesh.mdc` 检测和 `--force` 刷新行为。

### 验证

- `git diff --check -- plan.md`
- 本次为计划 / 文档更新，未运行构建或单元测试。

## 16:55 - 拆分 review 归档目录

- 将 `docs/reviews/` 下的 Studio / 桌面端 / Tauri / DMG / 前端栈 review 归档移动到 `docs/reviews/studio/`。
- 将 workflow/runtime 阶段门禁 review 归档移动到 `docs/reviews/workflow/`，并新增 `docs/reviews/README.md` 说明 review archive 与 `.agentmesh/runs/<run-id>/` 临时运行记录的边界。
- 同步更新 `plan.md`、本日 / 前日 changelog 和 `docs/archive/plan-before-plan-c-2026-05-17.md` 中的 review 路径引用，避免旧扁平路径失效。

### 验证

- `rg -n -P "docs/reviews/(?!studio/|workflow/|README|\\*\\*/)[^\\s)]+" .`（无旧扁平路径残留）
- `git diff --check`
- `git diff --cached --check`

## 16:31 - 细化 Plan C 执行切片

- `plan.md` 从架构决策稿细化为可执行计划：新增 P0 runtime contract prerequisites，先落地 workspace compatibility metadata、entrypoint-aware lock、direct call record、call adoption、bootstrap/provider discovery，再进入 React / Studio / DMG 阶段。
- 将 Calls tab 从单个 P2.8 拆为 App Server read API、React Calls UI、adoption UI 和 cleanup scope decision，明确 React 只能走 App Server API，`.agentmesh/calls/*` 由 App Server 读取。
- 补清 CLI + DMG co-install 细节：`.agentmesh/compatibility.json` 所属边界、legacy workspace 处理、lock owner JSON / stale lock 策略、provider CLI discovery、双 App Server / 版本偏移 / PATH poisoning 验证。
- 补强 desktop bootstrap 风险控制：`/api/bootstrap` 不能是无认证 same-origin fallback，reload recovery 必须通过 tokenized URL、Tauri 注入或受保护 cookie。
- 新增并提交 Claude 4.7、Gemini、Cursor 对 CLI + DMG co-install 方案的只读 review 记录，已采纳 finding 已同步进 `plan.md`。

### 验证

- `git diff --check -- plan.md`
- `node -e "const fs=require('node:fs'); const p=JSON.parse(fs.readFileSync('package.json','utf8')); console.log(p.name); console.log(p.bin && p.bin.agentmesh);"`
- 本次为文档 / 计划 / review 材料同步，未运行构建或单元测试。

## 16:18 - 修复 raw review heading 误删

- 将 `withoutRawReviewOutputs` 收敛为 `review/artifacts.ts` 的共享 helper，并改为只匹配独立行 `## Raw Review Outputs`，避免普通 finding 文本里引用该标题时被误当作 raw review 区块起点。
- `flow/prompt.ts` 和 `release/check.ts` 复用共享 helper，删除各自的 substring / split 版本，减少三处逻辑漂移。
- 新增底层 `findingsWithRawReviews` 和 release summary 回归，覆盖“正文引用 raw review heading 仍需保留、旧 raw review 区块仍需替换”的组合场景。

### 验证

- red `./node_modules/.bin/tsc -p tsconfig.json && node --test --test-name-pattern "strip only standalone raw review heading|release summary bounds raw review outputs" dist-node/tests-node/review-artifacts.test.js dist-node/tests-node/release-check.test.js`（2 个用例复现误删）
- green `./node_modules/.bin/tsc -p tsconfig.json && node --test --test-name-pattern "strip only standalone raw review heading|release summary bounds raw review outputs" dist-node/tests-node/review-artifacts.test.js dist-node/tests-node/release-check.test.js`（2/2）
- `node --test dist-node/tests-node/review-artifacts.test.js dist-node/tests-node/release-check.test.js dist-node/tests-node/release-check-flow.test.js dist-node/tests-node/flow-prompt.test.js dist-node/tests-node/flow-dispatch.test.js`（57/57）
- `git diff --check -- packages/runtime/src/review/artifacts.ts packages/runtime/src/flow/prompt.ts packages/runtime/src/release/check.ts tests-node/review-artifacts.test.ts tests-node/release-check.test.ts`

## 16:06 - 继续收敛 prompt token 放大路径

- Fanout synthesis prompt 现在会按候选输出逐个截断，每个 candidate output 最多内联 6KB，并写入来源路径、展示 / 原始 bytes 和 truncation marker，完整输出仍保留在 `outputs/<stage>/<agent>.md`。
- Release-check summary 现在重新从 `reviews/*.md` 生成有界 raw review 区块，每个 raw review 最多内联 2.5KB；如果 `findings.md` 已经含有旧的 `## Raw Review Outputs`，会先剥离再追加有界版本，避免原文尾巴绕过截断。
- Stage prompt 与 fanout synthesis prompt 共享 release summary prompt 截断逻辑，`release-summary.md` 最多内联 24KB，避免 summary 自身把 1MB 级别证据重新塞回模型。
- 新增回归覆盖 long candidate output、long release summary、findings 内旧 raw review tail 三类 token replay 风险。

### 验证

- `./node_modules/.bin/tsc -p tsconfig.json`
- `node --test --test-name-pattern "prompt assembly bounds large release summaries|plan fanout synthesis prompt bounds long candidate outputs|release summary bounds raw review outputs" dist-node/tests-node/flow-prompt.test.js dist-node/tests-node/flow-dispatch.test.js dist-node/tests-node/release-check.test.js`（3/3）
- `node --test dist-node/tests-node/release-check.test.js dist-node/tests-node/release-check-flow.test.js dist-node/tests-node/flow-dispatch.test.js dist-node/tests-node/flow-prompt.test.js`（53/53）
- `git diff --check -- packages/runtime/src/flow/prompt.ts packages/runtime/src/flow/dispatch.ts packages/runtime/src/release/check.ts tests-node/flow-prompt.test.ts tests-node/flow-dispatch.test.ts tests-node/release-check.test.ts`

## 15:43 - 收敛 Studio 桌面端最终计划

- 将 Plan C 提升为主计划：`plan.md` 现在记录 Studio 最终栈为 Tauri 2 + Vite + React + Node App Server，并明确当前分发目标是团队内部 unsigned macOS DMG。
- 旧 `plan.md` 已归档到 `docs/archive/plan-before-plan-c-2026-05-17.md`；临时 `plan-c.md` 已删除，避免并行计划漂移。
- 新增并保留 Claude 4.7、Gemini 和 Cursor 对 Plan C、Tauri vs Electron、内部 unsigned DMG 方案的只读评审记录；已采纳的 finding 已同步进 `plan.md`。
- 明确 agent 创建 / 删除 / 启停归 Node App Server 与 CLI/runtime 边界负责，Tauri 仅负责桌面 shell、sidecar 生命周期和 bootstrap。

### 验证

- `shasum -a 256 plan.md docs/archive/plan-before-plan-c-2026-05-17.md`
- `wc -l plan.md docs/archive/plan-before-plan-c-2026-05-17.md`
- 本次为文档与评审材料整理，未运行构建或测试。

## 15:25 - 限制 stage prompt context 重放

- Stage prompt 组装新增 `Packet Directory`，并把内联 `context.md` 限制为 24KB 前缀；超出时复用 truncation marker，完整证据仍保留在 packet 源文件中，降低 review / decide 等阶段重复消耗 token 的风险。
- `tests-node/flow-prompt.test.ts` 增加大 context 截断、24KB 展示字节数和 run dir 位于 `cwd` 外时绝对 packet path 的回归覆盖。
- 通过 AgentMesh `review-gate` 让 Claude 4.7、Gemini 和 Cursor 对本次 prompt assembly 修复做只读审查；三路均未报告 Must Fix，采纳并补齐绝对路径 fallback 测试与预算断言。

### 验证

- `./node_modules/.bin/tsc -p tsconfig.json`
- `node --test dist-node/tests-node/flow-prompt.test.js`（3/3）

## 14:52 - 收敛 agent 默认 id 冲突与 skip-verify 文档

- Agent 注册文档、README 和 AgentMesh Skill 进一步明确：默认省略 `agent-id`，若派生出的 default id 已存在，entry agent 不能自动追加后缀或改成角色名，必须让用户选择复用、删除重建或显式命名独立 profile。
- 修正 `docs/contracts/agent-registration.md` 中旧的 `--skip-verify` 描述，统一为 diagnostic-only、不写 config，并同步 Runtime Alignment 到当前已实现行为。
- `packages/runtime/src/adapters/registration.ts` 的 skip-verify warning 改为描述 agent candidate，避免暗示已经写入配置。
- 根据 review-gate `review-agent-naming-20260517` 的 Claude 4.7 / Gemini / Cursor 反馈，修正 README readiness 示例、Gemini 注册示例和 contract 旧口径；当前 Codex 安装版 AgentMesh Skill 已用 `skill install --target codex --force` 同步。
- `tests-node/core-contracts.test.ts` 和 `tests-node/readiness.test.ts` 增加 guardrail，覆盖 duplicate default id 说明，并防止 README / Skill 回退到 `agents add executor`、`agents add reviewer` 或 `--alias codex` 旧示例。

### 验证

- red `npm run build && node --test dist-node/tests-node/core-contracts.test.js`（缺少 duplicate default id 说明）
- green `npm run build && node --test dist-node/tests-node/core-contracts.test.js dist-node/tests-node/readiness.test.js`（45/45）
- `npm run build && node --test dist-node/tests-node/readiness.test.js dist-node/tests-node/core-contracts.test.js dist-node/tests-node/cli-surface.test.js`（66/66）
- `node dist-node/packages/cli/src/cli.js skill install --target codex --force`
- `node dist-node/packages/cli/src/cli.js skill verify --target codex`

## 11:15 - 新增 Studio frontend stack 决策提案与评审材料

- 新增并修订 `docs/decisions/studio-frontend-stack-proposal.md`，记录 Studio 前端下一步先引入 Vite 拆分资产、何时重新评估 React/Vue，以及 Tauri 只作为桌面 shell 的边界。
- 新增 Claude 4.7、Cursor 和 Gemini 只读评审记录，集中挑战 Vite 是否应先于 asset split、React/Vue 延迟是否合理、trigger gates 是否足够可验证。
- 该文档产物不改变 runtime、packet、workflow 或 Studio API 行为。

### 验证

- `npm test`（381/381）
- `git diff --check`

## 11:14 - 收敛 agent 注册默认 id 文档口径

- README、`agentmesh-skill/SKILL.md` 和 `docs/contracts/agent-registration.md` 同步 agent 注册新规范：默认省略 `agent-id`，由 AgentMesh 根据 canonical adapter / model 派生默认 id。
- 明确只有用户要求角色化命名或团队已有约定时才显式传 `[agent-id]`，entry agent 不再替用户临时发明 `executor`、`reviewer` 这类角色 id。
- `tests-node/core-contracts.test.ts` 和 `tests-node/readiness.test.ts` 增加 guardrail，防止 contract / skill 输出退回旧的角色 id 示例。

### 验证

- `npm test`（381/381）
- `git diff --check`

## 11:11 - Studio 设置页语言控件改为下拉框

- Studio 设置页语言控件从 `button role="switch"` 改为原生 `<select>`，提供 `中文` / `English` 两个选项。
- 语言切换逻辑从 click toggle 改为 select `change` 事件，继续持久化 `agentmesh.studio.language` 并同步页面文案、`aria-label` 和 `title`。
- `tests-node/studio-ui.test.ts` 更新为保护 combobox 结构、选项、change 行为，并防止旧 `.language-switch` / `role="switch"` 回归。

### 验证

- red `npm run build && node --test --test-name-pattern "Studio UI keeps accessibility|Studio UI keeps stored English language|Studio UI renders run navigator details" dist-node/tests-node/studio-ui.test.js`（旧 switch 实现被新测试抓到）
- green `npm run build && node --test --test-name-pattern "Studio HTML arranges|Studio UI keeps accessibility|Studio UI keeps stored English language|Studio UI renders run navigator details" dist-node/tests-node/studio-ui.test.js`（4/4）
- `node --test dist-node/tests-node/studio-ui.test.js dist-node/tests-node/studio.test.js`（23/23）
- Browser 实测设置页为 `combobox "选择语言"`，中英文切换正常。
- `npm test`（381/381）
- `git diff --check`

## 10:52 - 修复 Studio 运行列表少量数据拉伸

- 清理本地 `.agentmesh/runs` 中旧 `status.json.schema_version = 1` 运行目录，消除 Studio 配置页 `workflows unsupported packet schema version: 1` 诊断来源。
- Studio 左侧运行列表在数据较少时不再把日期分组和 run 行拉伸填满剩余高度；`.run-list`、`.run-group`、`.run-group-items` 改为顶部对齐并使用内容高度行。
- `tests-node/studio-ui.test.ts` 增加 CSS guardrail，防止 run list grid 退回 stretch 行为。
- 本次补丁不再包含无关语言控件或手册文案变更。

### 验证

- `npm run build`
- `node --test dist-node/tests-node/studio-ui.test.js`（11/11）
- `git diff --check`

## 01:38 - 完成 P5.Z 最终 review / release gate

- 新增 `docs/reviews/workflow/p5-final-workflow-stage-semantics-2026-05-17.md`，记录 P5 final review、opencode 不可用证据、accepted findings 处理、验证命令和 release verdict。
- Claude 4.7 最终只读 review 报告 Must Fix 为 0；opencode auth probe 30s 超时，按不可用 reviewer lane 记录，不作为 release blocker。
- 处理 accepted Should：新增 preset run `--task` + `--task-file` 互斥回归；为 Studio workflow detail metric 增加具体 `overflow-wrap:anywhere` CSS guardrail 断言。
- README 和 `index.html` 测试基线从 380 更新为 381；`plan.md` 标记 P5.Z 和 P5 阶段门禁完成。
- Release gate verdict：ready；残余风险记录为 byte-based prompt budget、no v1 packet migration、raw review heading collision 和 literal `--task-file` task text edge case。

### 验证

- `npm run agentmesh -- doctor --agent claude --agent opencode --json`（claude ready；opencode auth probe timeout）
- Claude 4.7 final review（Must Fix 0；accepted Should 1/2 handled）
- `npm run build && node --test --test-name-pattern "preset run rejects inline task|accessibility and responsive guardrails" dist-node/tests-node/flow-preset-run.test.js dist-node/tests-node/studio-ui.test.js`（2/2）
- `npm run build && node --test dist-node/tests-node/flow-preset-run.test.js dist-node/tests-node/studio-ui.test.js dist-node/tests-node/core-contracts.test.js dist-node/tests-node/flow-prompt.test.js dist-node/tests-node/flow-dispatch.test.js dist-node/tests-node/flow-run.test.js dist-node/tests-node/workflow-registry.test.js dist-node/tests-node/sdk-read.test.js`（107/107）
- `git diff --check`
- `npm test`（381/381）

## 01:27 - 完成 P5.5 packet v2 workflow 工作日志汇总

- 汇总记录 breaking packet v2 行为：`status.json` / `assignment.toml` 以 node-id keyed `stage_assignments` 为事实源，并固化 `stage_invocations`、`stage_failure_policies`、`stage_fallbacks`、`stage_attempts` 和 provenance。
- 明确 No v1 packet migration：旧 packet v1 不自动升级，workflow recipe 必须声明 `compatible_packet_schema_versions = [2]`，不接受 `[1, 2]` 过渡。
- 汇总 workflow / preset / global config 分工：workflow 定义线性 stage node 和 failure policy 语义，preset 固定具体 stage agents、fallback 和 run defaults，global config 只提供本机 default primary agents、fallback routing、timeouts 和 policy 输入。
- 记录 Studio/docs 更新范围：Studio 展示 repeated node ids、artifact preview links、fallback/failure/attempt/timeout audit details；README、index 和 docs contracts 同步 preset-first UX、decide checkpoint、packet v2 与中文“审查”口径。
- `plan.md` 标记 P5.5 完成后，当前下一步切到 P5.Z 最终收尾校准。

### 验证

- `rg -n "^# 2026-05-17|^## [0-9]{2}:[0-9]{2} - |packet v2|No v1 packet migration|preset-first UX|fallback/failure|Studio|审查" changelog/2026-05-17.md`
- `git diff --check`

## 01:26 - 完成 P5.4 README / index workflow semantics 同步

- README 和 `index.html` 同步最终入口口径：preset-first UX、direct workflow run namespace、decide checkpoint、packet schema version 2、No v1 packet migration、default primary agents、fallback/failure policy。
- README 增加 preset init/add/doctor/run 入口、`[default_stage_agents]` / `[fallback]` 配置示例、workflow `compatible_packet_schema_versions = [2]` 和 `[failure_policy]` 示例。
- Landing page 更新 P5 semantics、380 tests、packet v2 / No v1 migration、preset commands、packet attempt facts 和能力边界。
- 新增 `tests-node/core-contracts.test.ts` docs smoke，防止 README/index 缺失最终 workflow semantics 关键词。
- `plan.md` 标记 P5.4 完成，当前下一步切到 P5.5 changelog / 工作日志同步。

### 验证

- red `npm run build && node --test --test-name-pattern "README and landing page describe final workflow semantics" dist-node/tests-node/core-contracts.test.js`（README/index lacked final preset-first packet v2 semantics）
- green `npm run build && node --test --test-name-pattern "README and landing page describe final workflow semantics" dist-node/tests-node/core-contracts.test.js`（1/1）
- `npm run build && node --test dist-node/tests-node/core-contracts.test.js dist-node/tests-node/management-cli.test.js dist-node/tests-node/flow-preset-run.test.js dist-node/tests-node/workflow-registry.test.js`（47/47）
- `git diff --check`
- `npm test`（380/380）

## 01:17 - 完成 P5.3 Studio packet v2 展示

- Studio workflow detail 现在把 stage related artifacts 渲染为可点击 preview link，点击后切到 artifacts tab 并加载对应 artifact。
- Stage attempt audit summary 展开 packet v2 execution facts，显示 lane、primary/requested/actual agent、fallback_from、lane_attempt、attempt、timeout、error_kind 和 error details。
- Studio UI 回归继续覆盖 repeated node ids、artifact links、fallback/failure/attempt/timeout details；SDK packet v2 read surface 保持 targeted 覆盖。
- `plan.md` 标记 P5.3 完成，当前下一步切到 P5.4 README/index 同步。

### 验证

- red `npm run build && node --test --test-name-pattern "renders run navigator details|renders packet v2 stage nodes" dist-node/tests-node/studio-ui.test.js`（artifact links were static / attempt audit trail omitted lane, fallback and timeout fields）
- green `npm run build && node --test --test-name-pattern "renders run navigator details|renders packet v2 stage nodes" dist-node/tests-node/studio-ui.test.js`（2/2）
- `npm run build && node --test dist-node/tests-node/studio-ui.test.js dist-node/tests-node/studio.test.js dist-node/tests-node/sdk-read.test.js`（26/26）
- `git diff --check`
- `npm test`（379/379）

## 01:09 - 完成 P5.2 docs contracts / examples 同步

- `workflow-toml.md`、`preset-toml.md`、`config-toml.md`、`stage-dispatch.md`、`status-json.md` 同步 packet v2、stage rules、direct run、fallback/timeout、no v1 migration 和 non-goals 口径。
- `docs/workflows/review-gate.toml` 明确作为 packet-v2 recipe metadata / alignment artifact。
- CLI 现在在 direct workflow run 与 preset run 创建 packet 前拒绝同时提供 `--task` 和 `--task-file`。
- `tests-node/core-contracts.test.ts` 增加 contract TOML examples parse smoke；`tests-node/flow-run.test.ts` 增加 task input mutual exclusion 回归。
- `plan.md` 标记 P5.2 完成，当前下一步切到 P5.3 Studio/SDK 展示。

### 验证

- red `npm run build && node --test --test-name-pattern "workflow run rejects inline task|contract docs exist" dist-node/tests-node/flow-run.test.js dist-node/tests-node/core-contracts.test.js`（docs lacked P5.2 sections / CLI accepted both task inputs）
- green `npm run build && node --test --test-name-pattern "workflow run rejects inline task|contract docs exist|contract TOML examples parse" dist-node/tests-node/flow-run.test.js dist-node/tests-node/core-contracts.test.js`（3/3）
- `npm run build && node --test dist-node/tests-node/core-contracts.test.js dist-node/tests-node/workflow-registry.test.js dist-node/tests-node/flow-run.test.js dist-node/tests-node/flow-preset-run.test.js dist-node/tests-node/config-layering.test.js`（88/88）
- `git diff --check`
- `npm test`（379/379）

## 01:03 - 完成 P5.1 prompt assembly 收敛

- Stage prompt 与 fanout synthesis prompt 共用 ordered prior evidence，保留 type-specific prior output labels。
- Review findings 里的 raw review tail 会移动到 bounded `Prior Raw Reviews` section；每个 raw review 超预算时显示 truncation marker、来源路径和展示 / 原始 bytes。
- 新增 `tests-node/flow-prompt.test.ts` 回归，覆盖多 reviewer findings 与 repeated review raw outputs 不再被 decide prompt 无界 replay。
- `plan.md` 标记 P5.1 完成，当前下一步切到 P5.2 docs contracts/examples。

### 验证

- red `npm run build && node --test --test-name-pattern "prompt assembly truncates" dist-node/tests-node/flow-prompt.test.js`（long raw review output replayed without truncation marker）
- green `npm run build && node --test --test-name-pattern "prompt assembly truncates" dist-node/tests-node/flow-prompt.test.js`（1/1）
- `npm run build && node --test dist-node/tests-node/flow-prompt.test.js dist-node/tests-node/flow-dispatch.test.js dist-node/tests-node/flow-retry-resume.test.js dist-node/tests-node/release-check-flow.test.js dist-node/tests-node/release-check.test.js`（50/50）
- `git diff --check`
- `npm test`（377/377）

## 00:52 - 完成 P4.Z dispatch runtime 收尾校准

- 新增 `docs/reviews/workflow/p4-dispatch-runtime-2026-05-17.md`，记录 P4 dispatch review：open Must findings 为 0，P4.1-P4.4 的 Should findings 均已处理。
- README 和 `index.html` 同步 P4 dispatch runtime 状态，把测试基线从 372 更新到 376。
- `plan.md` 标记 P4.Z 与 P4 阶段门禁完成，当前下一步切到 P5.1 prompt assembly。

### 验证

- `npm run build && node --test dist-node/tests-node/flow-dispatch.test.js dist-node/tests-node/flow-retry-resume.test.js dist-node/tests-node/release-check-flow.test.js dist-node/tests-node/release-check.test.js dist-node/tests-node/packet-validate.test.js dist-node/tests-node/sdk-read.test.js dist-node/tests-node/studio-ui.test.js`（75/75）
- `git diff --check`
- `npm test`（376/376）

## 00:49 - 完成 P4.4 retry/resume/attach 与 release verdict 校准

- Retry/resume 继续按 exact node id 操作；新增断言确认 retry 后同一 lane 的 `lane_attempt` 从 1 递增到 2，并保留 failed/completed attempt 历史。
- Current attach 保持 host-only 路径，不创建 fallback 或 dispatch attempt。
- Invalid release verdict 事件确认写入 `node_id`，release verdict 仍只由 release-check 最终 decide node 记录。
- `plan.md` 标记 P4.4 完成，当前下一步切到 P4.Z dispatch runtime 收尾校准。

### 验证

- `npm run build && node --test dist-node/tests-node/flow-retry-resume.test.js dist-node/tests-node/flow-dispatch.test.js dist-node/tests-node/release-check-flow.test.js dist-node/tests-node/release-check.test.js`（49/49）
- `git diff --check`
- `npm test`（376/376）

## 00:45 - 完成 P4.3 fallback attempts 与 timeout 记录

- Dispatch 改为按 packet `stage_invocations` / `stage_fallbacks` 执行 primary 与 fallback attempts，并写入 `stage_attempts`。
- 每条 attempt 记录 lane id、primary/requested/actual agent、fallback_from、lane_attempt、per-agent attempt、timeout_seconds、status 和 error_kind。
- Terminal policy 下 timeout 会记录为 `timed_out` 并直接失败，不走 fallback。
- Fanout primary 失败时可由 fallback 接管同一 lane 输出，synthesis 继续使用 fallback 写入的候选结果。
- `plan.md` 标记 P4.3 完成，当前下一步切到 P4.4 retry/resume/attach 行为。

### 验证

- red `npm run build && node --test --test-name-pattern "fallback attempts|timed out attempts" dist-node/tests-node/flow-dispatch.test.js`（dispatch ignored packet fallback / timeout attempts）
- green `npm run build && node --test --test-name-pattern "fallback attempts|timed out attempts" dist-node/tests-node/flow-dispatch.test.js`（2/2）
- red `npm run build && node --test --test-name-pattern "plan fanout records fallback" dist-node/tests-node/flow-dispatch.test.js`（fanout primary failure aborted stage）
- green `npm run build && node --test --test-name-pattern "plan fanout records fallback" dist-node/tests-node/flow-dispatch.test.js`（1/1）
- `npm run build && node --test dist-node/tests-node/flow-dispatch.test.js dist-node/tests-node/release-check-flow.test.js dist-node/tests-node/release-check.test.js dist-node/tests-node/flow-run.test.js dist-node/tests-node/flow-preset-run.test.js dist-node/tests-node/packet-validate.test.js`（85/85）
- `git diff --check`
- `npm test`（376/376）

## 00:36 - 完成 P4.2 fanout synthesis prompt 去重

- Fanout synthesis prompt 改为 synthesis-specific base，保留 request、assignment、prior artifacts、release/user-gate contract 和 fanout outputs。
- 当 run 有 `context.md` 时，synthesis prompt 只写 context reference，不再重复携带候选 fanout prompt 已经接收过的 full context。
- 新增 `plan fanout synthesis prompt references context without replaying it` 回归测试，确认候选 prompt 可见 context sentinel，但 synthesis prompt 不 replay context 且 prompt bytes 小于候选 prompt。
- Release-check decide fanout、non-final decide verdict gating、flow context 和 release summary 邻近测试通过。
- `plan.md` 标记 P4.2 完成，当前下一步切到 P4.3 fallback attempts / timeout。

### 验证

- red `npm run build && node --test --test-name-pattern "plan fanout synthesis prompt references context" dist-node/tests-node/flow-dispatch.test.js`（synthesis prompt replayed `CONTEXT_SENTINEL`）
- green `npm run build && node --test --test-name-pattern "plan fanout synthesis prompt references context" dist-node/tests-node/flow-dispatch.test.js`（1/1）
- `npm run build && node --test dist-node/tests-node/flow-dispatch.test.js dist-node/tests-node/release-check-flow.test.js dist-node/tests-node/release-check.test.js dist-node/tests-node/flow-context.test.js`（58/58）
- `git diff --check`
- `npm test`（373/373）

## 00:30 - 完成 P4.1 stage artifact / raw output 命名收口

- Repeated review raw output artifact id 从 `review_review_2_<agent>` 收敛为 `review_2_<agent>`，路径保持 `reviews/review_2/<agent>.md`，避免 artifact id 混用 node id 前缀。
- `docs/contracts/artifacts-toml.md` 同步 repeated review artifact 示例。
- `tests-node/flow-dispatch.test.ts` 更新 repeated workflow artifact 断言，覆盖 `decision_2.md`、`findings_2.md`、`handoff_2.md` 和 `review_2_<agent>` manifest 命名。
- `plan.md` 标记 P4.1 完成，当前下一步切到 P4.2 dispatch fanout / synthesis。

### 验证

- red `npm run build && node --test --test-name-pattern "dispatch all runs repeated workflow nodes" dist-node/tests-node/flow-dispatch.test.js`（missing `review_2_runner`）
- green `npm run build && node --test --test-name-pattern "dispatch all runs repeated workflow nodes" dist-node/tests-node/flow-dispatch.test.js`（1/1）
- `npm run build && node --test dist-node/tests-node/flow-dispatch.test.js dist-node/tests-node/review-artifacts.test.js dist-node/tests-node/core-contracts.test.js dist-node/tests-node/packet-validate.test.js`（54/54）
- `git diff --check`
- `npm test`（372/372）

## 00:26 - 完成 P3.Z runtime creation 收尾校准

- 新增 `preset run writes complete packet v2 routing materialization` 回归测试，覆盖 config + preset + workflow run creation 后的 packet v2 执行事实完整物化。
- 测试同时用 `PacketStatusSchema` 和 `packet validate` 校验生成的 packet，并断言 stage-node keyed `stage_assignments`、`stage_invocations`、`stage_failure_policies`、`stage_fallbacks`、`stage_attempts`、`assignment_provenance`、`fallback_provenance` 和 `timeout_provenance`。
- 新增 `docs/reviews/workflow/p3-runtime-creation-2026-05-17.md`，记录 runtime creation review：open Must findings 为 0，accepted Should 已处理；dispatch-time fallback attempts/timeout execution 保留为 P4 范围。
- README 和 `index.html` 同步 P3 runtime creation 状态与 `npm test` 372/372 基线。
- `plan.md` 标记 P3.Z 与 P3 阶段门禁完成，当前下一步切到 P4.1 stage artifact / raw output 写入。

### 验证

- `npm run build && node --test dist-node/tests-node/config-layering.test.js dist-node/tests-node/flow-preset-run.test.js dist-node/tests-node/flow-run.test.js`（57/57）
- `git diff --check`
- `npm test`（372/372）
