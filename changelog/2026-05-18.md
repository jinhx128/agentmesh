# 2026-05-18

## 00:34 - 同步 monorepo 与 Studio 一步到位执行计划

- `plan.md` 收敛为一步到位的 monorepo / Studio 执行计划，明确 CLI、Desktop DMG、Studio Web、App Server、runtime、SDK、skills 的最终边界和安装形态。
- 新增 review Must Fix 可追溯矩阵，把 Cursor、Claude 4.7、Gemini 复审、自审和用户决策落到具体章节、slice 与验证门禁。
- 补齐 P1.0 Web Studio React assets 默认入口、P1.1 `agentmesh call --prompt-file` 输入契约、P1.5 packet 单当前格式口径，避免旧页面回退、prompt-file 空输入和 packet `v1` / `v2` 双版本叙事继续扩散。
- 收紧 App Server / CLI 写路径、Desktop token 链路、SDK 稳定面与 runtime 内部面、CLI tarball / Desktop bundle 分发门禁。
- 根据最终自审修正 P0 当前下一步、P0.2 验证范围和 P1.5 全量 `npm test` 门禁。

### 验证

- `git diff --check -- plan.md`

## 00:36 - 完成 P0 review 边界门禁收尾

- 新增 `docs/architecture/package-boundaries.md`，把 review Must Fix 转成包职责、禁止项和验证门禁清单。
- 将 `plan.md` 的 P0.2、P0.Z 和 P0 阶段门禁标记为完成，并把当前下一步推进到 P1.0。
- P0 收尾不改变运行代码；影响范围是计划、架构文档和变更记录。

### 验证

- `git diff --check`
- `git status --short`

## 00:51 - 修复 Studio CLI 默认 React 入口

- `agentmesh studio` 和 standalone `npm run studio` 默认解析构建后的 React/Vite assets，并把 `assetDir` 传入 Studio App Server。
- 显式 assets 缺失时不再静默回退旧 HTML，而是提示执行 `npm run build:studio-frontend`；测试可通过 `AGENTMESH_STUDIO_ASSET_DIR` 注入临时 assets 路径。
- Studio CLI 回归测试覆盖默认 React `index.html`、hashed assets、`/api/calls` API 不被静态资源接管，以及缺 assets 的错误提示。
- README 已同步本地 Studio 启动说明；Gemini 外审首轮 Should Fix 已处理，复审 `LGTM`。

### 验证

- `npm run build:node && npm run build:studio-frontend && node --test dist-node/tests-node/studio-cli.test.js`
- `npm run build && node --test dist-node/tests-node/studio-cli.test.js dist-node/tests-node/studio.test.js`
- `npm run agentmesh -- studio --port 4777 --no-open`，curl `/` 和 `/api/calls`

## 01:00 - 修复 prompt-file adapter 输入契约

- `packages/runtime/src/adapters/invocation.ts` 调整 `command` adapter 默认 prompt 行为：只有显式声明 `prompt_file_arg` 时传文件路径，否则把 `--prompt-file` 内容归一化为 prompt text 并通过 stdin / `prompt_arg` 传入。
- 补充 adapter invocation 测试，覆盖 Codex、Claude、Cursor、Gemini、OpenCode 的 prompt-file 内容传递，并断言不把文件路径传给 AI CLI。
- 补充 direct call history 测试，确认 `agentmesh call --prompt-file` 仍记录 `prompt_source=file`、`prompt_ref` 和 prompt artifact。
- 同步 `docs/contracts/adapter-invocation.md`，删除未声明时“positional prompt file”的活跃契约口径。
- Gemini live smoke 使用真实 `agentmesh call --prompt-file` 输出 `P1.1 prompt file smoke OK`；Gemini 外审 `LGTM`。

### 验证

- `npm run build:node && node --test dist-node/tests-node/adapter-invocation.test.js dist-node/tests-node/call-history.test.js`
- `npm run build && node --test dist-node/tests-node/adapter-invocation.test.js dist-node/tests-node/call-history.test.js`
- `agentmesh call --agent gemini-gemini-3-1-pro-preview --prompt-file <tmp> --output-file docs/reviews/studio/p1.1-gemini-prompt-file-smoke-2026-05-18.md`

## 01:16 - 收敛 packet 单当前格式口径

- `CURRENT_PACKET_SCHEMA_VERSION` 改为跟随 `CURRENT_SCHEMA_VERSION`，保留 `schema_version` 作为机器校验字段，不再把当前 packet 格式产品化命名成 v2。
- 测试 helper 从 `packetStatusV2` 收敛为 `currentPacketStatus`，并同步 packet/status/workflow/Studio/SDK 相关 fixtures 与断言。
- Workflow recipe 兼容声明统一为当前常量 `[1]`，只保留用于拒绝未来未知 schema 的负例。
- README、index、contracts 和 `docs/workflows/review-gate.toml` 已统一到 `current packet schema` 口径；active surface 旧 `packet v2` / `[2]` / `No v1` 叙事扫描无输出。
- Gemini 通过 AgentMesh 外审 `LGTM`，输出见 `docs/reviews/studio/p1.5-gemini-2026-05-18.md`。

### 验证

- `npm run build && node --test dist-node/tests-node/core-contracts.test.js dist-node/tests-node/packet-validate.test.js dist-node/tests-node/workflow-registry.test.js dist-node/tests-node/flow-run.test.js dist-node/tests-node/sdk-read.test.js dist-node/tests-node/studio-ui.test.js`
- `npm test`
- `rg -n "compatible_packet_schema_versions = \\[2\\]|must equal \\[2\\]|compatiblePacketSchemaVersions: \\[2\\]|compatiblePacketSchemaVersions, \\[2\\]|packet v2|Packet v2|packetStatusV2|No v1|v1-to-v2|schema version 2|packet schema 2|Packet schema 2|packet-v2|v2-run|No legacy migration" README.md index.html docs/contracts docs/workflows examples packages tests-node -g '!docs/archive/**' -g '!docs/reviews/**'`
- `git diff --check`

## 01:27 - 定义 workspace package/app 构建边界

- 根 `workspaces` 扩展为 `packages/*` 和 `apps/*`，并同步 `package-lock.json` 的 workspace link。
- 为 `packages/cli`、`packages/runtime`、`packages/sdk`、`apps/studio`、`apps/studio-desktop` 补充独立 `tsconfig.json`；`packages/core` 增加 package build 脚本。
- 为 `apps/studio` 和 `apps/studio-desktop` 新增 workspace `package.json`，让 Studio App Server / React frontend 和 Desktop host 成为显式 app 单元。
- 新增 `npm run build:workspaces`，按 workspace 运行各 unit 的 build 脚本；根 `build:node` / `build:studio-frontend` 输出路径保持不变。
- Project references 本 slice 暂不引入：保留 root `tsc` 作为 emit 边界，各 workspace 用 `tsc --noEmit` 做独立检查，P1.3 再收敛源码 import 边界。
- Gemini 通过 AgentMesh 外审 `LGTM`，输出见 `docs/reviews/studio/p1.2-gemini-2026-05-18.md`。

### 验证

- `npm run build:workspaces`
- `npm run build:node`
- `npm run build:studio-frontend`
- `node --test dist-node/tests-node/studio-desktop-distribution.test.js`
- `npm test`
- `git diff --check`

## 02:03 - 收敛跨包 import 与 SDK 只读边界

- `packages/**` 与 `apps/**` 源码跨包引用收敛到 `@agentmesh/*` 包名，新增 `scripts/check-boundaries.mjs` 强制 core、frontend、SDK/runtime import 方向。
- `build:node` 在 `tsc` 后执行 `scripts/rewrite-package-imports.mjs`，把编译产物里的 workspace 包名重写到 `dist-node` 内相对 JS 路径，避免运行测试时穿回 TS 源码。
- `@agentmesh/sdk` 去除 `@agentmesh/runtime` 依赖并保持 read-only；call adoption 写入改由新增 `agentmesh calls adopt` CLI 命令进入 runtime 边界，Studio App Server 只通过配置的 CLI 触发写入。
- Studio catalog 的 agents/workflows 读取改用 SDK，MCP inventory 保留 CLI 诊断；frontend 边界脚本允许 type-only `@agentmesh/core` / `@agentmesh/sdk` contract，但继续拒绝 runtime 和非 type package import。
- Gemini 初审发现 SDK 写 API、catalog CLI 读和 frontend type-only 边界问题，均已修复；复审 `LGTM`。

### 验证

- `npm run build:node && node --test dist-node/tests-node/package-structure.test.js dist-node/tests-node/call-history.test.js dist-node/tests-node/studio.test.js`
- `npm run check:boundaries`
- `rg "\\.\\./\\.\\./.*packages|\\.\\./\\.\\./.*runtime|\\.\\./\\.\\./.*studio" packages apps -g '*.ts' -g '*.tsx' -g '!apps/studio-desktop/src-tauri/**'`
- `rg "from ['\\\"]@agentmesh/runtime|import\\(['\\\"]@agentmesh/runtime" apps packages/sdk -g '*.ts' -g '*.tsx' -g '!apps/studio-desktop/src-tauri/**'`
- `npm run build:workspaces`
- `npm test`
- `git diff --check`

## 02:25 - 落定内部发布形态

- 根 package `agentmesh` 增加 tarball `files` 白名单，只打包 `dist-node/packages/`、`dist-node/apps/studio/` 和 `agentmesh-skill/`，避免把源码、测试、review 和 changelog 一起塞进 CLI tarball。
- 根 TypeScript 构建开启 declaration emit，CLI tarball 现在包含 core/runtime/sdk/cli/studio 的 `.d.ts` 与对应构建 JS。
- `tests-node/package-structure.test.ts` 新增 root pack、声明文件、`npm pack --dry-run` 清单和 clean install smoke 门禁；clean install 在临时项目安装 root tarball 后执行 `agentmesh --help`、`agentmesh doctor --json`、`agentmesh skill show`。
- 新增 `docs/distribution/package-shape.md`，记录 root CLI tarball 是唯一 installable package、内部 workspace package 继续 private、root 无 `@agentmesh/*` workspace dependency 缺口，以及 Desktop channel 不消费 CLI tarball、只消费 app-server/runtime/studio-web build artifacts。
- 外审：Gemini 初审有 dependency key 空格误报，已核对不成立；接受其 core pack 显式断言建议后，Claude 4.7 外审 `LGTM`。

### 验证

- `npm run build && node --test dist-node/tests-node/package-structure.test.js`
- `npm run build && node --test dist-node/tests-node/package-structure.test.js dist-node/tests-node/studio-desktop-distribution.test.js`
- `npm pack --dry-run`
- `npm run build:workspaces`
- `npm test`
- `npm run check:boundaries`
- `git diff --check`

## 02:32 - 完成 P1 阶段收尾校准

- `plan.md` 将 P1 阶段完成门禁和 `P1.Z` 标记为完成，并把当前下一步推进到 `P2.1 抽出 packages/app-server 并直连 runtime`。
- P1.Z 复核确认 workspace 拓扑、跨包 import 边界、SDK/runtime read/write 边界、CLI tarball 发布形态、Desktop bundle 边界和当前 packet schema 口径都已具备进入 P2 的基础。
- 外审：Claude 4.7 因额度限制未产出 review，按计划失败策略改用 Gemini；Gemini 通过 AgentMesh 外审 `LGTM`。

### 验证

- `npm run build`
- `npm test`
- `npm run check:boundaries`
- `git diff --check`

## 03:14 - 完成 P2.1 App Server package 拆出

- 新增 `packages/app-server` workspace package，迁移 Studio HTTP server、packet/call/catalog browser、mutation、agent lifecycle 和 asset fallback，`agentmesh studio` 与 Desktop host 改为调用 `@agentmesh/app-server`。
- App Server 写路径直连 runtime：flow dispatch/retry/resume/attach、call adoption、agent lifecycle 不再 spawn CLI；CLI agents 命令改为复用同一组 runtime lifecycle API。
- Runtime 补齐程序化写 API 与 cwd 传递：agent lifecycle API、call adoption metadata 校验、dispatch agent lookup/调用 cwd、agent registration command/args/env override。
- 边界检查禁止 App Server 写路径引入 `child_process`、`@agentmesh/cli` 或 `packages/cli`；package-structure 测试覆盖 `@agentmesh/app-server` 声明、pack 产物和 App Server 不 spawn CLI 写路径。
- 外审：Cursor `LGTM`；Codex `LGTM` 且提出两条 Should Fix，已修复 adoption metadata runtime 校验和 lifecycle 异常落 failed operation；Claude 4.7 / Gemini 因额度不可用未产出有效 review。

### 验证

- `npm run build && node --test dist-node/tests-node/call-history.test.js dist-node/tests-node/studio.test.js`
- `npm run build && node --test dist-node/tests-node/package-structure.test.js dist-node/tests-node/studio.test.js dist-node/tests-node/studio-desktop-options.test.js dist-node/tests-node/studio-distribution-coexistence.test.js dist-node/tests-node/cli-surface.test.js`
- `npm test`

## 03:35 - 完成 P2.2 Studio Web app 拆出

- 新增 `apps/studio-web` workspace package，迁移 React/Vite frontend 源码和 Vite 配置；`apps/studio` 收敛为只启动 App Server 的本地 launcher。
- `npm run build:studio-frontend` 改用 `apps/studio-web/vite.config.ts`，Vite 产物稳定写入 `dist-node/apps/studio-web/frontend`，不受当前工作目录影响。
- CLI Studio 默认 assetDir、Desktop host 默认 assetDir、sidecar bundle frontend 检查、root tarball `files` 白名单和 README / distribution 文档均切到 `dist-node/apps/studio-web/frontend`。
- React / ReactDOM 依赖归属 `@agentmesh/studio-web`，根 CLI package 不再声明 React runtime dependency。
- 外审：Cursor 初审发现 Vite `outDir` cwd 风险，已修复；Cursor follow-up 确认此前 CLI assetDir blocking 为 path anchor 误判，当前无 blocking。Codex review 尝试挂起且未产出文件，已终止。

### 验证

- `npm run build && node --test dist-node/tests-node/package-structure.test.js`
- `npm run build && node --test dist-node/tests-node/package-structure.test.js dist-node/tests-node/studio-cli.test.js dist-node/tests-node/studio-ui.test.js dist-node/tests-node/studio.test.js dist-node/tests-node/studio-desktop-distribution.test.js dist-node/tests-node/studio-desktop-options.test.js`
- `npm run build --workspace @agentmesh/studio-web`
- `npm test`

## 03:52 - P2.3 写路径发布门禁实现就绪

- `scripts/check-boundaries.mjs` 将 App Server CLI / child_process 禁止项提升到 `packages/app-server/src/**` 包级门禁，只豁免 `packages/app-server/src/mcp-diagnostics.ts` 的 read-only MCP list 诊断。
- `catalog.ts` 不再直接持有 `child_process` / CLI spawn；MCP 诊断 helper 隔离到 `mcp-diagnostics.ts`，避免 `catalog.ts` 整文件豁免过宽。
- 禁止项扩展到 `spawn`、`exec`、`execFile`、`fork` 及其 `Sync` 变体、`process.execPath`、`@agentmesh/cli`、`packages/cli`；`package-structure` 测试同步镜像该 gate。
- 外审：Cursor 初审发现整文件豁免和非 `spawn` API 漏检，已修复；Cursor follow-up `PASS`。第二 reviewer 未满足：Gemini quota，OpenCode / Claude / Codex 挂起或超时，Mimo 模型不可用。
- 状态：实现和本地门禁就绪，当时因外审数量门槛需要用户确认是否接受替代审查证据。

### 验证

- `npm run build && node --test dist-node/tests-node/package-structure.test.js`
- `npm run build && node --test dist-node/tests-node/package-structure.test.js dist-node/tests-node/studio.test.js dist-node/tests-node/studio-desktop-options.test.js dist-node/tests-node/studio-distribution-coexistence.test.js dist-node/tests-node/cli-surface.test.js`
- `npm run check:boundaries`

## 10:14 - 调整 reviewer 门槛并放行 P2.3

- 按用户决策，`plan.md` 中外审执行门槛统一调整为“至少一个 reviewer”。
- `P2.3 App Server 写路径发布门禁` 改为已完成；Cursor 初审问题已修复，Cursor follow-up `PASS` 满足新门槛。
- 当前下一步从 `P2.3` 决策推进到 `P2.Z 阶段收尾校准`。

### 验证

- P2.Z 收尾验证见 10:26 条目。

## 10:26 - 完成 P2.Z 阶段收尾校准

- `P2.Z 阶段收尾校准` 和 P2 阶段完成门禁已标记完成，当前下一步推进到 `P3.1 Sidecar 产物契约`。
- 修复全量测试负载下的两个 smoke 等待窗口抖动：Studio 缺 assets CLI 退出测试 timeout 从 1s 放宽到 5s，Desktop sidecar readiness timeout 从 5s 放宽到 15s。
- 外审：Cursor 对 P2.Z 收尾 `LGTM`，输出见 `docs/reviews/studio/p2.z-cursor-2026-05-18.md`；接受其 Should Fix，把本次提交摘要从旧 refactor 口径改为 test-stability 口径。

### 验证

- `npm run build`
- `npm run build && node --test dist-node/tests-node/studio-cli.test.js dist-node/tests-node/studio-desktop-distribution.test.js`
- `npm test`
- `npm run studio -- --host 127.0.0.1 --port 0 --no-open` 等价 smoke：`GET /` 和 `GET /api/calls` 均为 200
- `npm run check:boundaries`
- `git diff --check`

## 10:57 - 完成 P3.1 Sidecar 产物契约

- Desktop/App Server 移除 `cliPath` / `runtimeCliPath` plumbing，App Server catalog 改为通过 runtime config API 读取 MCP 配置，不再 spawn CLI 或打包 App-bundled CLI artifact。
- 删除 `packages/app-server/src/mcp-diagnostics.ts`；`scripts/check-boundaries.mjs` 和 `package-structure` 测试把 App Server `child_process` / CLI / `process.execPath` 禁止项提升为包级门禁。
- Tauri `bundle.resources` 改为显式资源表，只包含 desktop host、sidecar、Studio web frontend、App Server、runtime/core/sdk 和 runtime deps；distribution smoke 拒绝 broad `dist-node` 与 `dist-node/packages/cli`。
- `sidecar-bundle.ts` 固定生成 JS launcher + bundled Node + runtime `node_modules`，复制依赖时过滤 `.ts` / `.tsx` / `.mts` / `.cts` 源文件，并由 bundle、distribution smoke 和测试共同验证发布目录不依赖 TS 源码。
- README 与 distribution 文档同步：Desktop app-originated mutations 通过 App Server runtime APIs 执行；CLI 仍是 terminal / entry-agent 的 PATH-visible 入口。
- 外审：Cursor 对 P3.1 `PASS`，输出见 `docs/reviews/studio/p3.1-cursor-2026-05-18.md`；两项 Should Fix 已修复（README 旧 CLI subprocess 文案、`StudioCatalogOptions.commandTimeoutMs` 死字段），并补强 `.mts` / `.cts` 过滤。

### 验证

- `npm run build && node --test dist-node/tests-node/studio-desktop-distribution.test.js dist-node/tests-node/package-structure.test.js`
- `npm run build && node --test dist-node/tests-node/studio.test.js dist-node/tests-node/studio-desktop-distribution.test.js`
- `npm run studio-desktop:sidecar:bundle`
- `npm run studio-desktop:package:dev`
- `npm run check:boundaries`
- `find dist-node/apps/studio-desktop dist-node/packages/app-server dist-node/packages/runtime dist-node/packages/core dist-node/packages/sdk dist-node/apps/studio-web/frontend -type f \\( -name '*.ts' -o -name '*.tsx' -o -name '*.mts' -o -name '*.cts' \\) ! -name '*.d.ts' ! -name '*.d.ts.map' | head -50`
- `git diff --check`
- `npm test`

## 11:34 - 完成 P3.2 Desktop 安全 bootstrap

- Desktop 启动链改为 Tauri 生成 per-launch token，通过 sidecar stdin JSON handshake 传给 Node App Server；sidecar argv、launch JSON、WebView URL 和日志不再携带 token。
- Tauri 在导航前写入 `agentmesh_studio_token` 的 `HttpOnly; SameSite=Strict` native WebView cookie，并只接受无 query 的 `http://127.0.0.1:<port>/` ready URL。
- App Server auth 移除 query token，只接受 Bearer 或 `agentmesh_studio_token` cookie；补 Host/Origin/CORS loopback gate，恶意 Host、恶意 Origin、缺 token、旧 token 和 query-token 请求都会明确拒绝。
- Studio frontend bootstrap 不再从 URL token 建立认证，只清理 legacy token query；co-install、distribution、bootstrap、stale token 和 mutation tests 均切到 cookie auth 或 query-token negative case。
- 同步 README、App Server contract、distribution smoke、frontend decision、public extension surface 和 macOS distribution metadata；`plan.md` 的 P5 也按用户决策明确 DMG-only 只满足 Desktop Studio，入口 agent 需要 PATH-visible CLI 与用户选择的 skill target。
- 外审：Cursor 对 P3.2 `PASS`，输出见 `docs/reviews/studio/p3.2-cursor-2026-05-18.md`；接受并修复 App Server contract 可选 auth 澄清和 P3.1/P3.2 distribution wording。

### 验证

- `npm run build && node --test dist-node/tests-node/studio-desktop-options.test.js dist-node/tests-node/studio-desktop-distribution.test.js dist-node/tests-node/studio.test.js dist-node/tests-node/studio-distribution-coexistence.test.js`
- `npm run studio-desktop:sidecar:bundle`
- `npm run studio-desktop:package:dev`
- `npm run check:boundaries`
- `cargo check --manifest-path apps/studio-desktop/src-tauri/Cargo.toml`
- `npm test`
- `git diff --check`

## 11:58 - 完成 P3.3 Lock/compat UI 降级

- Runtime run lock 冲突现在暴露 typed `run_locked` error 和 lock owner metadata；App Server mutation 映射为 HTTP `423 Locked`、`retryable: true` 和可展示的 lock payload。
- Workspace compatibility read-only / refused 冲突改为 typed `WorkspaceCompatibilityError`，App Server 稳定返回 HTTP `409 Conflict`、`workspace_read_only` / `workspace_refused` 和 `retryable: false`，不再依赖英文错误信息前缀。
- Studio Safe Actions 展示 lock operation、entrypoint、runtime、pid、operation_id、heartbeat/expires 和 retryable 状态；Settings/About 展示 runtime、entrypoint、last writer、compat decision 和 read-only/refused 升级提示。
- Desktop App Server 的 compatibility diagnostics 标记 `current_entrypoint` 为 `desktop`，CLI / local App Server 默认保持 `cli`。
- App Server contract 已同步 423 lock 与 409 compatibility mutation 语义；Cursor 初审和 follow-up 外审均 `PASS/LGTM`。

### 验证

- `npm run build && node --test dist-node/tests-node/studio-desktop-options.test.js dist-node/tests-node/studio-distribution-coexistence.test.js dist-node/tests-node/studio.test.js dist-node/tests-node/studio-ui.test.js`
- `npm run check:boundaries`
- `git diff --check`
- `npm test`

## 12:02 - 完成 P3 Desktop 阶段收尾校准

- `plan.md` 将 P3 阶段完成门禁和 `P3.Z 阶段收尾校准` 标记为完成，当前下一步推进到 `P4.1 抽出 packages/skills`。
- P3 收尾确认 Desktop sidecar 产物契约、安全 bootstrap、lock/compat UI 降级、dev packaging、CLI/Desktop 共存和边界门禁均满足进入 P4 的条件。
- `npm run studio-desktop:package:dev` 通过，distribution smoke 返回 `ok=true`、`issues=[]`，sidecar launcher、bundled Node、runtime deps 和 Studio web frontend 验证通过；dev 模式缺签名 / notarization 环境变量为预期状态。
- co-install smoke 覆盖 CLI / Desktop 分离 session、Desktop mutation 不走 PATH 中的假 CLI、entry-agent 仍解析 PATH-visible `agentmesh`、共享 run lock 和 newer packet fail-fast。
- 外审：Cursor 对 P3.Z `PASS/LGTM`，无 Must Fix；接受其建议补跑 Tauri `cargo check` 并纳入阶段证据。

### 验证

- `npm run build`
- `npm run studio-desktop:package:dev`
- `node --test dist-node/tests-node/studio-distribution-coexistence.test.js`
- `npm test`
- `npm run check:boundaries`
- `git diff --check`
- `cargo check --manifest-path apps/studio-desktop/src-tauri/Cargo.toml`

## 12:17 - 完成 P4.1 skills package 拆出

- 新增 `@agentmesh/skills` workspace package，集中承载 AgentMesh Skill 模板、版本元数据注入、install target mapping 和 verify report contract。
- 将 `agentmesh-skill/SKILL.md` 迁移到 `packages/skills/agentmesh-skill/SKILL.md`，将 `packages/runtime/src/skill/verify.ts` 迁移到 `packages/skills/src/verify.ts`，runtime 不再导出 skill writer 逻辑。
- CLI `skill show/export/install/verify` 改为消费 `@agentmesh/skills`；root tarball 白名单改为携带 `packages/skills/agentmesh-skill/` 和构建后的 `dist-node/packages/skills/**`。
- 构建 import rewrite 增加 `@agentmesh/skills`，边界脚本禁止 `packages/skills/src/**` 反向依赖 runtime 或 CLI。
- README 与 distribution package-shape 文档已同步新的 skills package 和 tarball 形态；外审初审与 follow-up 均 `PASS/LGTM`。

### 验证

- `npm run build && node --test dist-node/tests-node/package-structure.test.js dist-node/tests-node/readiness.test.js`
- `node dist-node/packages/cli/src/cli.js skill show`
- 临时 workspace：`skill install --target codex --force` 后 `skill verify --target codex --json`
- `npm run build:workspaces`
- `npm run check:boundaries`
- `git diff --check`
- `npm test`

## 12:24 - 完成 P4.2 默认 skill 安装路径对齐

- `@agentmesh/skills` 新增 `expectedSkillFilesForTarget`，为 Studio / App Server 等只读诊断提供同一套 target path contract，而不写入宿主文件。
- Codex、Cursor、Gemini、OpenCode、Copilot 默认指向当前项目 `.agents/skills/agentmesh/SKILL.md`；Claude 默认指向当前项目 `.claude/skills/agentmesh/SKILL.md`；`homeDir` 仅作为未来 host-home diagnostics 预留，不参与当前路径解析。
- `skill install --force` 只刷新 expected target 文件；当 legacy `.cursor/rules/agentmesh.mdc` 存在时，install stdout 不再打印 legacy 路径，`skill verify --json` 仍报告 `legacy_only` 诊断且不会删除 legacy 文件。
- `docs/contracts/skill-output.md` 同步记录 `expectedSkillFilesForTarget` 只读诊断契约。
- 外审：Cursor 初审和 follow-up 均 `PASS/LGTM`，两项 Should Fix 已修复。

### 验证

- `npm run build && node --test dist-node/tests-node/readiness.test.js`
- `node --test dist-node/tests-node/package-structure.test.js dist-node/tests-node/readiness.test.js`
- `node dist-node/packages/cli/src/cli.js skill show`
- 临时 workspace：`skill install --target codex --force` 后 `skill verify --target codex --json`
- `npm run check:boundaries`
- `git diff --check`
- `npm test`

## 12:30 - 完成 P4 skill 阶段收尾校准

- `plan.md` 将 P4 阶段完成门禁和 `P4.Z 阶段收尾校准` 标记为完成，当前下一步推进到 `P5.1 CLI 与入口 agent 命令安装验收`。
- P4 收尾确认 `@agentmesh/skills`、CLI `skill show/export/install/verify`、target path contract、legacy Cursor diagnostics、root tarball skill 模板路径和文档口径一致。
- 按用户决策，阶段验证只在临时 workspace 中执行 `skill install --target codex --force` 与 `skill verify --target codex --json`，没有把 AgentMesh Skill 自动安装到当前项目或用户宿主。
- 外审：Cursor 对 P4.Z `PASS/LGTM`；接受其 Should Fix，把 `docs/contracts/skill-output.md` 中 metadata 由 runtime 生成的旧说法改为 `@agentmesh/skills` / `agentmeshSkillMarkdown` 口径，follow-up 继续 `PASS/LGTM`。

### 验证

- `npm run build`
- `node dist-node/packages/cli/src/cli.js skill show`
- 临时 workspace：`skill install --target codex --force` 后 `skill verify --target codex --json`
- `npm test`
- `npm run check:boundaries`
- `git diff --check`
- `npm run build && node --test dist-node/tests-node/package-structure.test.js dist-node/tests-node/readiness.test.js dist-node/tests-node/core-contracts.test.js`

## 12:44 - 完成 P5.1 CLI 与入口 agent 命令安装验收

- 新增 `docs/distribution/cli-command-install.md`，定义团队成员获得 PATH-visible `agentmesh` 的 source checkout、tarball 和 private registry 三种路径。
- README、`docs/distribution/package-shape.md`、`docs/distribution/studio-coexistence-smoke.md` 和 `docs/distribution/studio-macos.md` 明确：DMG-only 只满足 Desktop Studio；入口 agent / Skill 必须由用户选择 PATH-visible CLI 和对应 host Skill target。
- `Install Command Line Tool` 被记录为可选、用户确认后的入口，切换前必须检测并展示已有 PATH command；raw `npm install -g` 本身不提供确认，确认责任属于团队安装说明、rollout wrapper 或 DMG action。
- Skill 安装说明改为按用户选择 target 独立安装/验证；Codex、Cursor、Gemini、OpenCode、Copilot 使用 `.agents/skills/agentmesh/SKILL.md`，Claude 使用 `.claude/skills/agentmesh/SKILL.md`。
- 新增 `npm run cli:install-smoke`，并扩展 root tarball clean install smoke：临时项目安装后执行 `agentmesh --help`、`agentmesh doctor --json`、`agentmesh skill show`、`agentmesh skill install --target codex --force` 和 `agentmesh skill verify --target codex --json`。
- 外审：Cursor 初审 `PASS/LGTM`；接受 3 项 Should Fix 并修复 verify target 口径、app update wording 和 tarball 文件名；follow-up `PASS/LGTM`。

### 验证

- `npm run cli:install-smoke`
- `node --test dist-node/tests-node/package-structure.test.js dist-node/tests-node/core-contracts.test.js dist-node/tests-node/readiness.test.js`
- `npm test`
- `npm run check:boundaries`
- `git diff --check`

## 13:18 - 完成 P5.2 Desktop DMG 验收

- Desktop DMG 资源表纳入可选 Agent Integrations 所需的 app-managed CLI / skills / package resources；Studio mutation 路径仍保持 `studio-web -> app-server -> runtime`，不调用 PATH 或 app-bundled CLI。
- Settings / Agent Integrations 新增命令行工具与 Agent Skill 安装入口：`Install Command Line Tool` 会检测 PATH-visible `agentmesh`、目标文件差异和 wrapper metadata，替换或 PATH shadowing 必须用户确认；`Install Selected Skills` 由用户选择 `codex`、`cursor`、`gemini`、`opencode`、`copilot`、`claude`，每个 target 独立报告结果。
- Desktop skill 安装优先使用 app-bundled skill artifact，避免 workspace 内同名 `packages/skills/agentmesh-skill/SKILL.md` 影响 DMG 包内一致性；UI 会展示 skill target path / hint，并在部分失败时列出失败 target。
- README 和 distribution docs 同步说明：DMG-only 只满足 Desktop Studio，入口 agent / skill 仍需要 PATH-visible CLI；wrapper 记录安装时 App 内部绝对路径，移动或更新 `AgentMesh.app` 后需要重新安装。
- 外审：Cursor 初审 `PASS/LGTM`，接受 5 项 Should Fix 并修复；follow-up `LGTM for P5.2 closeout`，见 `docs/reviews/studio/p5.2-cursor-2026-05-18.md` 和 `docs/reviews/studio/p5.2-cursor-followup-2026-05-18.md`。

### 验证

- `npm run build && node --test dist-node/tests-node/studio-desktop-options.test.js dist-node/tests-node/studio-ui.test.js dist-node/tests-node/readiness.test.js dist-node/tests-node/package-structure.test.js dist-node/tests-node/core-contracts.test.js`
- `npm run studio-desktop:package:dev`
- `npm run check:boundaries`
- `git diff --check`
- `npm run build && node --test dist-node/tests-node/studio-desktop-distribution.test.js`
- `npm test`

## 13:24 - 完成 P5.Z 项目收尾校准

- `plan.md` 将 P5.2 commit 记录补齐为 `a138a99 docs(distribution): validate internal dmg install`，并将 `P5.Z 项目收尾校准` 与 P5 阶段完成门禁标记完成。
- P5 收尾确认：CLI clean install、Desktop dev DMG package smoke、DMG command-line wrapper smoke、shared target `codex` 与 Claude target skill install/verify、boundary、whitespace 和全量测试均通过。
- 记录 shared target 等价类：`codex` / `cursor` / `gemini` / `opencode` / `copilot` 共用 `.agents/skills/agentmesh/SKILL.md` 写入逻辑，P5.Z smoke 用 `codex` 代表 shared target，target matrix 测试覆盖 shared targets 和 Claude 例外路径。
- 外审：Cursor P5.Z 初审指出流程记录缺口（P5.Z/P5 勾选、P5.Z changelog、P5.2 commit 记录）并给出 `needs_decision`；已接受并修复。follow-up 确认上轮 Must 与原 Should 均已闭合，同时要求本收尾提交后核对 clean tree。
- 发布门禁结论：ready；signed/notarized public distribution 仍不在本阶段范围，dev smoke 缺签名 / notarization 环境变量为预期状态。

### 验证

- `npm run build`
- `npm run studio-desktop:package:dev`
- `npm run cli:install-smoke`
- `node --test --test-name-pattern "desktop command-line tool install|desktop skill install" dist-node/tests-node/studio-desktop-options.test.js`
- `npm test`
- `npm run check:boundaries`
- `git diff --check`
- `git status --short`

## 16:21 - 同步 Studio Web UI 与 Mantine 重构计划

- `plan.md` 切换为 AgentMesh Studio Mantine 一步到位重构方案，并把原 monorepo / Studio 技术计划归档到 `docs/archive/plan-before-ui-framework-2026-05-18.md`。
- Studio Web 接入 Mantine 9.2.1 基础设施，新增 `StudioThemeProvider`、Mantine CSS layer、PostCSS 配置和 DOM lib 编译配置；`package-lock.json` 同步新增依赖树。
- React Studio 导航重组：Runs / Calls 切换移动到左侧列表模块，顶部应用入口保留配置、设置、手册；新增手册工作区和基础章节切换。
- 配置页 Agents / Workflows / MCP 以及 Workflow 来源改为真正可点击 tab，并将 Workflow 自定义来源文案从“用户配置”统一为“自定义”。
- 运行列表与调用列表按日期分组改为可收缩；Workflow Flow 补回节点间方向箭头；右侧 Run 详情改为概述、操作、发布 / 审查、产物、事件 tab 展示，减少长页面滚动。
- UI 交互细节同步：刷新按钮尺寸固定，按钮 hover 只做样式变化不位移，隐藏面板通过 `[hidden]` 规则明确收起；Studio UI 回归测试同步覆盖这些行为。

### 验证

- `npm run build && node --test dist-node/tests-node/studio-ui.test.js`
- `git diff --check`

## 17:30 - 完成 Studio Mantine 一步到位迁移

- Studio React UI 已整体迁移到 Mantine shell 和控件体系，覆盖 Runs / Calls / Config / Settings / Manual / Actions 主路径；Runs / Calls 切换下沉到左侧数据模块，右侧 workspace 使用独立滚动容器。
- `packages/app-server/src/assets.ts` 静态 fallback 收敛为最小 loading shell，不再维护完整旧控制台视觉；`apps/studio-web/src/styles.css` 删除旧 shell / navigator / catalog selector 合同，只保留布局胶水和领域可视化样式。
- `tests-node/studio-ui.test.ts` 改为语义、可访问性、核心状态和 API contract 断言，并补充特殊字符 ID 编码与非 2xx mutation / adoption payload 保真测试。
- 修复 Desktop sidecar 打包在 Homebrew Node 26 下缺少 `libnode*.dylib` 导致空 PATH 启动前 dyld 失败的问题，macOS sidecar bundle 现在会复制 `libnode*.dylib` 并保持重复打包幂等。
- `plan.md` 已同步 P1 完成状态、浏览器 smoke 证据、AgentMesh review 结论和最终验证结果；Cursor 与 Mimo review 均为 0 Must Fix。

### 验证

- `npm test`：448/448 通过
- `node --test dist-node/tests-node/studio-ui.test.js`：11/11 通过
- `node --test dist-node/tests-node/studio-desktop-distribution.test.js`：6/6 通过
- `git diff --check`

## 20:31 - 同步 Studio UI 交互迭代与创建能力

- Studio 左侧 Runs / Calls 数据模块补齐自动刷新下拉，可选 `Off`、`5s`、`10s`、`15s`、`30s`、`60s`；刷新按钮与搜索框高度对齐，日期分组恢复可收起状态。
- Runs / Calls 列表和详情时间统一使用系统本地时区展示；直接调用记录 id 改为本地时间、秒级精度且不再包含 ISO `T` / `Z` / 毫秒分隔。
- 顶部配置、设置、手册入口与左侧数据列表选中态解耦，切到配置 / 设置 / 手册时不再同时保留 Runs / Calls 列表项高亮；配置计数徽标只在配置页展示。
- 设置页承载中英文开关；运行概览、调用详情、事件和导航空状态接入同一套 Studio i18n 文案与本地时间格式化工具。
- 配置页将 Agent Lifecycle 合并进 Agents tab，创建 Agent 表单放在列表上方；Workflows tab 新增 Create Workflow 表单，并通过 `POST /api/v1/workflows` 写入 project / user scope workflow registry。
- 产物页改为上下布局，Artifact 列表在上、预览在下；Artifact 卡片压缩为名称和关键标签，详情移动到选中后的预览区，并在卡片之间补齐横向连接线和箭头。
- Studio UI、workflow lifecycle 和 call history 相关测试同步覆盖自动刷新、分组收起、本地时间、工作流创建接口、Artifact 连接线和调用 id 命名。

### 验证

- `npm run build`
- `node --test dist-node/tests-node/studio-ui.test.js`
- 浏览器 smoke：`http://127.0.0.1:4879` 产物 tab 渲染 12 个 Artifact 节点与 11 条连接线，选中项下方预览可见。

## 23:14 - 收口 Studio 中文 UI、设置结构与手册组件页

- Studio UI 收敛为中文单语言体验，移除中英文切换入口；保留 `Agent`、`Workflow`、`Packet`、`MCP` 等定义名原文展示。
- 顶部入口删除原“配置”，保留“设置 / 手册”；设置页改为 `资源`、`环境`、`关于` 三个 tab，其中资源承载原配置内容，环境承载本机集成设置，关于展示版本与兼容性。
- 全局 tab 样式统一为紧凑 segmented 视觉，减少长横线与大面积空白；Runs / Calls 自动刷新默认值调整为 `15s`。
- Agents 列表按工具 adapter 分组展示，新增工具 tab；创建 Agent 改为按钮打开弹窗，先选工具，再填写模型、推理等级和 Agent name。
- Workflow Flow 和 Artifact Flow 使用统一的紧凑节点与横向连接线；Artifact 预览补充产物信息区，预览文案移动到内容下方。
- 手册“核心概念”改为“组件”，并补齐 Agent、Tool Adapter、Workflow、Preset、Stage Node、Run、Packet、Artifact、Event、Context Pack、MCP Resource、Project Spec、Studio、App Server 等组件说明。

### 验证

- `npm run build`
- `node --test dist-node/tests-node/studio-ui.test.js`
- `node --test dist-node/tests-node/studio.test.js`
- `git diff --check`
