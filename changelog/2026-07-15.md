# 2026-07-15

## 15:27 - 修复 Antigravity 模型发现并准备 0.1.9 发布

- Antigravity CLI 改用 `agy models` 发现完整模型标签，调用时传入 `--model <model>`；旧配置的 `model = "current"` 保持兼容。
- 发现失败时不再接受会被 `agy` 拒绝的模型 slug，并支持带 `>` 前缀和自定义模型标签。
- Studio 在模型发现失败时仍允许编辑已有 legacy agent；补充模型发现、注册、调用的端到端回归测试。
- 清理不可达的 Antigravity 本地字符串扫描逻辑及其残留配置接口。
- 版本从 `0.1.8` 更新到 `0.1.9`，发布资产包含 npm tarball、macOS DMG、skill markdown 和 SHA256 校验文件。

### 验证

- `npm test`：519 passed, 0 failed。
- `agentmesh call --agent a-f1c8e0a2 --prompt '只回复 OK' --no-record` 返回 `OK`。
- `git diff --check` 通过。

## 20:03 - 加固 Agent 上下文交接并准备 0.1.10 发布

- 上下文交接现在明确标注缺失、截断和失败的证据，执行阶段携带结构化 handoff，综合阶段使用正确的工作目录。
- scoped context 统一排除内部 run 产物，覆盖 denied path、symlink、untracked 文件、MCP 部分失败和枚举失败等边界场景。
- untracked 文件采用 no-follow 安全打开并限制单文件、文件数量和聚合读取预算；无法安全打开时 fail-closed，预算触顶时保留已处理证据并记录失败 provenance。
- npm 发布入口兼容 npm 11：规范化 CLI bin 路径并迁移 scope 权限检查命令，避免发布 manifest 丢失 `agentmesh` 命令。
- 版本从 `0.1.9` 更新到 `0.1.10`，同步 CLI、全部 workspace、Tauri Desktop、README 和发布资产元数据。

### 验证

- `flow-context`：33 passed, 0 failed。
- 全量 Node 测试：541 个测试点，exit 0。
- `npm run build`、`cargo check`、`studio-desktop:package:dev` 和 `git diff --check` 通过。
- 已生成 npm tarball、macOS Apple Silicon DMG、skill markdown 和 SHA256 校验文件。

## 22:32 - 重构 Desktop 安装与更新能力

- Desktop 图标替换为 Converge 设计，以单一 SVG 母版生成 PNG、ICNS 和 ICO 资产，并补充小尺寸图标契约。
- Copilot 从 CLI Skill target、Skill 母版、Desktop UI/API、分发元数据、README、站点和活跃契约中完整移除；不删除用户磁盘上已经存在的文件，历史 changelog、review 和 archive 保持不变。
- Settings / Agent Integrations 不再写 app-managed wrapper，也不再要求 Bin 路径或 PATH shadowing 确认；现在检测实际 `agentmesh --version`，查询公共 npm latest，并通过 `npm install --global @jinhx128/agentmesh@latest` 安装或更新后重新探测。
- Settings / About 接入 Tauri updater/process 插件，支持检查、下载进度、签名校验、安装和重启；浏览器 Studio 不调用原生 API。
- 发布准备增加版本化 `.app.tar.gz`、`.sig` 和 `latest.json`，公钥进入 Tauri 配置，私钥和密码仅保存在仓库外的本机发布环境。
- 修复 bundled Homebrew `libnode` 继承 `0444` 后导致 Tauri 重复资源复制 EACCES 的问题，sidecar 打包统一写为 `0644`。
- 版本保持 `0.1.10`，本次未发布 npm 或 GitHub Release；`0.1.10` 到首个 updater 版本仍需手动安装一次 DMG。

### 验证

- CLI/Copilot、Studio UI、Tauri 配置和发布 metadata 聚焦测试通过。
- `cargo check` 与 `npm run studio-desktop:package:dev` 通过。
- 本地签名生成 updater archive/signature/`latest.json`，签名内容与 metadata 一致，DMG 校验有效，`SHA256SUMS` 全部通过。
