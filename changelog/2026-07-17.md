# 2026-07-17

## 14:58 - 完成五 CLI reviewer session 能力实测

- 在不读取 provider token、cookie、keychain、登录态或私有 session store 的前提下，使用公开 CLI 结构化输出完成 Codex、Claude Code、Cursor Agent、Antigravity 和 OpenCode 的 start/resume 能力矩阵；包含版本、字段名、占位命令、失败分类、立即恢复窗口与启用结论。
- Claude Code `2.1.207` 与 OpenCode `1.17.18` 完成结构化 start、实际 ID 显式 resume、伪 ID 拒绝和精确回复闭环，后续可按 `experimental` 接入；Codex、Cursor 与 Antigravity 保持 `fresh-only`。
- Cursor 的伪 ID 与实际 ID 表现相同，当前证据不能证明会话连续性；外审后已将其 `explicit_resume` 收敛为 `false`，避免仅凭 exit 0、回复或 ID 回显误判恢复成功。
- 原始结构化输出和真实 session ID 仅短暂存在于仓库外临时目录并已删除；能力矩阵、任务报告、git diff 的敏感值扫描均未发现真实 ID。

### 验证

- 五个 provider 共 40 个固定字段完整，`git diff --check` 通过，runtime/schema 零 diff。
- AgentMesh Review Gate 首轮提出 1 Should / 1 Nit，修正后复审为 `Spec compliance: Approved`、`Task quality: Approved`、`LGTM`，0 Must / 0 Should / 0 Nit。

## 15:02 - 校准 reviewer session 实现计划

- 以已外审能力矩阵为唯一 provider 事实源，将 P3.2 的 V1 enabled adapter 收敛为 `claude-code-cli`（`session_id`）和 `opencode-cli`（`sessionID`）；仅两者安排 session parser、脱敏 fixture 与 resume command builder。
- `codex-cli`（已观测 `thread_id`，start/resume 未闭环）、`cursor-agent`（已观测 `session_id`，伪 ID 也被接受）和 `antigravity-cli`（`none`）保持 `fresh-only`，不安排 V1 parser、fixture 或 command builder。
- P1–P5 逐项校验仍可执行：通用契约/基础设施不依赖 provider resume，P3 fallback 保持 fresh，P4 不复制 provider ID，P5 A/B 仅覆盖两个 enabled adapter；未修改 runtime、schema、测试或能力矩阵。

### 验证

- 对照能力矩阵核对 enabled adapter、parser 输入字段和三项 fresh-only 原因；`git diff --check` 通过，变更范围仅为计划与当日 changelog。

## 15:10 - 通过 reviewer session P0 阶段门禁

- P0 能力调研与计划校准完成：P3.2 仅为 Claude Code/OpenCode 安排 V1 session parser、fixture 和 resume builder，其他三个 adapter 明确走 capability-false fresh fallback。
- P1–P5 可执行性外审确认通用契约、scope/registry/lease、fresh fallback、跨宿主 scope 和正式 independent gate 不依赖未启用 provider。
- Cursor reviewer 首轮 exit 1 且无技术 finding；切换 Claude 4.8 fresh reviewer 后，spec compliance 与 task quality 均 Approved，`LGTM`，0 Must / 0 Should / 0 Nit。

### 验证

- AgentMesh Review Gate `workflow-20260717150733` 已完成 review/decide；P0.1 与 P0.Z 证据、计划进度和当日 changelog 已闭环。

## 15:23 - 增加 reviewer session 公共契约

- Core 新增 `auto` / `interactive_continuous` / `independent` review session mode、四种 attempt mode、optional attempt provenance，以及 adapter resume/structured-ID capability 与五类 session failure classification。
- Workflow recipe version 继续为 1，新增 optional `review_session_mode`；旧 recipe 缺字段时默认 `auto`，Review Gate 显式为 `auto`，Release Check 固定为 `independent`。
- Packet schema version 继续为 1，新增 StageAttempt 和 capability 字段均为 optional；本阶段未接入 provider parser、registry、scope、lease、CLI flag 或 dispatch resume。

### 验证

- TDD RED 确认 contract/export 缺失；focused GREEN 33/33，全量 `npm test` 和 `git diff --check` 通过。
- AgentMesh Review Gate `workflow-20260717152057`：spec/quality Approved，0 Must / 0 Should / 2 Nit；无阻断 finding。

## 15:44 - 冻结 reviewer session 运行策略输入

- `flow run` 与 preset run 新增 `--review-session-mode`、`--host-kind`、`--conversation-scope`；host kind 使用闭集，传播 token 只接受 `amscope_v1:<RFC4122 UUID>`。
- Run status 冻结 requested/effective policy 与脱敏 host scope 元数据，不保存 native conversation ID 或完整 propagated token；错误与 human/JSON 输出同样不回显敏感输入。
- Workflow `independent` 是不可降级硬门槛；CLI 可请求更严格的 independent，但不能改成 continuous。本阶段只记录 policy intent，未接入 registry/provider resume。

### 验证

- TDD RED 后 focused GREEN 58/58；全量 `npm test`、`git diff --check` 通过，legacy 无 flags run 回归通过。
- AgentMesh Review Gate `workflow-20260717153921`：Spec/Quality Approved、LGTM，0 Must / 0 Should / 3 Nit；无阻断 finding。
