# 2026-05-16

## 22:51 - 完成 P3.5 agent registration validation

- `agents add` 现在 fail fast 校验 agent id、alias、capability、reasoning_effort 和 `timeout_seconds`，并在写入前阻止 alias 与既有 agent id / alias 冲突。
- Registration 支持 `--timeout-seconds` 写入 agent 配置，范围固定为 30-3600；readiness probe 继续使用独立 probe timeout，避免把 agent invocation timeout 混入探活。
- `--skip-verify` 改为 diagnostic-only 路径，不再成功写配置；README、CLI usage 和 init 注释同步更新。
- Config loader 增加 agent registry 元数据校验，启动时拒绝 malformed alias/capability/reasoning/timeout 配置。
- `plan.md` 标记 P3.5 完成，当前下一步切到 P3.Z 阶段收尾校准。

### 验证

- `npm run build && node --test dist-node/tests-node/agent-registration.test.js dist-node/tests-node/cli-surface.test.js`（36/36）
- `npm run build && node --test dist-node/tests-node/agent-registration.test.js dist-node/tests-node/cli-surface.test.js dist-node/tests-node/config-layering.test.js dist-node/tests-node/readiness.test.js dist-node/tests-node/core-contracts.test.js`（110/110）
- `git diff --check`
- `npm test`（371/371）

## 22:39 - 完成 P3.4 failure/fallback/timeout resolution

- `createFlowRun()` 现在按 workflow policy、preset policy、preset fallback、global fallback、agent timeout 和 CLI `--timeout-seconds` 物化 packet v2 execution routing。
- `status.json` 写入 resolved `stage_invocations`、`stage_failure_policies`、`stage_fallbacks`、`fallback_provenance` 和 per-lane `timeout_provenance`，dispatch 后续无需重新读取 workflow/preset/config。
- Failure policy 支持 strict merge、`terminal` 清空 fallback、`required` 创建期校验；pure `current` 节点禁用 fallback 并拒绝 required fallback。
- Fallback route 支持继承、去重、过滤 primary agents、capability 校验、按 `max_fallback_agents` 截断，以及 preset/global fallback timeout precedence。
- CLI run/help 增加 creation-time `--timeout-seconds`；workflow registry 和 SDK detail 同步暴露 parsed `failurePolicy`。
- `plan.md` 标记 P3.4 完成，当前下一步切到 P3.5 agent registration validation。

### 验证

- `npm run build && node --test dist-node/tests-node/flow-run.test.js dist-node/tests-node/flow-preset-run.test.js`（25/25）
- `node --test dist-node/tests-node/flow-run.test.js dist-node/tests-node/flow-preset-run.test.js dist-node/tests-node/flow-dispatch.test.js dist-node/tests-node/workflow-registry.test.js dist-node/tests-node/config-layering.test.js dist-node/tests-node/core-contracts.test.js dist-node/tests-node/sdk-read.test.js dist-node/tests-node/studio-ui.test.js dist-node/tests-node/management-cli.test.js`（138/138）
- `git diff --check`
- `npm test`（367/367）

## 22:18 - 完成 P3.3 primary assignment resolution

- `createFlowRun()` 现在按 node explicit assignment、CLI stage-type role flags、legacy workflow defaults、preset stage/common defaults、global stage/common defaults 解析 primary assignments，并写入 node-id keyed `stage_assignments` 与 `assignment_provenance`。
- Direct workflow run 可仅依赖 global `[default_stage_agents]` 创建 packet；CLI role flags 会展开到 repeated node ids。
- Preset run 支持 preset explicit/default assignments fallback 到 global defaults，且保留 `preset` / `preset_source` provenance。
- Run creation 校验 unknown agent、stage capability mismatch、execute exactly one、fanout 上限和 `current` 混用；status contract 同步 assignment provenance values。
- `plan.md` 标记 P3.3 完成，当前下一步切到 P3.4 failure/fallback/timeout resolution。

### 验证

- `npm run build && node --test dist-node/tests-node/flow-run.test.js dist-node/tests-node/flow-preset-run.test.js dist-node/tests-node/management-cli.test.js dist-node/tests-node/config-layering.test.js`（62/62）
- `npm run build && node --test dist-node/tests-node/flow-run.test.js dist-node/tests-node/flow-preset-run.test.js dist-node/tests-node/flow-dispatch.test.js`（50/50）
- `git diff --check`
- `npm test`（363/363）

## 22:09 - 完成 P3.2 preset registry 与 run shortcut

- 新增 preset registry parser/validator/template/provenance helper，支持用户级与项目级 `presets/*.toml`，校验 workflow 引用、derived node ids、primary/fallback agent、failure policy shape、fallback execution bounds 和 4+ plan/decide soft warnings。
- CLI 新增 `agentmesh preset list/show/init/add/remove/doctor`；`preset init` 输出 workflow-derived node ids、stage assignment、failure policy 和 fallback placeholders。
- `agentmesh run <preset-id>` 改为只解析 preset namespace，创建 packet 时写入 `preset` / `preset_source`；裸 workflow id 会 fail fast 并提示显式使用 `--workflow`。
- `docs/contracts/preset-toml.md` 同步 registry CLI、默认 user registry、add/doctor 验证和 bare run namespace 规则。
- `plan.md` 标记 P3.2 完成，当前下一步切到 P3.3 primary assignment resolution。

### 验证

- `npm run build && node --test dist-node/tests-node/management-cli.test.js dist-node/tests-node/flow-preset-run.test.js dist-node/tests-node/core-contracts.test.js`（23/23）
- `git diff --check`
- `npm test`（359/359）

## 21:55 - 完成 P3.1 global default 与 fallback config

- config loader 支持 `[default_stage_agents]` common defaults、flat `[default_stage_agents.stage_types]` shorthand、nested `[default_stage_agents.stage_types.<type>]`、`[fallback]` common fallback 和 `[fallback.stage_types.<type>]` execution settings。
- 新增 global default/fallback 校验：禁止 global node defaults / node fallback，校验 agent id、`current` 混用、stage capability、execute stage-type exactly one、fanout/fallback 列表上限、fallback attempt 和 timeout bounds。
- `docs/contracts/config-toml.md` 同步 global config 不支持 node scope、stage-type fallback settings 覆盖 global settings，以及 flat stage-type default 示例。
- `plan.md` 标记 P3.1 完成，当前下一步切到 P3.2 preset registry 和 run shortcut。

### 验证

- `npm run build && node --test dist-node/tests-node/config-layering.test.js`（30/30）
- `git diff --check`
- `npm test`（354/354）

## 21:43 - 完成 P2 schema/readers 收尾校准

- P2.Z 通过 `review-gate` run `p2z-schema-readers-20260516-2133` 执行 schema/readers 只读审查，Claude review 无 Must findings。
- 接受并处理 3 个 Should：`stage_assignments` 改为 exact node-id key 校验，failure policy schema / workflow parser 拒绝 unknown policy fields，Studio 空 invocation 显示“无”而不是“未知”。
- 新增 `docs/reviews/workflow/p2-workflow-schema-readers-2026-05-16.md`，记录 accepted findings、deferred provenance schema、agent_timing sparse rationale、SDK 双读 residual risk。
- `plan.md` 标记 P2.1-P2.4、P2.Z 与 P2 阶段门禁完成，下一步进入 P3 runtime creation/config/preset resolution。

### 验证

- `npm run build && node --test dist-node/tests-node/core-contracts.test.js dist-node/tests-node/workflow-registry.test.js dist-node/tests-node/studio-ui.test.js`（40/40）
- `npm run build && node --test dist-node/tests-node/core-contracts.test.js dist-node/tests-node/packet-validate.test.js dist-node/tests-node/workflow-registry.test.js dist-node/tests-node/sdk-read.test.js dist-node/tests-node/studio.test.js dist-node/tests-node/studio-ui.test.js`（67/67）
- `npm test`（352/352）
- `git diff --check`

## 21:31 - 更新 SDK 与 Studio v2 状态读取

- SDK run summary 新增 packet v2 stage reader：暴露 `stage_nodes`、node-id keyed assignment、invocation、failure policy、fallback、attempt 和 provenance，并用 `stage_nodes` 推导 repeated node 的当前阶段。
- Studio Workflow Flow 改为从 `stage_nodes` 渲染 node id，可显示 `decide_2` 等重复节点；节点详情补充 assignment、invocation、failure policy、fallback、attempt record 和 provenance。
- 中文 Studio UI 与手册文案统一把 review 译为“审查”，并保留英文 protocol 名 `review`。

### 验证

- `npm run build && node --test dist-node/tests-node/sdk-read.test.js dist-node/tests-node/studio-ui.test.js`（14/14）

## 21:21 - 加强 workflow recipe 校验

- Workflow TOML parser 改为显式字段 allowlist，拒绝未知顶层字段；`id` 必填，不再从文件名兜底。
- `when_to_use`、`packet_artifacts`、`quality_gates` 必须是非空 string list，`packet_artifacts` 必须覆盖 workflow stage 的 canonical artifact。
- 新增 `[failure_policy.stage_types.*]` 和 `[failure_policy.nodes.*]` 校验：mode 只能是 `allow|required|terminal`，`max_fallback_agents` 在 `1-3` 内，terminal 不能配置 max fallback，未知 node id 错误会列出合法 derived node ids。
- `workflows add` 通过同一个 parser 在复制前拒绝无效 workflow 文件；SDK / Studio fixture 同步补齐 canonical artifacts。
- `docs/contracts/workflow-toml.md` 同步字段 allowlist、id 必填、required arrays、artifact coverage 和 failure policy 规则。

### 验证

- `npm run build && node --test dist-node/tests-node/workflow-registry.test.js dist-node/tests-node/management-cli.test.js`（27/27）
- `npm run build && node --test dist-node/tests-node/flow-run.test.js dist-node/tests-node/flow-dispatch.test.js dist-node/tests-node/sdk-read.test.js dist-node/tests-node/studio.test.js dist-node/tests-node/studio-ui.test.js dist-node/tests-node/workflow-registry.test.js dist-node/tests-node/management-cli.test.js`（95/95）
- `npm test`（350/350）

## 21:12 - 完成 P1 workflow contract freeze 收尾

- P1.Z 执行 review-gate run `p1z-contract-freeze-20260516-2050`；Claude 4.7 review 完成且无 Must findings，opencode lane 未在 900s 窗口内产出 review artifact，已作为不可用证据记录。
- 接受并处理 3 个 Should：`execute` 多 agent 错误文案改为 frozen contract 口径，删除 packet validator 里 v1 顶层 role 字段死分支，`createFlowRun()` 改用 core 导出的 `DEFAULT_INVOCATION_TIMEOUT_SECONDS`。
- 新增 `docs/reviews/workflow/p1-workflow-contract-freeze-2026-05-16.md`，记录 review 输入、accepted findings、P3 deferred findings 和 residual risk。
- `plan.md` 标记 P1 gate / P1.Z 完成，并校准 P2：P2.1/P2.2 已由 P1.2-P1.4 提前满足，下一步切到 P2.3 workflow validation。

### 验证

- `npm run build && node --test dist-node/tests-node/core-contracts.test.js dist-node/tests-node/workflow-registry.test.js dist-node/tests-node/packet-validate.test.js dist-node/tests-node/packet-io.test.js dist-node/tests-node/sdk-read.test.js dist-node/tests-node/studio.test.js dist-node/tests-node/studio-ui.test.js`（67/67）
- `npm run build && node --test dist-node/tests-node/flow-dispatch.test.js dist-node/tests-node/packet-validate.test.js dist-node/tests-node/flow-run.test.js dist-node/tests-node/core-contracts.test.js`（66/66）
- `npm test`（346/346）

## 20:50 - 固化 execution routing resolution 契约

- 新增 `docs/contracts/config-toml.md` 和 `docs/contracts/preset-toml.md`，明确 default primary agents、preset/node assignment、failure policy、fallback routing、timeout priority 和 provenance 的解析顺序。
- `stage-dispatch.md`、`status-json.md`、`workflow-toml.md`、`execution-policy.md`、`config-layering.md` 和 `agent-registration.md` 同步 packet v2 resolved execution facts、fallback attempt、`timed_out`、`lane_attempt`、`current` exclusion、`--timeout-seconds` 和 diagnostic-only `--skip-verify` 目标契约。
- core 导出 fanout/fallback/timeout 常量，并把 packet invocation/fallback/attempt timeout schema 收紧到 `30-3600`；workflow compatibility 从“包含 2”收紧为精确 `[2]`。
- `tests-node/core-contracts.test.ts` 固定新 contract docs 和 timeout/attempt schema；workflow/flow run tests 覆盖 `[1,2]` 不再被接受。

### 验证

- `npm run build && node --test dist-node/tests-node/core-contracts.test.js dist-node/tests-node/workflow-registry.test.js dist-node/tests-node/flow-run.test.js`（41/41）
- `npm test`（346/346）

## 20:39 - 允许 decide 作为可重复 checkpoint

- `deriveStageNodes()` 放开非连续 repeated `decide`，同时保留 `decide` 不能首位、不能连续的 workflow 结构约束。
- runtime artifact 命名改为语义后缀：`decision_2.md` / `decision_2`、`handoff_2`、`verification_2`、`findings_2`，避免把 stage node id 混入 artifact id。
- prompt assembly 删除 repeated-stage 专用分支，统一按顺序携带 prior evidence，并给历史输出标注 `Current Plan` / `Handoff` / `Verification` / `Findings` / `Decision`。
- release verdict 增加最终节点 gate：只有 `workflow = "release-check"` 且当前节点是最后一个 `decide` 时才记录 verdict；invalid verdict 的 `failed_stage` 现在指向实际 stage node id。
- `docs/contracts/*` 同步 repeatable decide、canonical artifacts、prior evidence 和 release verdict 节点规则。

### 验证

- `npm run build && node --test dist-node/tests-node/core-contracts.test.js dist-node/tests-node/workflow-registry.test.js dist-node/tests-node/flow-dispatch.test.js dist-node/tests-node/flow-retry-resume.test.js dist-node/tests-node/release-check-flow.test.js`（63/63）
- `npm test`（346/346）
- `git diff --check`

## 19:50 - 增加 context 和 prompt byte 观测

- `status.json` 新增 `context_bytes` 和 `prompt_bytes`，`flow run` 写入最终 `context.md` UTF-8 bytes，`writePrompt()` 与 fanout synthesis prompt 写入 per-prompt bytes、路径、stage、agent 和 kind。
- `flow status --json` 直接暴露 metrics；只观测不 fail，未生成 context 时记录 `context_bytes = 0`。
- SDK run summary 同步读取 `context_bytes` 和 `prompt_bytes`，方便 Studio 或外部只读 consumer 后续展示 token 成本代理指标。

### 验证

- `npm run build && node --test dist-node/tests-node/flow-context.test.js dist-node/tests-node/flow-dispatch.test.js`

## 19:54 - 补充 context policy 默认建议

- `agentmesh init` 的本地 config 模板新增注释版 `[context_policy]` 示例，给出 `max_files`、`max_bytes`、`denied_paths` 和 `redact_patterns` 的保守起点，但默认保持注释避免改变现有运行行为。
- `docs/contracts/context-policy.md`、`README.md` 和 `agentmesh-skill/SKILL.md` 同步说明 bytes 只是 token 成本代理，并推荐排除 `.agentmesh/runs`、`docs/archive`、`dist-node`、`node_modules` 这类生成或归档路径。
- 文档补充 `--exclude-correction <id>` 的 per-run 用法，避免无关 active correction 进入当前 run context。

### 验证

- `npm run build && node --test dist-node/tests-node/cli-surface.test.js dist-node/tests-node/config-layering.test.js dist-node/tests-node/flow-context.test.js`

## 20:01 - 为生成后 context 加预算边界

- `buildContextPack()` 在最终组装后按 `context_policy.max_bytes` 重新计算 UTF-8 bytes，超限时写入以 `AGENTMESH_CONTEXT_TRUNCATED` 开头的 bounded `context.md`，包含 `max_bytes`、`original_bytes` 和 scoped diff 的 `source_command`。
- scoped `git diff`、MCP/spec/corrections 等生成内容现在进入最终总量判断；`status.json.context_bytes` 记录实际写入的 bounded context bytes。
- `docs/contracts/context-policy.md` 和 `README.md` 说明截断 marker 是不完整证据和 residual risk 信号。

### 验证

- `npm run build && node --test dist-node/tests-node/flow-context.test.js --test-name-pattern="truncates|final byte budget"`

## 20:03 - 完成 P0 token budget 基线收口

- P0 阶段完成 context/prompt byte metrics、context policy 默认建议、scoped diff 和生成后 context 预算截断。
- 临时 review-gate fake fanout run 验证 `flow status --json` 可见 `context_bytes = 1138`，以及 `prompt_review_reviewer_a` / `prompt_review_reviewer_b` 的 prompt bytes。
- 当前无 open Must findings；P0 保留 residual risk：bytes 仍是 token 成本代理，不等同具体模型 tokenizer。

### 验证

- `npm test`（343/343）
- `git diff --check`
- 临时 `review-gate` fake run：`status = review_completed`，prompt byte metrics 可见。

## 20:04 - 完成 P1.1 代码事实校准

- 重新核对 core、runtime flow、workflow registry、CLI、SDK、Studio 和测试事实，确认当前实现仍是 packet schema v1，`decide` 仍是全局唯一 final gate。
- 确认 runtime 仍写/读 legacy 顶层 assignment 字段，`stage_invocations`、`stage_failure_policies`、`stage_fallbacks`、`stage_attempts` 和 provenance 尚未建模。
- 确认 prompt 仍保留 repeated-stage 专用分支，release verdict helper 仍硬编码 `decide`，Studio/README/index 中“评审”和 final-only decide 文案仍需 P5 同步。

### 验证

- `rg` 定位 `deriveStageNodes`、`PacketStatusSchema`、`stageAgents`、artifact naming、prompt assembly、release verdict、workflow registry、preset/default/fallback/timeout、SDK/Studio 文案。

## 20:25 - 固化 packet schema v2

- `CURRENT_PACKET_SCHEMA_VERSION = 2` 独立于通用机器文件 schema v1；`status.json` 新增必填 `stage_invocations`、`stage_failure_policies`、`stage_fallbacks`、`stage_attempts` 和 assignment/fallback/timeout provenance。
- `flow run` 物化 packet v2 状态，不再写顶层 `plan` / `execute` / `review` / `decide` legacy role 字段；dispatch 只从 node-id keyed `stage_assignments` 读取 agent。
- workflow recipe compatibility 改为 `compatible_packet_schema_versions = [2]`，packet validation 明确拒绝 v1 status；SDK/Studio 测试 fixture 同步升级到 v2。

### 验证

- `node --test dist-node/tests-node/core-contracts.test.js dist-node/tests-node/workflow-registry.test.js dist-node/tests-node/flow-run.test.js dist-node/tests-node/flow-context.test.js dist-node/tests-node/packet-validate.test.js dist-node/tests-node/packet-io.test.js dist-node/tests-node/flow-dispatch.test.js`
- `npm test`（344/344）

## 00:57 - 完成 Studio review/release 和操作区重构

- `apps/studio/src/assets.ts` 增加 mutation 操作上下文，显示当前 run、操作阶段和附加阶段，并在空态清除旧上下文。
- review/release 面板展示 release verdict diagnostic、剩余风险、原始 review 和 findings 分组，失败 mutation 输出保留命令、`exit_code`、`stdout`、`stderr`，错误状态不再被请求结束覆盖成空闲。
- `tests-node/studio-ui.test.ts` 补充 P1.5 覆盖，验证 review/release 证据、危险操作上下文和 mutation 失败输出。

### 验证

- `npm run build && node --test dist-node/tests-node/studio-ui.test.js dist-node/tests-node/studio.test.js`
- `git diff --check`

## 01:01 - 完成 Studio 配置视图整理

- `apps/studio/src/assets.ts` 新增 `#catalog-diagnostics` 区域，配置视图在显示 diagnostics 数量之外，逐行展示具体 `target` 和 `message`。
- 强化 agents/workflows 可见字段测试，覆盖 agent id、label、adapter、model、capabilities、aliases、source layer，以及 workflow id、name、status、source、stages。
- `tests-node/studio.test.ts` 同步校准 server catalog 字段断言，并确认 diagnostics 保留 agents/workflows target。

### 验证

- `npm run build && node --test dist-node/tests-node/studio-ui.test.js dist-node/tests-node/studio.test.js`
- `git diff --check`

## 01:18 - 完成 Studio UI 质量收口

- `apps/studio/src/assets.ts` 提升 mutation 主按钮触控高度到 44px，event pager 按钮到 36px，并让分页 DOM 使用 `.event-pager` class 使样式真实生效。
- tablet 宽度下将 overview 降为两列，stage row 与 events/artifacts 面板降为单列，修复 768px 视觉 QA 中的文字挤压问题。
- `tests-node/studio-ui.test.ts` 新增 accessibility/responsive guardrails，覆盖 icon-only aria、表单 label、触控目标、无装饰渐变/orb、无 viewport-width 字号和响应式断点。
- Browser QA 使用 localhost harness 检查 375px、768px、1280px 截图；确认关键 section、plan artifact、分发按钮存在，桌面宽度无横向滚动，artifact preview 和 mutation 操作可点击。

### 验证

- `npm test`
- `git diff --check`

## 06:25 - 定义公共扩展接口边界

- 新增 `docs/contracts/public-extension-surface.md`，列出现有 consumer（CLI、Studio、Desktop）和潜在 consumer（MCP server、本地脚本、第三方 adapter、external dashboard）。
- 将扩展能力分为 read-only、controlled write、internal 三类：第一版只允许读 workflows/agents/runs/events/artifacts/timing 等视图；创建 run、注册 agent、配置更新和状态机推进继续留在 CLI/runtime 控制路径。
- 明确当前 slice 只写 decision，不创建 `packages/sdk`；只有真实第二 consumer 证明需要且 CLI JSON / Studio API 不足时才 promotion。
- `tests-node/core-contracts.test.ts` 和 `tests-node/package-structure.test.ts` 固定 contract doc 和 no-new-package guardrail。
- Review 未发现需要额外修复的问题。

### 验证

- `npm run build && node --test dist-node/tests-node/core-contracts.test.js dist-node/tests-node/package-structure.test.js`
- `git diff --check`

## 06:43 - 增加只读 AgentMesh SDK

- 新增 `packages/sdk`，提供稳定只读 API：`listWorkflows`、`getWorkflow`、`listAgents`、`listRuns`、`getRun`、`listRunEvents`、`listArtifacts`，并复用 runtime 的 config、workflow registry 和 packet 解析逻辑。
- `apps/studio/src/packet-browser.ts` 改为 SDK 薄适配层，Studio 保持原有 run、events、artifacts、review/release evidence 输出 shape。
- CLI 的 `agents list`、`workflows list`、`workflows show` 开始复用 SDK；workflow/agent 写命令仍留在 runtime 控制路径。
- `docs/contracts/public-extension-surface.md` 从 P5.1 的 decision-only 更新为 read-only SDK promoted，并继续明确 no public write API、no event subscription、no adapter plugin、no MCP server package。
- Review/验证修复 `getWorkflow` 在无 config 项目中不应失败、CLI workflow JSON 需保留 `path` 兼容字段、旧 contract 测试仍要求 no-new-package 的问题。

### 验证

- `npm run build && node --test dist-node/tests-node/sdk-read.test.js dist-node/tests-node/studio.test.js dist-node/tests-node/cli-surface.test.js dist-node/tests-node/workflow-registry.test.js dist-node/tests-node/package-structure.test.js`
- `npm run build && node --test dist-node/tests-node/core-contracts.test.js dist-node/tests-node/management-cli.test.js dist-node/tests-node/sdk-read.test.js dist-node/tests-node/package-structure.test.js`
- `npm test`
- `git diff --check`

## 06:50 - 定义 adapter 插件接口

- 新增 `docs/contracts/adapter-plugin.md`，把第一版 adapter plugin 限定为接入新 agent：`id` / `label`、`detect()`、`resolveModel(input)`、`probe(config)`、`buildInvocation(request)`、`parseResult(output)`。
- 新增 `packages/runtime/src/adapters/plugin.ts`，提供 plugin manifest、agent config 构建、runtime adapter metadata 投影和只读/无 packet writer 的 helper surface。
- `tests-node/adapter-plugin.test.ts` 增加 fixture adapter，只用于 contract tests，覆盖 model resolve、agent config 构建、detect、probe、真实 fake CLI invoke 和 parseResult。
- 明确第三方 adapter 不能写 `status.json`、`events.jsonl`、`artifacts.toml` 或 stage artifacts，不能推进 runtime state、绕过 readiness、获取 run lock 或作为 cloud runner。
- Review 未发现需要额外修复的问题。

### 验证

- `npm run build && node --test dist-node/tests-node/adapter-plugin.test.js dist-node/tests-node/core-contracts.test.js dist-node/tests-node/adapter-registry.test.js dist-node/tests-node/adapter-invocation.test.js dist-node/tests-node/agent-registration.test.js`
- `npm test`
- `git diff --check`

## 06:53 - 评估 MCP server 和 UI package 后续边界

- 新增 `docs/decisions/extension-followups.md`，确认 MCP server 和 `packages/ui` 仍延期，不创建空壳 package。
- MCP server 后续如启动，第一步必须先写 `docs/contracts/mcp-readonly.md`，并限制为 workflows、agents、runs、events、artifacts 只读 resources，不启动 run、不修改 workflow / agent config、不写 packet 文件。
- `packages/ui` 只有在 Studio 和 Desktop 出现明显重复组件且不改变 Studio 信息架构时再抽；只抽展示组件，不迁移业务协议、SDK reads、mutation commands 或 app-server 逻辑。
- `tests-node/package-structure.test.ts` 固定 `packages/mcp-server` / `packages/ui` 不存在，以及 no empty package 决策。
- Review 未发现需要额外修复的问题。

### 验证

- `npm run build && node --test dist-node/tests-node/package-structure.test.js`
- `git diff --check`

## 06:58 - 同步 P5 公共扩展接口文档

- `README.md` 同步 P5 done、334 tests、只读 SDK、adapter plugin contract、MCP server / `packages/ui` 延后条件、public write SDK 非目标和 signed/notarized DMG packaging host 限制。
- 根目录 `index.html` 同步 P5 done、334 tests、SDK-backed read paths、adapter plugin contract 和后续扩展 Later 触发条件，不再把 P5 描述为 next。
- `plan.md` 标记 P5 完成，补齐 P5.1 decision-only 到 P5.2 SDK promotion 的证据，并把当前下一步改为按真实 consumer 触发后续扩展。
- Review/release gate 修复 `plan.md` 顶部仍把 Public SDK 作为后续扩展、README/index 测试数仍是 333 的旧口径；历史 P4 326 证据保留。

### 验证

- `npm test`
- `git diff --check`

## 04:38 - 记录 runtime timing 明细

- `status.json` 新增结构化 `runtime_timing`，记录 `flow run` 的 config load、MCP connect 和 run total timing；`flow status --json` 可直接读取，human 输出保持简洁。
- per-agent `agent_timing` 增加 `config_load_ms`、`adapter_spawn_ms`、`agent_total_ms` 和 `total_ms`；当前同步 `spawnSync` 路径无法观察 first output，因此 `first_output_ms` 只在合同中保留为后续 async spawn 字段。
- MCP client 增加 `onTiming` callback，context ingestion 会把 MCP connect timing 汇总到 packet status；adapter 调用增加 `runAgentCallWithTiming`，dispatch 成功和失败路径都会写入已知 timing。
- `docs/contracts/status-json.md` 同步 runtime timing 字段语义；fake command adapter baseline：run `config_load_ms=2`、`total_ms=2`，agent `adapter_spawn_ms=96`、`agent_total_ms=97`。
- Review 修复 core timing schema helper 命名不准、MCP timing error wrapper 未保留原始错误链的问题。

### 验证

- `npm run build && node --test dist-node/tests-node/core-contracts.test.js dist-node/tests-node/adapter-invocation.test.js dist-node/tests-node/mcp-client.test.js dist-node/tests-node/flow-context.test.js dist-node/tests-node/flow-dispatch.test.js`
- `npm test`
- `git diff --check`

## 04:47 - 复用 MCP client 连接

- 新增显式 `createMcpClientCache` / `closeMcpClientCache`，同一 cache 内相同 command、args、configured env 和 cwd 的 MCP list/read 复用一个 active stdio session。
- 裸 `readMcpTextResource` / `readMcpTextResources` / `listMcpResourceHints` 仍保持单次调用后关闭；`flow run` context capture 使用短生命周期 cache，结束后显式关闭，避免跨 run 或 daemon 级泄漏。
- cache hit/miss 进入 `onTiming`；context capture 汇总 `mcp_cache_hits` / `mcp_cache_misses` 到 `status.runtime_timing`。
- 错误、协议失败、timeout、server exit 或显式 close 会从 cache 移除 session；baseline 样本：首次 miss `mcp_connect_ms=108`，后续 hit `mcp_connect_ms=0`。
- Review 补上 context capture 的 cache hit/miss status 可见面，并整理 cache key helper 可读性。

### 验证

- `npm run build && node --test dist-node/tests-node/mcp-client.test.js dist-node/tests-node/mcp-resource.test.js dist-node/tests-node/flow-context.test.js dist-node/tests-node/core-contracts.test.js`
- `git diff --check`

## 04:52 - 增加 async adapter invocation

- 新增 `runAgentCallAsync`，使用 `child_process.spawn()` 执行 agent 调用，为后续 fanout/concurrent dispatch 提供 async runtime surface。
- async 路径复用现有 adapter invocation preparation，覆盖 stdout capture、explicit output-file arg、stdin prompt、非零退出、timeout 和 spawn failure；同步 `runAgentCall` 保持给现有非 fanout 路径使用。
- async timing 记录 `first_output_ms`、`adapter_spawn_ms`、`agent_total_ms` 和 `total_ms`；无 stdout capture 时继续省略 `first_output_ms`。
- `docs/contracts/adapter-invocation.md` 同步 async invocation 行为和错误边界。
- Review 修复 async output-file write error 没有 reject、timeout 后缺少 SIGKILL 兜底的问题。

### 验证

- `npm run build && node --test dist-node/tests-node/adapter-invocation.test.js`
- `git diff --check`

## 01:22 - 抽出 Studio 参数解析

- 新增 `apps/studio/src/args.ts`，提供 `parseStudioArgs`，集中处理 Studio `--host`、`--port` 默认值和端口校验。
- `apps/studio/src/main.ts` 改为复用 parser，为后续 `agentmesh studio` CLI 入口共享参数解析做准备。
- 新增 `tests-node/studio-args.test.ts`，覆盖默认 host/port、显式 host/port、port 0 和非法端口。

### 验证

- `npm run build && node --test dist-node/tests-node/studio-args.test.js`
- `npm test`
- `git diff --check`

## 01:35 - 增加 agentmesh studio CLI 入口

- 新增 `packages/cli/src/commands/studio.ts`，支持 `agentmesh studio --host --port --workspace --no-open`，并复用 Studio 参数解析启动本地 Studio server。
- `packages/cli/src/cli.ts` 接入 `studio` 命令和 usage；`apps/studio/src/main.ts` 也传入当前安装内 CLI 路径，让 catalog 和 mutation 默认使用同一份 CLI 安装。
- `tests-node/studio-cli.test.ts` 覆盖服务启动、workspace 指向、非法端口、browser open 失败仍输出可复制 URL，以及 `--config` 相对路径在切换 workspace 前解析。
- Review 发现相对 `--config` 会在 `--workspace` 目录下被重新解释，已改为 CLI 层先解析为绝对路径后传给 Studio server。

### 验证

- `npm run build && node --test dist-node/tests-node/studio-cli.test.js`
- `npm test`
- `git diff --check`

## 01:43 - 完成 P1 Studio 产品化文档收口

- `README.md` 更新当前验证基线为 232 tests，记录 `agentmesh studio --host --port --workspace --no-open`，并把当前下一步改为 P2 Agent 注册默认命名与配置分层产品化。
- `index.html` 同步 P1 done、232 tests、`agentmesh studio` 入口、Studio CLI 手册和 P2/P3-P5 路线图，不再展示旧的 L8.6/170 tests 状态。
- `plan.md` 将 P1 标记为已完成，并把“当前下一步”改为 P2；P1.10 review 未发现新的 accepted findings。

### 验证

- `npm test`
- `git diff --check`

## 01:48 - 定义 agent 注册验证 contract

- 新增 `docs/contracts/agent-registration.md`，明确 `agents add` 从 adapter/model 解析、temporary candidate agent、注册前 readiness probe 到写入 config 的默认流程。
- contract 定义失败策略：unknown adapter、adapter CLI 不存在、无法解析或 ambiguous model、重复 id、auth/model probe 失败、timeout 和 config 写入失败都 fail fast 且不写 config。
- 明确 `--skip-verify` 只是逃生口：可跳过 auth/model availability probe，但不能跳过 adapter/model resolution、重复 id、命令路径和 config write safety；跳过时必须输出 warning。
- `tests-node/core-contracts.test.ts` 纳入 agent registration contract 文档存在性和关键语义检查。

### 验证

- `npm run build && node --test dist-node/tests-node/core-contracts.test.js`
- `git diff --check`

## 01:54 - 覆盖 agent model resolution 测试

- 新增 `tests-node/agent-registration.test.ts`，覆盖 exact model、normalized shorthand、provider final segment slug、ambiguous candidates、not_found 和 punctuation-only not_found。
- 新增 `packages/runtime/src/adapters/models.ts`，提供最小 `resolveAdapterModel`、`normalizeModelKey`、`modelSlugSegment` 和静态候选 skeleton；真实 adapter discovery 留到 P2.3。
- `packages/cli/src/commands/agents.ts` 在写 config 前解析模型；not_found 或 ambiguous 时返回 2 并不写 user config。
- `tests-node/cli-surface.test.ts` 增加 `agents add` 模型无法解析和模糊简称的失败测试。
- Review 修复 punctuation-only 输入被误报为 ambiguous 的边界。

### 验证

- `npm run build && node --test --test-name-pattern="model" dist-node/tests-node/agent-registration.test.js dist-node/tests-node/cli-surface.test.js`
- `npm run build && node --test dist-node/tests-node/agent-registration.test.js dist-node/tests-node/cli-surface.test.js dist-node/tests-node/management-cli.test.js`
- `git diff --check`

## 02:04 - 增加 adapter model discovery 来源

- `packages/runtime/src/adapters/models.ts` 新增 `AdapterModelDiscovery` / `AdapterModelDiscoveryHook`，区分 `static-fixture`、`adapter-cli` 和 `unsupported` 来源，并让 `resolveKnownAdapterModel` 通过 discovery 结果解析 canonical model。
- 基于本机 CLI help 校准：Codex、Claude、Gemini 当前不暴露稳定 list models 命令，hook 明确标记 unsupported；OpenCode 暴露 `opencode models [provider]`，hook 记录 `opencode models`。
- normalized matching 改为把非字母数字分隔符统一成 `-`，同时保留 `gpt55` 这类 compact shorthand；provider-prefixed model 继续保留 canonical id，并只用 final segment 生成默认 slug。
- `tests-node/agent-registration.test.ts` 覆盖 discovery 来源、unsupported command adapter、known adapter fixture 解析；`tests-node/cli-surface.test.ts` 将 OpenCode surface 测试模型校准为当前 fixture 中的 `zhuanzhuan/deepseek-v4-pro`。

### 验证

- `npm run build && node --test --test-name-pattern="model" dist-node/tests-node/agent-registration.test.js dist-node/tests-node/cli-surface.test.js`
- `npm run build && node --test dist-node/tests-node/agent-registration.test.js dist-node/tests-node/cli-surface.test.js dist-node/tests-node/management-cli.test.js`
- `git diff --check`

## 02:07 - 增加默认 agent identity helper

- `packages/runtime/src/adapters.ts` 新增 `defaultAgentId`、`defaultAgentLabel` 和 `adapterShortName`，默认 id 使用 adapter short name 加 canonical model 的 final-segment slug。
- 默认 label 复用 adapter registry 的 `label`，并展示 canonical model，避免用户输入 `gpt55` 时把 raw shorthand 写进默认身份。
- `tests-node/agent-registration.test.ts` 覆盖 Codex `gpt55 -> gpt-5.5 -> codex-gpt-5-5`，以及 provider-prefixed OpenCode model 使用 final segment 生成 id。

### 验证

- `npm run build && node --test dist-node/tests-node/agent-registration.test.js`
- `git diff --check`

## 02:14 - 抽出注册前 readiness probe

- `packages/runtime/src/doctor/readiness.ts` 新增 `probeAgentReadiness(agent, options)`，让 doctor 的 command existence、help/version、auth/model probe 可直接用于未落盘的 candidate agent。
- 新增 `packages/runtime/src/adapters/registration.ts`，提供 `buildAgentRegistrationCandidate` 和 `probeAgentRegistrationReadiness`，返回写入前可使用的 `ok`、classification、message、hints 和 skip-verify warnings。
- registration readiness 对 generic `command` adapter 只验证命令可执行；AI CLI adapter 默认跑 auth/model probe，并把 model unavailable、auth timeout、help/version failure 转成可行动错误。
- `--skip-verify` helper 跳过 auth/model probe 并输出 warning，但 missing command 仍会失败；重复 id 校验保留到 P2.6 的 CLI 写入层接入。
- `tests-node/readiness.test.ts` 覆盖 in-memory candidate probe、skip-verify warning、missing command、model unavailable、auth timeout、help/version failure；`tests-node/agent-registration.test.ts` 覆盖 candidate 构造和 command adapter 行为。

### 验证

- `npm run build && node --test dist-node/tests-node/readiness.test.js dist-node/tests-node/agent-registration.test.js`
- `git diff --check`

## 02:23 - 接入验证式 agents add

- `packages/cli/src/commands/agents.ts` 将 `agents add` 的 positional id 改为可选；省略 id 时先解析 canonical model，再生成默认 id / label，例如 `codex + gpt55` 写入 `codex-gpt-5-5` 和 `model = "gpt-5.5"`。
- 写入前新增 resolved config layers 查重；重复 id fail fast 且不改文件，首次注册时没有现有 config 会按空 registry 处理。
- 默认运行注册前 readiness probe，probe 失败不写 config；`--skip-verify` 明确跳过 auth/model probe 并输出 warning，但仍要求 adapter/command 基础校验通过。
- unknown adapter 和过多 positional args 返回 usage 风格状态码 2；CLI help 更新为 `agents add [agent-id] --adapter <adapter> --model <model-or-alias> [--skip-verify]`。
- `tests-node/cli-surface.test.ts` / `tests-node/management-cli.test.ts` 使用 fake AI CLI shims，避免注册测试依赖本机真实 Codex/Gemini/OpenCode 登录状态，并覆盖默认 id、重复 id、readiness gate、skip verify 和 unknown adapter。

### 验证

- `npm run build && node --test dist-node/tests-node/cli-surface.test.js dist-node/tests-node/management-cli.test.js dist-node/tests-node/agent-registration.test.js`
- `npm test`
- `git diff --check`

## 02:28 - 更新验证式 agent 注册文档

- `README.md` 的 agent 注册示例优先展示省略 id 写法，说明 `gpt55` 会解析并写入 canonical model `gpt-5.5`，默认 id 为 `codex-gpt-5-5`。
- README 明确 `agents add` 会先运行 readiness probe，adapter 命令缺失、登录/模型不可用或 timeout 不会写 config；`--skip-verify` 仅作为离线 setup 逃生口。
- `agentmesh-skill/SKILL.md` 同步 setup 示例，改用 adapter/model alias 和当前 OpenCode fixture model，并提醒跳过验证后先运行 `agentmesh doctor`。
- `packages/cli/src/commands/init.ts` 生成的 config 注释同步 optional id、canonical model 默认命名、readiness probe 和 `--skip-verify` 警示。
- `tests-node/cli-surface.test.ts` 增加显式 id 兼容测试，并断言 init 模板包含新的注册命令和 skip-verify 文案。

### 验证

- `npm run build && node --test dist-node/tests-node/cli-surface.test.js dist-node/tests-node/readiness.test.js`
- `git diff --check`

## 02:31 - 验证 agent 注册 rollout

- 运行 agent registration rollout targeted smoke，覆盖 model resolution、CLI add/remove surface、management CLI 和 readiness probe 集成。
- 运行完整 `npm test`，确认 agent 注册默认命名、readiness gate、文档/skill 更新未影响 workflow、doctor、packet、Studio 等既有测试面。
- 使用临时 fake `codex` CLI 做手工 smoke：`agents add --adapter codex --model gpt55 --capability plan` 写入 `codex-gpt-5-5`、`label = "Codex CLI (gpt-5.5)"` 和 `model = "gpt-5.5"`，没有触碰真实 provider 登录态。

### 验证

- `npm run build && node --test dist-node/tests-node/agent-registration.test.js dist-node/tests-node/cli-surface.test.js dist-node/tests-node/management-cli.test.js dist-node/tests-node/readiness.test.js`
- `npm test`
- `git diff --check`

## 02:51 - 产品化 scoped MCP server 管理

- `agentmesh mcp list/add/remove` 增加用户级 / 项目级 scope 管理；`mcp list` 只读取 configured servers，不启动 MCP server，也不调用 `resources/list`。
- MCP server entry schema 收紧为 `command`、`args`、`resource_hints`，拒绝 token、env secret、session path 等不应落盘字段。
- `agentmesh doctor` 增加 MCP diagnostics，覆盖 duplicate id、command missing、server start/initialize failure 和 `resources/list` failure。
- Studio 配置视图新增 MCP column，通过 CLI 读取 source layer、source path、resource hints 和 diagnostics。
- Review 修复 `mcp add --scope project` 可能写出跨层重复 MCP id 的风险，改为写入前检查 resolved config layers。

### 验证

- `npm run build && node --test dist-node/tests-node/mcp-inventory.test.js`
- `npm test`
- `git diff --check`

## 03:09 - 实现 context policy 分层

- 新增 `[context_policy]` 配置 schema 和合同文档，支持 `required_sources`、`denied_paths`、`redact_patterns` 并集叠加，以及 `max_bytes`、`max_files`、`freshness_max_age_seconds` 取更严格值。
- `flow run` 在 packet 创建前解析最终 policy，自动追加必读资料，命中 denied path、超过文件数 / 字节数或无效 redaction regex 时 fail fast；成功 run 在 `status.json.resolved_context_policy` 和 `context.md` 记录 resolved policy。
- context capture 对匹配内容执行 redaction，并在 provenance 中记录 `redaction_state = "redacted"`；Studio run summary 展示 active context policy。
- Review 修复非数组 policy list 字段会在 merge 阶段抛 `source is not iterable` 的边界，改为每层先 schema 校验并返回可诊断错误。

### 验证

- `npm run build && node --test dist-node/tests-node/config-layering.test.js dist-node/tests-node/flow-context.test.js dist-node/tests-node/studio.test.js dist-node/tests-node/studio-ui.test.js dist-node/tests-node/core-contracts.test.js`
- `npm test`
- `git diff --check`

## 03:24 - 实现 review / release policy 分层

- 新增 `[review_policy.<workflow-id>]` 和 `[release_policy.<workflow-id>]` 配置 schema 与合同文档；project/explicit 层可叠加，user config 中的 review/release policy 会 fail fast。
- review policy 通过 `required_review_profiles` 引用 capability profile，不在项目配置中写死个人模型；`flow run` 创建前把 profile 解析成 concrete reviewer agent ids，并补入 review assignment。
- release policy 在 `status.json.resolved_review_release_policy` 中记录 source layers、policy hash、required evidence、needs-decision risks、skipped gates 和 missing evidence。
- `release-summary.md` 新增 Review/Release Policy 区块，summary refresh 会同步更新 status 中的 missing evidence；reviewer registry 只暴露 `review` / `reviewer.*` 这类 review-facing capability profiles。

### 验证

- `npm run build && node --test dist-node/tests-node/config-layering.test.js dist-node/tests-node/flow-run.test.js dist-node/tests-node/reviewer-registry.test.js dist-node/tests-node/release-check.test.js dist-node/tests-node/core-contracts.test.js`
- `npm test`
- `git diff --check`

## 03:36 - 实现 execution policy 分层

- 新增 `[run_defaults]` 和 `[execution_policy]` 配置 schema 与 `docs/contracts/execution-policy.md`；run defaults 按层覆盖，execution policy 数字上限取更严格值，`require_user_gate` 取 OR，`allow_auto_dispatch` 取 AND。
- `flow run` 在 packet 中固化 `status.json.resolved_execution_policy` 和 `status.json.config_provenance`，并把默认值按 policy cap 收紧；policy 要求 user gate 时会要求 `--decide current`。
- dispatch/retry 读取 packet 内 resolved policy，执行 adapter timeout cap、fanout cap、`allow_auto_dispatch=false` 禁止自动 agent 调用，以及 `max_retry_attempts` 重试上限。
- Studio packet summary/API 和 UI overview 展示 resolved execution policy compact summary。

### 验证

- `npm run build && node --test dist-node/tests-node/config-layering.test.js dist-node/tests-node/flow-run.test.js dist-node/tests-node/flow-dispatch.test.js dist-node/tests-node/studio.test.js dist-node/tests-node/studio-ui.test.js dist-node/tests-node/core-contracts.test.js`
- `npm test`
- `git diff --check`

## 03:52 - 合并 model alias 和 capability profile 注册验证

- 新增 `[model_aliases.<alias>]` 配置 schema，`agents add` 先走 adapter discovery，再用显式 alias fallback；unsupported discovery adapter 也会优先解析匹配 alias，最后才把原输入当 canonical model。
- 新增 `[capability_profiles.<profile-id>]` 和 `[capability_profile_preferences.<profile-id>]`，强制 model aliases / preferences 属于 user 层，capability profiles 属于 project 层；explicit config 仍可用于 CI / 临时覆盖。
- review/release policy 支持引用已声明 capability profile，`flow run` 会把 profile 解析为 concrete reviewer ids；无 preference 且唯一匹配时写入 `profile_resolution_warnings`，无匹配或多候选 fail fast。
- 收紧新配置段解析：未知字段、invalid stage、非字符串列表、缺失 model/adapter 等都返回可诊断错误；合同文档同步 model alias、profile preference、packet warning 和 release summary 行为。

### 验证

- `npm run build && node --test dist-node/tests-node/cli-surface.test.js dist-node/tests-node/agent-registration.test.js dist-node/tests-node/config-layering.test.js dist-node/tests-node/flow-run.test.js dist-node/tests-node/release-check.test.js`
- `npm test`
- `git diff --check`

## 03:57 - 完成 P2 文档收尾校准

- `README.md` 更新到 P2 完成后的事实：当前验证基线为 287 tests，下一步改为 P3 Verify Stage Type 与运行时性能 / Fanout 调度。
- README 同步 agent 注册默认 id、model alias、注册前 readiness probe、`--skip-verify` 风险、用户/项目/显式配置分层，以及 review/release policy、run defaults 和 execution policy 示例。
- 根目录 `index.html` 更新首页状态、测试数、agent 注册示例、CLI 手册和路线图：P2 标为 done，P3-P5 标为 next。
- `plan.md` 将 P2 状态标为已完成，并把当前下一步改为 P3。

### 验证

- `npm test`
- `git diff --check`

## 04:01 - 定义 verify stage contract

- 新增 `docs/contracts/verify-stage.md`，固定 `verify` 的语义边界：只产生命令、测试、构建、smoke、回归验证证据，不写 release verdict，也不接受/拒绝 review findings。
- 定义 canonical artifact：首个 verify 写 `verification.md` / artifact id `verification`，重复 verify 写 `verification_2.md`、`verification_3.md` 以及 node-aware artifact id。
- 同步 workflow、packet layout、artifact manifest、prompt assembly、stage dispatch 和 status JSON 合同；P3.2 只支持 single-agent verify，multi-agent verify 先 fail fast 并指向 contract。
- 明确 `verify` capability 为显式 opt-in，旧 packet 保持可读，后续 workflow defaults 和 capability profile schema 需要识别 `verify` stage。

### 验证

- `npm run build && node --test dist-node/tests-node/core-contracts.test.js`
- `git diff --check`

## 04:16 - 实现 verify core/runtime stage

- `verify` 进入 core stage schema，workflow/status validation 支持 `verify` 和 `verify_2` 这类重复 stage node，同时保持 `decide` 只能最后且最多一次。
- `flow run` 支持 `--verify` 和 `[workflow_defaults.<workflow-id>].verify`，config capability profile stage schema 也接受 `verify`；旧 packet 仍不需要 legacy `status.verify` 字段。
- runtime dispatch 支持 single-agent verify，写入 `verification.md` / `verification_2.md`、artifacts manifest、events、stage timing 和 agent timing；未声明 `verify` capability 的 agent 会 fail fast。
- multi-agent verify 在 P3.2 明确 fail fast，不写 aggregate artifact，并提示 `docs/contracts/verify-stage.md`；README、index、init 模板和 config contract 同步 `--verify` / verify defaults 文案。
- Review 修复 multi-agent verify 错误文案未指向 contract、CLI/config 文档未同步 `--verify` 的问题。

### 验证

- `npm run build && node --test dist-node/tests-node/core-contracts.test.js dist-node/tests-node/flow-run.test.js dist-node/tests-node/flow-dispatch.test.js dist-node/tests-node/packet-validate.test.js dist-node/tests-node/config-layering.test.js`
- `npm test`
- `git diff --check`

## 04:23 - 暴露 verified-delivery workflow 和 Studio verify 可见面

- 新增内置 `verified-delivery` workflow，采用 `plan -> execute -> verify -> review -> decide`，并把 `verification.md` 纳入 packet artifacts 和质量门。
- Studio packet browser / UI 测试锁定 verify stage timing、`verification.md` artifact list、artifact preview，以及 catalog 中带 verify 的 workflow 展示。
- README、根目录 `index.html` 和 `docs/contracts/verify-stage.md` 同步 `verified-delivery` 可见面；命令速查改用 `verified-delivery --verify <id>`，避免暗示 `bug-fix` 支持 `--verify`。
- Review 修复命令速查中 `bug-fix [--verify]` 示例不准确、Studio UI fixture workflow 名称和 verify 阶段不一致的问题。

### 验证

- `npm run build && node --test dist-node/tests-node/workflow-registry.test.js dist-node/tests-node/studio.test.js dist-node/tests-node/studio-ui.test.js`
- `npm test`
- `git diff --check`

## 05:00 - 迁移 flow dispatch async API

- 新增 `withRunMutationLockAsync`，让 async mutation 在 awaited success / failure 后通过 `finally` 释放 run lock；同步 `docs/contracts/run-lock.md` 的 lock 生命周期说明。
- `dispatchFlowStage`、`retryFlowStage`、`resumeFlow`、`dispatchRemainingStages` 和 `dispatchOneStage` 改为 awaitable，为后续 fanout 并发调度准备 runtime API。
- `flow dispatch`、`flow retry`、`flow resume` CLI command 改为 async 并 await runtime dispatch；顶层 CLI 已通过 `await main()` 等待返回。
- `tests-node/flow-dispatch.test.ts` 补充 async lock release 覆盖，并继续校验现有 dispatch / retry / review fanout 行为。
- Review 校准 CLI async 调用链和 lock release failure path，未发现需要额外代码修复的问题。

### 验证

- `npm run build && node --test dist-node/tests-node/flow-dispatch.test.js`
- `npm test`
- `git diff --check`

## 05:12 - 并行 fanout review 和 verify agents

- 新增 async `dispatchFanoutAgents` helper：fanout 阶段先写所有 agent prompt，再并行调用 `runAgentCallAsync`，并等待全部 agent settle 后统一处理成功、失败和复用结果。
- review fanout 改为并发执行；成功 raw review 统一刷新进 `findings.md`，失败 reviewer 追加 needs-decision note，同时保留已成功 reviewer 的 partial evidence。
- verify fanout 支持多 verifier：每个 verifier 写 `outputs/<node-id>/<agent>.md`，AgentMesh 生成 canonical `verification.md` / `verification_<n>.md` 聚合摘要；失败 verifier 会进入 partial aggregate，retry 只重跑失败或缺失 slot。
- 同步 `docs/contracts/stage-dispatch.md`、`docs/contracts/status-json.md` 和 `docs/contracts/verify-stage.md`，把 verify fanout 从 fail-fast 边界更新为 per-agent evidence + aggregate contract。
- Review 修复 verify aggregate artifact event 中硬编码 `stage_type` 的可维护性问题，改为从 stage node 读取。

### 验证

- `npm run build && node --test dist-node/tests-node/flow-dispatch.test.js dist-node/tests-node/review-artifacts.test.js dist-node/tests-node/core-contracts.test.js`
- `npm test`
- `git diff --check`

## 05:19 - 并行 plan / decide candidate fanout

- plan / decide fanout candidate 阶段复用 async fanout helper：先写所有 candidate prompt，再并行启动 assigned agents，全部 settle 后才进入 synthesis controller。
- synthesis prompt 继续按 assigned-agent 顺序列出候选输出；candidate 失败时不生成 synthesis prompt / canonical artifact，并保留已成功 candidate output 供 retry 复用。
- synthesis 失败后 retry 只重跑 synthesis controller，已完成 candidate 输出不会被覆盖或重复执行。
- release-check decide fanout 仍只从 canonical `decision.md` 更新 `release_verdict`；raw candidate 中的 `Verdict: not_ready` 不会覆盖 synthesized `Verdict: ready`。
- `docs/contracts/stage-dispatch.md` 同步 plan / decide candidate 并发事实；review 未发现需要额外代码修复的问题。

### 验证

- `npm run build && node --test dist-node/tests-node/flow-dispatch.test.js dist-node/tests-node/release-check-flow.test.js dist-node/tests-node/release-check.test.js dist-node/tests-node/core-contracts.test.js`
- `npm test`
- `git diff --check`

## 05:31 - 记录 fanout agent events/logs

- fanout dispatch 为每个 agent 记录 `stage.agent_started`、`stage.agent_completed`、`stage.agent_failed` 和 `stage.agent_reused`，payload 包含 stage、agent、output path、exit code、timeout 状态和 duration。
- async adapter invocation 返回 stdout/stderr/timedOut；fanout worker stdout/stderr 非空时分别写入 `logs/<stage>/<agent>.stdout.log` 和 `.stderr.log`，失败 reviewer 的 stderr 也保留给排障。
- `execution_policy.max_fanout_concurrency` 从“超过即拒绝”改为 fanout worker 并发窗口；超过窗口的 assigned agents 分批执行，不新增内部硬编码默认并发数。
- 同步 `docs/contracts/stage-dispatch.md`、`docs/contracts/execution-policy.md` 和 `docs/contracts/adapter-invocation.md` 的事件、日志和限流语义。
- Review 未发现需要额外修复的问题。

### 验证

- `npm run build && node --test dist-node/tests-node/core-contracts.test.js dist-node/tests-node/flow-dispatch.test.js`
- `npm test`
- `git diff --check`

## 05:38 - 完成 P3 verify/fanout 收尾校准

- README 更新当前事实：P3 已完成、验证基线为 310 tests，`verify` 是正式 stage，runtime timing、MCP 连接复用、async adapter invocation、review/verify/plan/decide fanout 和 per-agent events/logs 已落地。
- 根目录 `index.html` 更新首页状态、测试数、workflow 示例、Studio evidence 文案和路线图，把当前下一步切到 P4 Studio 桌面版与双渠道分发。
- `plan.md` 将 P3 标为已完成，补齐 P3.11 的 targeted/full verification、review 和 changelog 证据。
- Review 校准 README/index/plan diff 后未发现需要额外代码修复的问题；仅补充首页 Run packet 文案明确包含 verify。

### 验证

- `npm run build && node --test dist-node/tests-node/adapter-invocation.test.js dist-node/tests-node/flow-dispatch.test.js dist-node/tests-node/release-check.test.js dist-node/tests-node/release-check-flow.test.js`
- `npm test`
- `git diff --check`

## 05:42 - 定义 Studio App Server 边界

- 新增 `docs/contracts/app-server.md`，固定 packaged App Server 的职责：服务 Studio UI、动态 `127.0.0.1` 端口、per-launch token、health check、graceful shutdown 和 app-bundled runtime mutation。
- `docs/decisions/studio-shell.md` 补充 App Server Boundary，明确 App-originated actions 使用 app-bundled runtime，terminal 和 entry-agent skill 继续按 PATH-visible `agentmesh` 解析。
- 明确 App Server mutation 必须走 CLI/runtime commands 并复用 filesystem run-lock；unknown lock schema 视为 active/blocking，unsupported newer packet/config schema 必须 fail-fast 或 read-only。
- `tests-node/core-contracts.test.ts` 纳入 App Server contract；`tests-node/package-structure.test.ts` 增加 future desktop/app-server guardrail，避免绕过 runtime lock 直接写 packet files。
- Review 未发现需要额外修复的问题。

### 验证

- `npm run build && node --test dist-node/tests-node/core-contracts.test.js dist-node/tests-node/package-structure.test.js`
- `git diff --check`

## 05:46 - 选择 Studio desktop 分发栈

- 新增 `docs/decisions/studio-distribution.md`，选择 Tauri 2 作为首个 macOS desktop host 和 distribution stack。
- 决策文档固定首发目标为 macOS DMG、Developer ID signing、notarization、app-bundled runtime/App Server/CLI sidecar，并明确 Windows/Linux 需要独立决策。
- 更新策略选择 `tauri-plugin-updater`、signed update artifacts、GitHub Releases static JSON metadata、stable/beta channel policy 和 manual rollback。
- 记录 Electron fallback 条件：确实需要 Chromium API、`node-pty` / native Node modules，或 Tauri sidecar packaging 经验证不可接受。
- `tests-node/package-structure.test.ts` 增加 decision guardrail；P4.2 review 未发现需修复问题。

### 验证

- `npm run build && node --test dist-node/tests-node/package-structure.test.js`
- `git diff --check`

## 05:53 - 增加 Studio desktop host harness

- 新增 `apps/studio-desktop/src/options.ts` 和 `host.ts`，支持默认动态端口、显式 workspace/port/runtime CLI、per-launch token、tokenized WebView URL 和 actionable port failure。
- Studio server 增加可选 `authToken`，desktop 模式下 `/api/*` 和页面请求需要 query/header/cookie token；root 页面会设置同源 token cookie，browser/WebView 可继续加载静态资源。
- Review 后修复 malformed `agentmesh_studio_token` cookie 会触发 500 的问题；畸形 token 现在按未授权返回 401，并补充回归断言。
- App-originated mutation 通过 `runtimeCliPath` 调用 app-bundled runtime/CLI entrypoint，不查 PATH；desktop smoke 覆盖读取真实临时 `.agentmesh/runs` 和 fake bundled CLI mutation。
- 新增 `apps/studio-desktop/src-tauri/tauri.conf.json` 作为 Tauri 2/macOS DMG/updater sidecar 配置起点，并在 `package.json` 增加 `studio-desktop` dev script。
- `tests-node/studio-desktop-options.test.ts` 和 `tests-node/package-structure.test.ts` 覆盖 desktop parser、App Server token、mutation boundary、Tauri config 存在和 package script。

### 验证

- `npm run build && node --test dist-node/tests-node/studio-desktop-options.test.js dist-node/tests-node/package-structure.test.js`
- `npm test`
- `git diff --check`

## 06:08 - 配置 Studio macOS App 分发 smoke

- Tauri desktop config 补齐 `AgentMesh` product name、`dev.agentmesh.studio` bundle id、DMG target、icon source、macOS entitlements 和 `createUpdaterArtifacts`。
- 新增 `apps/studio-desktop/distribution/macos.json`，固定 app-managed runtime、stable/beta update endpoint、app archive/signature/latest metadata 产物和 npm/global CLI 分发隔离。
- 新增 stable/beta `latest.*.darwin-aarch64.example.json`，作为 GitHub Releases static JSON metadata 起点。
- 新增 `apps/studio-desktop/src/distribution-smoke.ts` 和 `package.json` 的 `studio-desktop:package:dev`、`studio-desktop:package:signed`、`studio-desktop:update:metadata` 脚本；signed 模式在缺少 Developer ID/notarization/updater private key 或 updater pubkey 未替换时 fail fast，dry-run 只报告 gate。
- 新增 `docs/distribution/studio-macos.md`，记录 unsigned smoke、signed/notarized build、Tauri updater metadata、secret/env-only 规则和 app update / npm CLI channel boundary。
- Review 后收窄 dev/metadata smoke 输出：无证书开发环境不再显示 signed-only warning，signed dry-run 仍列出缺失 env 和 updater pubkey。

### 验证

- `npm run build && node --test dist-node/tests-node/studio-desktop-distribution.test.js`
- `npm run studio-desktop:package:dev`
- `npm run studio-desktop:update:metadata`
- `npm run build && node dist-node/apps/studio-desktop/src/distribution-smoke.js --mode signed --dry-run`
- `npm test`
- `git diff --check`

## 06:16 - 增加 Studio 双渠道共存 guardrails

- 新增 `tests-node/studio-distribution-coexistence.test.ts`，模拟 app-bundled CLI 与 PATH-visible `agentmesh` 同时存在，确认 desktop App-originated mutation 始终使用 `runtimeCliPath`，不走 PATH sentinel。
- 同一测试覆盖 terminal / entry-agent 风格调用仍解析 PATH-visible `agentmesh`。
- 增加 app CLI 与 npm CLI 共享同一 run 的 filesystem run-lock smoke：npm 侧持锁时，desktop app mutation 返回 409 且不写穿 active lease。
- Review 发现 unsupported newer packet mutation 不是预期 schema fail-fast；已在 `packages/runtime/src/packet/io.ts` 为 `loadStatus()` 增加 `status.json.schema_version` gate，并补回归断言 status/artifact 不被覆盖。
- 新增 `docs/distribution/studio-coexistence-smoke.md`，记录 app-bundled/PATH/run-lock/schema/Install Command Line Tool/web Studio/CLI Studio/desktop Studio smoke evidence；`tests-node/package-structure.test.ts` 固定文档证据。

### 验证

- `npm run build && node --test dist-node/tests-node/studio-distribution-coexistence.test.js dist-node/tests-node/package-structure.test.js dist-node/tests-node/packet-io.test.js dist-node/tests-node/packet-validate.test.js`
- `npm run build && node --test dist-node/tests-node/studio-cli.test.js dist-node/tests-node/studio-ui.test.js dist-node/tests-node/studio-desktop-options.test.js dist-node/tests-node/studio-distribution-coexistence.test.js dist-node/tests-node/package-structure.test.js`
- `npm test`
- `git diff --check`

## 06:22 - 同步 Studio Desktop 分发文档状态

- README 更新当前状态为 P4 完成、验证基线为 326 tests，并新增 desktop host harness、macOS distribution smoke、signed/notarized DMG 证书环境限制、app-bundled runtime 与 PATH-visible `agentmesh` 边界。
- 根目录 `index.html` 更新首页状态、测试数、安装入口、Studio 说明、能力边界和路线图：P4 标记完成，下一步切到 P5 Public Surface。
- `plan.md` 将 P4 标记为完成，并明确本仓库已完成 desktop host / distribution config / smoke evidence；真实 signed/notarized DMG 仍由具备 Tauri/Rust、Developer ID 和 notarization 凭证的 packaging host 执行。
- Review 后修复 README/index 仍停留在 P3/P4 next/310 tests 的旧状态残留。

### 验证

- `npm test`
- `git diff --check`

## 14:36 - 让默认 agents 支持 verify capability

- `agents add` 的默认 capabilities 改为从核心 `STAGE_TYPES` 派生，新注册 agent 默认包含 `plan`、`execute`、`verify`、`review`、`decide`。
- 内置 adapter metadata 同步支持 `verify` stage 和 `verifier` role；显式 `--capability` 列表仍保持精确覆盖，不会自动给旧列表注入 `verify`。
- `README.md`、`agentmesh-skill/SKILL.md`、`docs/contracts/agent-registration.md`、`docs/contracts/verify-stage.md` 和 `examples/agentmesh.toml` 同步迁移说明与示例。
- `tests-node/agent-registration.test.ts`、`tests-node/cli-surface.test.ts`、`tests-node/adapter-registry.test.ts`、`tests-node/flow-dispatch.test.ts` 覆盖默认能力、显式覆盖、adapter metadata 和旧四项能力列表拒绝 verify。
- AgentMesh review-gate `verify-default-capability-20260516142519` 已由 Claude Opus 4.7 和 Gemini 3.1 Pro Preview review，并由 current decide 汇总 accepted findings。
- 本机用户级 `/Users/zz/.config/agentmesh/config.toml` 中 `claude`、`codex`、`gemini`、`mimo`、`opencode` 已同步加入 `verify` capability；该本机配置不属于仓库提交内容。

### 验证

- `git diff --check`
- `npm test`（336 tests passed）

## 18:37 - 完善 Studio 工作台导航、手册和产物预览

- Studio 左侧改为固定工作台：`AgentMesh` 品牌居中展示，配置、设置、手册作为主菜单，运行搜索和刷新保持在运行列表顶部且不随右侧内容滚动。
- 运行详情重排为概述、操作、发布 / 评审、产物、事件 tab；workflow 流程图移动到概述内，节点和产物流程支持横向滚动，不再撑破页面宽度。
- 配置页的 agents/workflows/MCP 和 workflows 的内置/用户配置改为 tab 切换；手册补充架构设计、核心概念、Packet、使用教程，并在架构设计中加入 Studio / App Server / CLI / Packet / Runtime 的流程图。
- 产物页按时间倒序展示流程节点，进入产物 tab 时自动选中第一个产物并加载预览；预览请求增加序号保护，避免默认预览慢返回覆盖用户手动选择，失败时显示可读错误。
- `tests-node/studio-ui.test.ts` 补充导航固定、右侧滚动、横向滚动、手册架构图、产物默认选择、手动选择不被覆盖和预览错误展示的回归断言。
- `plan.md` 切换为 Workflow Stage Semantics / Plan A 的唯一执行事实源，并将旧计划归档到 `docs/archive/plan-before-plan-a-2026-05-16.md`。

### 验证

- `git diff --check`
- `npm test`（338 tests passed）

## 18:54 - 收敛 Workflow Stage Semantics 计划契约

- `plan.md` 按 Plan A 作为唯一执行事实源继续收敛，并吸收 Claude Opus 4.7 对当前计划的 Must / 关键 Should review findings。
- 明确 direct workflow run 的 CLI role flags 按 stage type 展开到所有 repeated nodes；需要区分 `decide` / `decide_2` 等 repeated node 时必须使用 preset 的 node-id keyed `[stage_assignments]`。
- 明确 bare `agentmesh run <id>` 只解析 preset namespace，workflow 直跑必须显式 `--workflow`；preset/workflow 同名时 bare run 仍按 preset 解析。
- 固化 packet / runtime 契约：v1 packet 统一 hard error、`compatible_packet_schema_versions` 精确为 `[2]`、`stage_attempts` 同时记录 `lane_attempt` 和 per-agent `attempt`、multi-agent `plan` / `decide` 才创建 synthesis lane。
- 补齐 default/fallback 规则：`execute` default primary agent 最终必须刚好 1 个，terminal policy 会清掉继承的 `max_fallback_agents`，4+ `plan` / `decide` soft warning 必须在 CLI/doctor/run validation 和 Studio 可见。

### 验证

- `git diff --check -- plan.md docs/archive/plan-before-plan-a-2026-05-16.md`
- `rg` 扫描确认无 `rejected or reported`、`"attempt": 2`、兼容版本只要求包含 2、`review accepted findings 清零` 等旧歧义残留。
- `grep` 确认 `plan.md` 无 tab 缩进。

## 19:18 - 收敛本地 AgentMesh 配置初始化

- `agentmesh init` 改为把 `.agentmesh/` 写入项目 `.gitignore`，并迁移旧的 `.agentmesh/runs/` ignore 行，避免新旧规则并存。
- README、AgentMesh Skill、配置分层 / corrections / project spec contract、roadmap 和首页统一为“每人 clone/pull 后维护 checkout-local 配置”的口径，并补充老 checkout 迁移说明。
- 两方 review-gate `local-config-review-20260516-duo` 已由 Claude Opus 4.7 和 Cursor review，current decide 接受补测试、补迁移说明和术语收口项；DS/opencode 因本机非交互 review 失败未纳入最终 duo review。

### 验证

- `npm run build && node --test --test-name-pattern "init keeps agentmesh gitignore entries" dist-node/tests-node/cli-surface.test.js`
- `git diff --cached --check`
- staged-index clean HOME snapshot `npm test`（339 tests passed）

## 19:26 - 增加 Cursor Agent adapter

- 新增内置 `cursor-agent` adapter，alias 为 `cursor`，默认 headless 参数为 `--print --trust`，可用 `composer-2-fast` 注册 fast Cursor reviewer。
- Cursor 模型 fixture 增加 `auto`、`composer-2`、`composer-2-fast`、`grok-4.3`、`kimi-k2.5`，并记录 `cursor-agent models` discovery hook；`compose2-fast` / `composer2fast` 这类 shorthand 只在 Cursor adapter 下解析。
- `agents add` 对 Cursor 明确把 `reasoning_effort` 保存为 `none`；用户显式传非 `none` 时输出 warning，避免配置看起来生效但实际 CLI 无对应参数。
- Cursor invocation 增加空 prompt guard，并继续使用 positional prompt；README 同步 `--trust` 使用边界和 Cursor 不支持 `reasoning_effort` 的说明。
- Claude Opus 4.7 和 Cursor review 未发现 Must Fix；已吸收 reasoning effort、composer alias scope、README trust 说明、直接 `adapterShortName("cursor-agent")`、模型边界和空 prompt 回归测试等 Should Fix。
- 本机用户级 `/Users/zz/.config/agentmesh/config.toml` 已注册 `cursor` agent，模型为 `composer-2-fast`；该配置不属于仓库提交内容。

### 验证

- `npm run build && node --test dist-node/tests-node/agent-registration.test.js dist-node/tests-node/adapter-invocation.test.js`
- `node --test --test-name-pattern 'init, agents, and adapters' dist-node/tests-node/cli-surface.test.js`
- `git diff --check`
- `node dist-node/packages/cli/src/cli.js doctor --agent cursor --skip-auth-probe --json`
- `npm test` 当前剩余 11 个 `flow-dispatch.test.js` 失败，错误集中为 `dist-node/.../adapters.js` 或 `dist-node/.../cli.js` module-not-found，需单独处理 full-suite harness / dist-node 可用性问题。

## 19:40 - 将 token budget 优化并入 Plan A

- `plan.md` 新增 `P0. Token Budget 与 Prompt 成本护栏` 前置阶段，把 context / prompt bytes 观测、context policy 默认建议、scoped diff 预算边界拆成 P0.1-P0.3。
- 根据 Claude Opus 4.7、Cursor 和 Gemini review 结论，将当前下一步从 `P1.1` 调整为 `P0.1 增加 context / prompt 字节观测`，先观测再做硬限制。
- 在 P4.2 加入 synthesis prompt 去重要求，在 P5.1 加入 `findings.md` raw reviews 和 repeated-stage prior raw reviews 的摘要 / 截断边界。
- 整体验证和风险章节补充 token budget 验收、byte/token 代理误差、截断 blindness、fanout/synthesis/findings 重复携带 context 等风险。

### 验证

- `git diff --check -- plan.md`
