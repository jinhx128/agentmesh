# 2026-06-11

## 10:35 - Studio 全局 workspace 历史可见性

- 新增用户级 workspace registry，使用 `~/.config/agentmesh/workspaces.json` 记录已登记 workspace，并用 realpath 后的路径生成稳定 `workspace_id`。
- 新增 `agentmesh workspaces add/list/enable/disable`，支持导入旧 workspace；`agentmesh call` 产生记录时会 best-effort 登记当前 workspace。
- Studio Runs 与 Calls 默认聚合当前 workspace 和 registry 中 enabled workspace 的历史记录，并在列表、详情、artifact preview、call adoption 链路中贯穿 `workspace_id`。
- Studio 前端列表展示 workspace label，并用 `workspace.id + run_id/call_id` 做选中 key，避免跨 workspace 同名记录串联。
- 详情、artifact preview 和 adoption API 只允许通过当前 workspace 或 registry 中 enabled workspace 解析记录，拒绝直接按任意 path 读取本机文件。

### 验证

- `npm run build:node`
- `node --test dist-node/tests-node/workspace-registry.test.js dist-node/tests-node/workspaces-cli.test.js dist-node/tests-node/call-history.test.js dist-node/tests-node/studio.test.js dist-node/tests-node/studio-ui.test.js`
- `npm run build:studio-frontend`
- `git diff --check`
- 本地 Studio smoke：`/api/runs` 与 `/api/calls` 均返回 `agentmesh`、`zhuanzhuan` 两个 workspace，diagnostics 为空。

## 16:01 - 0.1.5 公共发布准备

- 将根包调整为公共 npm 包 `@jinhx128/agentmesh@0.1.5`，同步 workspace package、Tauri/Cargo、SDK/runtime 默认版本和分发文档中的版本号。
- 修复 CLI Studio 浏览器启动后的 `/api/bootstrap` 认证问题，允许 CLI Studio bootstrap 返回未认证的初始化信息，同时保留 Desktop Studio 的 cookie token mutation 边界。
- 新增 GitHub Release tarball 命名归一化，`npm pack` 产生的 scoped tarball 会在 Release 资产中重命名为 `agentmesh-<version>.tgz`，并保持 npm registry 包名为 `@jinhx128/agentmesh`。
- 新增 `npm run publish:npm` 和 `npm run publish:github` 两个发布入口；npm 发布脚本会检查干净工作区、npm 登录、scope 权限、重复版本、build 和安装 smoke，GitHub 发布脚本复用现有 Release 资产上传与验证流程。
- 将 Desktop updater metadata、Tauri endpoint 和分发配置统一到 `jinhx128/agentmesh`，避免 Desktop update channel 与 CLI/GitHub Release 仓库不一致。
- 同步 README、`docs/distribution/*` 和测试断言，覆盖公共 npm 安装、GitHub Release 文件名、发布脚本、Studio auth 和 updater repo 行为。

### 验证

- `bash -n scripts/publish-npm.sh scripts/publish-github-release.sh`
- `npm run publish:npm -- --help`
- `npm run publish:github -- --help`
- `npm run build && node --test --test-name-pattern "release publish wrappers|update metadata targets" dist-node/tests-node/package-structure.test.js dist-node/tests-node/studio-desktop-distribution.test.js`
- `npm test`
- `git diff --check`
