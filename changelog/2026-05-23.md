# 2026-05-23

## 22:26 - 增加 AgentMesh 版本检查和更新入口

- CLI 新增 `agentmesh --version`、`agentmesh version [--json]`、`agentmesh update check [--json]` 和 `agentmesh update install --target <cli|desktop>`；更新检查读取 GitHub Release 最新资产，CLI 目标给出 npm tarball 安装命令，Desktop 目标返回需要手动下载替换的 DMG。
- App Server 新增 `/api/v1/update/check`，Studio 设置 / 关于页展示当前版本、最新版本、CLI 更新命令和 Desktop DMG 下载地址；兼容性诊断加载失败不会影响版本检查的独立错误展示。
- README 同步 CLI / Desktop 更新方式，明确当前 Desktop 自动更新尚未启用，更新 Desktop Studio 需要下载新 DMG 并完整替换 `AgentMesh.app`。

### 验证

- `npm run build:node && node --test --test-name-pattern "version commands report|update check reports|update install dry-run" dist-node/tests-node/update.test.js`：4/4 通过
- `npm run build:node && node --test --test-name-pattern "Safe actions, settings, integrations|Studio API clients keep App Server endpoint contracts|Studio server exposes AgentMesh update diagnostics" dist-node/tests-node/studio-ui.test.js dist-node/tests-node/studio.test.js`：3/3 通过
- `npm run build && node --test dist-node/tests-node/*.test.js`：484/484 通过

## 21:35 - 修复 OpenCode 创建 Agent 探测并提升到 0.1.2

- Studio 创建 Agent 的模型列表接口复用通用 provider CLI resolver；OpenCode 即使安装在 `~/.opencode/bin/opencode` 且不在 PATH 中，也能被模型探测使用，不再被工具下拉置灰。
- 版本统一提升到 `0.1.2`：同步 workspace 包、package lock、Tauri/Cargo 版本、运行时兼容版本、MCP client version、Desktop updater 示例元数据、README、发行说明和测试期望。
- 重新打包并安装 Desktop Studio，安装态关于页显示运行时 `0.1.2`；创建 Agent 下拉已确认 OpenCode CLI 可选并能加载模型。

### 验证

- `npm run build:node && node --test --test-name-pattern "Studio agent model endpoint discovers provider CLIs outside PATH|model discovery can hydrate supported adapters from their CLI output|skill output declares AgentMesh protocol version metadata|desktop command-line tool install" dist-node/tests-node/studio.test.js dist-node/tests-node/agent-registration.test.js dist-node/tests-node/readiness.test.js dist-node/tests-node/studio-desktop-options.test.js`：5/5 通过
- `node dist-node/packages/cli/src/cli.js cli detect --json`：本机 5 个支持 provider CLI 均可解析，OpenCode 来源为 `well_known`
- `npm test`：479/479 通过
- `npm run studio-desktop:package:dev`：通过，updater 示例元数据版本为 `0.1.2`
- `cargo tauri build --config apps/studio-desktop/src-tauri/tauri.conf.json --bundles dmg --debug`：生成 `AgentMesh_0.1.2_aarch64.dmg`
- `shasum -a 256 apps/studio-desktop/src-tauri/target/debug/bundle/dmg/AgentMesh_0.1.2_aarch64.dmg`：`395f9809c7b0d13ec966ef412b878788f4be0539ad4452241518a4e7bdcfb541`
- `hdiutil verify apps/studio-desktop/src-tauri/target/debug/bundle/dmg/AgentMesh_0.1.2_aarch64.dmg`：VALID
- 从 DMG 覆盖安装 `/Applications/AgentMesh.app`，旧 app 备份到 `/Applications/AgentMesh.app.backup-20260523-213924`；安装态 UI 已确认关于页 `0.1.2` 和 OpenCode 创建 Agent 可选

## 21:11 - 增加 CLI 检测页和命令

- 新增 runtime 共享 `detectSupportedProviderClis`，统一维护 Codex、Claude Code、Cursor Agent、Antigravity 和 OpenCode 这 5 个 provider CLI 的检测范围。
- Studio 设置 / 环境新增“CLI 检测”页签，展示每个支持 CLI 的命令、来源、路径、版本和诊断信息；原“命令行工具”仍只负责 AgentMesh wrapper 安装。
- 新增 `agentmesh cli detect [--json]`，CLI 与 Studio 共用同一套 resolver，不要求所有 provider CLI 都安装。
- 同步 AgentMesh Skill 和 README，引导入口 agent 先用 `agentmesh cli detect --json` 查看本机支持工具安装状态。

### 验证

- `npm run build:node && node --test --test-name-pattern "cli detect reports supported provider CLIs|desktop integrations expose no-global CLI status|React app renders|skill output declares AgentMesh protocol version metadata" dist-node/tests-node/cli-surface.test.js dist-node/tests-node/studio-desktop-options.test.js dist-node/tests-node/studio-ui.test.js dist-node/tests-node/readiness.test.js`：4/4 通过
- `node dist-node/packages/cli/src/cli.js cli detect --json`：本机 5 个支持 provider CLI 均可解析，OpenCode 来源为 `well_known`
- `npm test`：478/478 通过
- `npm run studio-desktop:package:dev`：通过，`ok=true`、`issues=[]`
- `cargo tauri build --config apps/studio-desktop/src-tauri/tauri.conf.json --bundles dmg --debug`：生成 `AgentMesh_0.1.1_aarch64.dmg`
- 从 DMG 覆盖安装 `/Applications/AgentMesh.app`，旧 app 备份到 `/Applications/AgentMesh.app.backup-20260523-211418`；安装态 UI 已确认“CLI 检测”页展示 5/5 和 OpenCode 常见路径来源

## 20:54 - 通用化 provider CLI 发现逻辑

- 新增 runtime 共享 provider tool resolver，实际 agent 调用和 `agentmesh doctor` 都会先查 PATH，再查常见用户安装目录，最后用登录 shell 兜底解析。
- 配置仍保持 `command = "opencode"` 这类可移植命令名，不把本机绝对路径写入用户配置或仓库。
- 补充回归测试：当 PATH 为空、OpenCode 安装在 `~/.opencode/bin/opencode` 时，`agentmesh call` 仍能发现并调用该 CLI。

### 验证

- `npm run build:node && node --test --test-name-pattern "agent calls discover provider CLIs installed outside PATH" dist-node/tests-node/adapter-invocation.test.js`：先失败于 `spawnSync opencode ENOENT`，修复后通过
- `npm run build:node && node --test dist-node/tests-node/adapter-invocation.test.js dist-node/tests-node/readiness.test.js`：54/54 通过
- `agentmesh doctor --agent a-19bef3c3 --json`：`ok=true`，`provider_tool_source=well_known`
- `npm test`：477/477 通过

## 20:33 - 收口 Studio 首屏授权 review follow-up

- App Server 首屏 HTML 增加 `Referrer-Policy: no-referrer`，避免 launch fallback token 通过资源请求 Referer 外带。
- Studio Desktop 回归测试补充 `/?token=...` 种 cookie 后继续加载 `/assets/*` 的断言，并确认 `/assets/*?token=...` 不会放宽授权。
- 同步 App Server 合同、README 安装说明和 GitHub Release notes 模板，明确 query token 只限首屏 `/`、Desktop Studio 需要同版本 DMG 完整替换、CLI tarball 是独立渠道。

### 验证

- `node --check scripts/github-release.mjs`
- `node scripts/github-release.mjs --help`
- `npm run build && node --test dist-node/tests-node/studio-desktop-options.test.js dist-node/tests-node/studio.test.js`：47/47 通过
- `npm test`：476/476 通过
- `git grep` 扫描本机用户目录、临时截图目录和截图缓存路径：无命中
- `git diff --check`

## 20:16 - 修复 Desktop Studio 首屏 unauthorized 并脱敏本机路径

- Desktop Studio 启动时仍优先准备 `agentmesh_studio_token` cookie，但 cookie 写入失败不再阻断导航；Tauri WebView 会在首屏 URL 上追加一次性 `token` 作为兜底。
- App Server 只允许查询参数 token 用于加载首屏 `/`，不会放宽 `/api/*`；React bootstrap 会把启动 token 转成 Authorization header，并从地址栏移除。
- 脱敏旧 review/changelog 文档中的本机绝对路径，改为 repo-relative path 或 `~/.config/...`。

### 验证

- `git grep` 扫描本机用户目录、临时截图目录和截图缓存路径：无命中
- `npm run build && node --test dist-node/tests-node/studio.test.js dist-node/tests-node/studio-desktop-options.test.js dist-node/tests-node/studio-desktop-distribution.test.js`
- `npm test`：476/476 通过
- `npm run studio-desktop:sidecar:bundle`
- `cargo check --manifest-path apps/studio-desktop/src-tauri/Cargo.toml`
- `npm run studio-desktop:package:dev`
- `cargo tauri build --config apps/studio-desktop/src-tauri/tauri.conf.json --bundles dmg --debug`
- `git diff --check`

## 20:02 - 固化 GitHub Release 发布流程

- 新增 `scripts/github-release.mjs`，把 Release 资产准备、GitHub Release 创建/补传和远端资产校验收口到同一条可重跑流程。
- 新增 `npm run release:assets`、`npm run release:github`、`npm run release:github:verify`，避免只推送 tag 却没有创建 GitHub Release。
- 新增 `docs/distribution/github-release.md`，明确发布完成定义、标准流程、漏发补救和失败处理；README 打包章节补 GitHub Release 入口。
- 单独 verify 默认校验远端 Release 和资产完整性；创建/补传后强制比较本地 digest，也可用 `--compare-local` 手动比较当前 `dist-release/`。
- 创建/补传 Release 时强制要求 HEAD 等于当前版本 tag 且工作区干净，防止发布后继续改动时误覆盖同版本资产。

### 验证

- `node --check scripts/github-release.mjs`
- `node scripts/github-release.mjs --help`
- `npm run release:assets -- --skip-build`
- `npm run release:github:verify`
- `npm test`：476/476 通过
- `git diff --check`

## 16:18 - 发布 0.1.1 并修复 Studio 安装态工作区读取

- 版本统一提升到 `0.1.1`：同步根包、workspace 包、Tauri/Cargo 版本、package lock、运行时兼容版本、MCP client version、Desktop updater 示例元数据、README 和首页发布资产说明。
- Desktop Studio 在 Finder / LaunchServices 启动且 cwd 为 `/` 时，不再把根目录或空的 `$HOME/.agentmesh` 当作 workspace；支持 `AGENTMESH_STUDIO_WORKSPACE`，并会从常见开发目录发现最近的 AgentMesh workspace。
- Tauri sidecar 启动参数透传 `--workspace` / `--workspace=...`，并把 macOS `Info.plist` 权限说明纳入发行包校验，避免安装态访问用户目录时缺少 usage description。
- Studio 运行详情修复 workflow flow 尺寸和方向箭头，统一 workflow 与产物流节点尺寸；节点和详情展示实际 Agent，退出码缺字段时显示“未记录”，current-only 阶段显示“无外部进程”。
- 审查发布 raw review tab 优先展示 Agent 名称，设置页兼容性元数据缺失时展示可诊断的“尚未生成 / 未记录”状态。

### 验证

- `npm install --package-lock-only --ignore-scripts`
- `npm test`：476/476 通过
- `cargo check`
- `npm run studio-desktop:package:dev`
- `cargo tauri build --debug --bundles dmg`：生成 `apps/studio-desktop/src-tauri/target/debug/bundle/dmg/AgentMesh_0.1.1_aarch64.dmg`
- `git diff --check`
