# 2026-05-20

## 00:06 - 固定内置 Workflow ID 并收紧资源引用

- 内置 Workflow 改为使用固定短 ID 常量，Runtime、SDK、CLI、release-check gate 和 Studio catalog 统一读取同一组 ID；用户侧显示继续优先展示 Workflow 名称。
- Workflow / Preset 创建改为底层自动维护 ID，TOML 手写内容不再接受顶层 `id`，避免用户改名后引用失效或不同来源产生歧义。
- Preset 的关联 Workflow、默认 Agent 和阶段分配继续用下拉展示名称，但配置与运行时引用只落 ID；相关文档、手册、AgentMesh skill 和测试 fixture 已同步。
- Studio 运行搜索补充 `latest_event` 匹配，内部 Workflow ID 替换后仍可通过 review 等事件关键词找到相关运行。

### 验证

- `npm run build:node`
- `node --test dist-node/tests-node/core-contracts.test.js dist-node/tests-node/workflow-registry.test.js dist-node/tests-node/management-cli.test.js dist-node/tests-node/flow-run.test.js dist-node/tests-node/flow-context.test.js dist-node/tests-node/flow-dispatch.test.js dist-node/tests-node/flow-preset-run.test.js dist-node/tests-node/release-check-flow.test.js dist-node/tests-node/release-check.test.js dist-node/tests-node/config-layering.test.js dist-node/tests-node/readiness.test.js dist-node/tests-node/studio.test.js dist-node/tests-node/studio-ui.test.js dist-node/tests-node/sdk-read.test.js`：260/260 通过
- `npm run build:studio-frontend`
- `npm test`：468/468 通过
- `git diff --check`

## 01:15 - 补齐资源更新链路

- CLI 新增 `agents update <agent-id>`、`workflows update <workflow-id>` 和 `preset update <preset-id>`；更新都通过内部 ID 定位，名称仅作为用户可读展示。
- App Server 新增 workflow / preset 的 PUT 更新端点，并复用 runtime 校验与 registry 覆盖逻辑；内置 Workflow 仍不可更新。
- Studio 资源页为自定义 Workflow 和 Preset 增加编辑入口，编辑弹窗沿用“用户 / 项目”和“手动填写 / 选择 TOML”两套创建表单能力，保存时按 ID 调用更新。
- Catalog 现在为 Workflow / Preset 返回编辑所需的详情字段，Preset 默认 Agent 与阶段分配继续按 agent ID 落盘、按名称展示。

### 验证

- `npm run build:node`
- `npm run build:studio-frontend`
- `node --test dist-node/tests-node/cli-surface.test.js dist-node/tests-node/management-cli.test.js dist-node/tests-node/studio.test.js dist-node/tests-node/studio-ui.test.js`：79/79 通过
- `npm test`：469/469 通过
- `git diff --check`

## 12:15 - 资源注册统一为用户级

- Agent、Workflow、Preset 和 MCP server 注册表统一为用户级全局资源；项目配置只保留 workflow defaults、context policy、execution policy 等运行策略。
- Runtime 拒绝项目配置里的 `[agents.*]` 和 `[mcp_servers.*]`，Workflow / Preset registry 不再读取 `.agentmesh/workflows` 或项目级 preset 目录；CLI、App Server 和 Studio 都移除资源创建时的 scope / project directory 写入路径。
- 资源管理命令改为固定写入用户配置：`agents add/update/remove`、`workflows add/update/remove`、`preset add/update/remove`、`mcp add/remove` 不再接受 `--scope`；旧 `--scope` 参数会明确报错。
- Studio 资源页、手册、README、AgentMesh skill 和配置分层契约同步为“资源全局、项目只存运行状态/策略”的文案；本机项目配置里的旧项目级 agent 已迁移到 `~/.config/agentmesh/config.toml`。

### 验证

- `npm test`：470/470 通过
- `node --test dist-node/tests-node/call-history.test.js dist-node/tests-node/flow-preset-run.test.js dist-node/tests-node/readiness.test.js`：59/59 通过

## 12:46 - 自查清理项目级资源残留

- 自查上一轮“资源统一用户级”改动后，继续删除 Studio 里残留的项目目录选择 API、App Server route、桌面 / CLI 目录 picker 透传和对应测试夹具。
- 清掉 Workflow / Preset registry 里仅剩 `user` 的 write-scope 类型别名，避免代码层继续暗示存在项目级资源写入分支。
- 手册“创建资源”说明同步为 Workflow 和 Preset 统一注册到用户配置、跨项目复用。

### 验证

- `npm run build:node`
- `npm test`：469/469 通过
- `git diff --check`

## 14:21 - 区分 Agent 启用状态和动作文案

- Studio Agent 列表和 Catalog 资源卡片里的状态 badge 改为显示 `已启用` / `已停用`，避免和可点击动作混淆。
- 停用后的操作按钮改为 `重新启用`，保留现有 `disabled` 配置、CLI 和 App Server 启停逻辑不变。
- 同步新增 Studio copy key，统一两处 agent 卡片的状态和动作文案。

### 验证

- `npm run build --workspace @agentmesh/studio-web`
- `git diff --check`

## 16:34 - 精简 Workflow 和 Preset 手填表单

- Studio Workflow / Preset 手填表单里的名称和说明字段去掉 `Workflow` / `Preset` 前缀，改为上下文内更简洁的 `名称` / `说明`。
- Workflow 阶段字段从逗号文本输入改为可多选的阶段下拉，固定选择 `plan`、`execute`、`verify`、`review`、`decide` 并继续生成同样的 TOML stage 列表。
- 补充 Studio UI 渲染断言，覆盖 Workflow 阶段选择控件和 Workflow / Preset 字段文案。

### 验证

- `npm run build:node && node --test dist-node/tests-node/studio-ui.test.js`：14/14 通过
- `npm run build --workspace @agentmesh/studio-web`
- `git diff --check`

## 16:49 - 限制有上限的 Agent 多选

- Studio 高级设置里的默认 Agent 多选限制为最多 6 个，Fallback Agent 多选限制为最多 3 个，与 runtime 的 fanout / fallback 上限保持一致。
- Preset 手填表单里的默认 Agent 多选限制为最多 6 个；阶段分配多选按阶段限制，`execute` 阶段最多 1 个，其它阶段最多 6 个。
- MultiSelect 组件使用 `maxValues` 阻止继续追加选择，同时在 `onChange` 层截断超限输入；Agent capabilities 和 Workflow 阶段类型选择没有业务数量上限，本次未加限制。
- 补充 Studio UI 源码断言，覆盖高级设置和 Preset 表单的多选上限。

### 验证

- `npm run build:node && node --test dist-node/tests-node/studio-ui.test.js`：14/14 通过
- `npm run build --workspace @agentmesh/studio-web`
- `git diff --check`

## 18:03 - 完善 Studio 高级设置和删除确认

- Studio 高级设置拆成“用户默认值 / 阶段默认 Agent / Fallback 设置”三个 tab；新增全局 `default_stage_agents.stage_types` 编辑能力，`execute` 阶段默认 Agent 限制为 1 个，其它阶段限制为 6 个。
- 高级设置里的重试次数、Agent 超时秒数、Fallback 尝试次数和 Fallback 超时秒数改为只接受数字输入，保存时继续按空值删除对应用户配置。
- Agent、Workflow 和 Preset 删除都增加二次确认；Workflow / Preset 新增前端 DELETE API、App Server 删除端点和 lifecycle remove 实现，删除用户 registry 文件后刷新 catalog。
- Workflow / Preset DELETE 端点补齐 `scope` / `project_dir` query 拒绝逻辑，避免 scoped 请求误删全局用户级资源；新增服务端回归测试覆盖拒绝后文件仍保留。

### 验证

- `npm run build:node`
- `node --test dist-node/tests-node/studio.test.js`：30/30 通过
- `npm run build --workspace @agentmesh/studio-web`
- `node --test dist-node/tests-node/studio-ui.test.js`：14/14 通过
- `git diff --check`

## 18:16 - 补充 Workflow 手填字段说明

- Studio 创建 / 编辑 Workflow 的手填表单为“使用场景”、“产物文件”和“质量门禁”补充短说明，降低字段理解成本。
- 说明文案统一进入 Studio copy key，并通过 Mantine Textarea 的 `description` 展示，不改变 TOML 生成逻辑和字段默认值。

### 验证

- `npm run build -w @agentmesh/studio-web`
- `git diff --check`
