# 2026-07-18

## 01:51 - 增加 reviewer session Lease 与管理 CLI

- 新增按 reviewer registry key 串行化的本机 lease、自动 heartbeat、epoch/CAS 证据与安全 crash recovery；每个 owner 使用唯一 candidate，旧 owner/reclaimer 不会删除 successor。
- 新增 `agentmesh sessions` 的 scope create、list、inspect、按 session/scope close 与 expired purge；管理输出仅包含安全摘要，不暴露 provider/native session identity、registry key 或原始 entry。
- crash recovery 使用 boot fingerprint + monotonic pulse，跨机器重启仍能安全判断旧证据；Linux 使用 `/proc` start identity，macOS 无法可靠证明 live PID 身份时 fail-closed。
- management tombstone/index 区分真正 unknown 与幂等 close retry；summary 使用 host/mode 闭集、严格 reviewer id，并拒绝与当前敏感 identity 相等的 opaque 值。

### 验证

- Focused：`npm run build:node && node --test dist-node/tests-node/reviewer-session-{lease,cli,registry}.test.js`，52/52 通过。
- 全量：`npm test`，630/630 通过；`git diff --check` 通过。
- 最终独立复核：Spec compliance 与 Task quality 均 Approved，0 Must / 0 Should / 0 Nit。

## 04:33 - 完善 Studio 活动管理与桌面更新偏好

- 活动列表的“运行/调用”改为无中括号的高辨识度标签，右侧增加本地化当前状态与时间；支持真实 `review_running`、`decide_completed`、`execute_2_failed` 等阶段状态。
- 日期分组仍可折叠；组内默认展示 5 条，展开全部后点击日期组会重置为 5 条；“展开其余 N 条”移除前置图标。
- 每条活动增加独立删除按钮与确认弹窗；App Server 提供 run/call DELETE API，删除原语通过 inherited fd + child cwd identity 锚定目标目录，防止 ancestor symlink、root swap、旧刷新响应复活与外部路径误删。
- About 兼容性卡移除兼容性文件、元数据状态、Packet Schema、最低读取版本与最后写入方，仅保留运行版本、入口、最低写入版本和最后更新时间。
- 桌面端新增“自动检测桌面端更新”开关，默认开启并持久化到 Tauri app config；启动时仅自动检查一次，关闭后不检查，手动检查不受影响；自定义 get/set command 使用最小 Tauri ACL 授权。

### 验证

- Focused 68/68、Studio server 44/44、Cargo 4/4、ACL focused 1/1；最终全量 `npm test` 638/638，`git diff --check` 通过。
- 内置浏览器验证标签/状态、组内展开、日期组重置、删除确认/取消和 About 精简字段；未删除真实活动数据。
- 调试 `.app` 真机验证自动检查成功、开关 on→off、重启恢复 off 且不自动检查，再恢复为 on；ACL 错误已消失。
- 最终独立复核：Spec compliance 与 Task quality 均 Approved，0 Must / 0 Should / 0 Nit。

## 05:40 - 完成 reviewer session 本机状态层门禁

- 将 reviewer lease election 移到独立的 0700 coordination anchor，并以 0600 dev/ino identity evidence 绑定物理 registry；registry rename/recreate、coordination/anchor replacement、real path 与 ancestor-symlink alias 均不能建立并行 lease namespace，heartbeat、release 和 stale reclaim 只操作身份匹配的证据。
- `sessions purge --expired` 现在识别 management record 原子写入遗留的 stale temp，仅清理 dead owner 的 0600 普通文件，保留 live/fresh/symlink/unrelated 文件。
- 新增可重放 disposable verifier：创建临时 repository、linked worktree、HOME、scope key 与 registry，仅输出安全布尔值/计数并在 `finally` 清理；CLI human/JSON 投影的 forbidden-value match count 为 0。
- P2 scope、registry、lease 与 sessions CLI 阶段完成，当前下一步进入 P3.1 Adapter Session Contract 与 Fake CLI；本阶段不接 provider resume 或 dispatch 编排。

### 验证

- 最终 P2 focused 68/68；可重放 verifier 完成且 cleanup=true、forbidden-value match count=0。
- 首轮修复后全量 `npm test` 643/643；物理路径 alias follow-up targeted 1/1、lease focused 23/23；`git diff --check` 通过。
- AgentMesh gate `workflow-20260718044744` 已 `decide_completed`；原始 2 Must/1 Should 和 follow-up 1 Must 全部关闭，最终定向复核 0 Must / 0 Should / 0 Nit。

## 05:59 - 定义 Adapter Session 公共契约

- 新增 provider-neutral `AdapterSessionDirective`、`AdapterStructuredResult`、安全结果投影和 optional plugin session hooks；旧 plugin 或 capability/hook 不完整时继续走原有 fresh invocation。
- registry clone 保留 `supports_resume` / `supports_structured_session_id`；当前内置 adapter 不提前启用 session 能力，真实 Claude Code/OpenCode parser 与 builder 留到 P3.2。
- test-only fake CLI 覆盖 structured start、resume success、not found、rate limited、auth required 与 invalid output；自由文本不能猜测 session ID。
- 安全投影同时脱敏 result 自带 ID 与调用 directive 中已知的 resume ID，即使失败结果没有携带 ID，也不会从 output、public failure 或 unknown diagnostics 泄漏。

### 验证

- Fresh Node build 与 focused adapter tests 22/22 通过，`git diff --check` 通过。
- 独立 reviewer 首轮 1 Must/1 Should 已关闭，最终 Spec/Quality Pass、LGTM、0 Must/0 Should/0 Nit。
- AgentMesh Review Gate `workflow-20260718055803` 已 `decide_completed`；未访问真实 provider、registry、token、cookie、keychain、login 或 private session state。

## 06:21 - 接入已验证的 Provider Session Adapter

- 仅为能力矩阵已闭环的 Claude Code 与 OpenCode 启用 structured session/resume：分别使用 `session_id` 与 `sessionID`，并保持已验证的 fresh/resume argv shape。
- Codex、Cursor 与 Antigravity capability 明确为 false，不新增 parser、fixture 或 resume builder，继续沿用原有 fresh invocation。
- 两类 parser 只接受脱敏 fixture 证明的完整三 event lifecycle、固定顺序/基数和一致 ID；Claude success result 还必须满足 `is_error === false`。截断、重复、乱序、自由文本、未知 event 和 ID 异常统一拒绝为 `invalid_output`。
- Readiness 增加 `supports_resume` / `supports_structured_session_id` 静态投影，不执行 provider start、resume 或 auth probe；所有 fixture session 值固定为 `session-test-123`。

### 验证

- Fresh Node build、`adapter-invocation` + `readiness` focused 63/63、`git diff --check` 通过。
- 独立 reviewer 首轮 lifecycle Must 已关闭，最终 Approved、LGTM、0 Must/0 Should/0 Nit。
- AgentMesh Review Gate `workflow-20260718062036` 已 `decide_completed`；已知限制仍是只验证 immediate resume，dispatch/fallback 尚未接入。

## 13:21 - 编排 Reviewer Session Dispatch 与失败回退

- Continuous review 现在按 safe host scope、workspace/worktree、reviewer、adapter/model/reasoning/fingerprint 建立本机 session；同 scope 后续 run 在 entry lease 内 resume，不同 scope/linked worktree 保持 fresh。Independent 从 run creation 起即不创建 scope key，也不读写 registry/lease/provider session。
- 固定 `run mutation → entry lease → provider spawn` 锁序；lease busy 使用带 lane attempt 幂等键的 `fresh_isolated` 且不捕获/写入 provider ID，epoch close/purge race 不能把 stale entry 写回。
- Failure matrix 实现有界动作：expired/not-found/context-overflow/invalid/unsupported 最多一次 fallback fresh；network/rate-limit/provider-busy 最多一次预算内 retry；auth/permission/config/incompatible hard fail；所有路径无 resume/fresh 循环并使用 remaining timeout。
- Structured timeout 保留 `timed_out/error_kind=timeout`；classifier 不从 stdout/model free text 猜测失败类型。Provider/native ID、propagation token、registry key、raw diagnostics 与测试 sidecar 均通过零泄漏约束。
- 新增 exact `reviewer_session.*` events 与 attempt session provenance；P3.4 仍负责 prompt delta、findings/decide/release 的 non-hermetic 展示和裁决语义。

### 验证

- P3.3 focused 首轮 167/167、累计修复后 171/171；最终 production-wiring directed 3/3，controller fresh 完整七文件组合 exit 0；Node build 与 `git diff --check` 通过。
- 累计 review 共 9 Must/1 Should 全部修复，最终独立定向复核 Spec/Quality PASS、LGTM、0 Must/0 Should/0 Nit。
- AgentMesh Review Gate `workflow-20260718132006` 已 `decide_completed`；未访问真实 provider、用户 registry、token、cookie、keychain、login 或 private session store。

## 19:58 - 传播 Reviewer Session Delta 与 Non-Hermetic Provenance

- Resumed provider 的实际 prompt 现在内联 current diff、verification、active corrections，并追加 bounded since-last changed-files delta；旧 file/line 引用被明确标记失效，current packet request/diff/verification/corrections 始终是权威证据。
- AgentMesh-owned terminal sentinel 让 prompt 重入保持幂等；fresh、fallback-fresh、fresh-isolated 与 independent 不携带 resumed-only delta。Diff、verification、corrections 各自独立有界并显式记录 truncation，避免某类证据被大 diff 挤掉。
- Correction record 增加 optional `session_impact=data|persona|system`：legacy/data correction 继续 resume 并重发当前内容；persona/system add、supersede、removal 触发安全 fingerprint 轮换，不复用旧 provider session，statement/free text 不参与 fingerprint。
- Findings、decide prompt、release summary 从 completed 且具有 usable raw output 的 StageAttempt 生成 bounded provenance；失败 resume 后的 fresh fallback 不产生假 non-hermetic warning。Independent release 遇 resumed evidence 会产生 `session_resume` needs-decision risk，不能静默作为 ready 证据。

### 验证

- Focused suite 211/211；controller fresh 重跑完整 P3.4 focused 组合 exit 0；Node build 与 `git diff --check` 通过。
- 独立 review 的 7 Must/1 Should 全部修复，最终 Spec/Quality PASS、LGTM、0 Must/0 Should/0 Nit。
- AgentMesh Review Gate `workflow-20260718195634` 已 `decide_completed`；未访问真实 provider、用户 registry、token、cookie、keychain、login 或 private session store。

## 20:49 - 完成 Reviewer Session Dispatch 阶段门禁

- 新增可重放 disposable fake-CLI E2E，闭环 fresh、resume、resume failure、close/rotate 与单次 fallback fresh；生成 artifact 对 fake provider ID 和原始 propagated scope 的匹配均为 0，临时 workspace/HOME/config/registry 在 `finally` 清理。
- 修复 resumed retry 的终态处理：recoverable 终态只 close/rotate 并 fallback fresh 一次，retry 成功不再误发 `resume_failed`；OpenCode structured invocation 使用 caller cwd/worktree，provider 未 spawn 时不再伪造 `session_mode=fresh`。
- P3 Adapter Session、Provider Adapter、Dispatch/lease/failure matrix、structured delta 与 non-hermetic provenance 阶段全部完成；Codex、Cursor、Antigravity 仍保持 fresh-only，只有能力矩阵验证过的 Claude Code 与 OpenCode 启用 resume。
- Cursor 定向 closure 与 Claude 函数级复核均为 LGTM、0 Must/0 Should/0 Nit；GLM 最后一次函数级复核在 240 秒超时且没有 review output，作为 reviewer 可用性残余证据记录，不解释为技术结论。

### 验证

- Fresh P3 targeted：315/315；disposable E2E cleanup=true、泄漏计数 0。
- Fresh 全量 `npm test`：694/694，包含 Node 与 Studio frontend build；`git diff --check`、`git diff --check 920c4f0` 通过。
- AgentMesh gates：`workflow-20260718203342`、`workflow-20260718203944` 均 `decide_completed`。
