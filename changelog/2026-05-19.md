# 2026-05-19

## 14:35 - 收口 Studio 资源、设置与中文单语言体验

- Studio 资源页补齐 Presets 入口，并把 Agents、Workflows、Presets、MCP 统一纳入资源计数与 tab；Agents 支持编辑、启停、删除，创建和编辑表单改为工具 / 模型 / 能力下拉，Agent 标识由 Studio 内部生成且编辑只读。
- Workflow / Preset 创建改为弹窗流程，默认保存到用户配置；选择项目时支持文件夹选择，导入来源支持选择 TOML 或手动填写。Preset 手动创建会根据关联 Workflow 展示阶段，并为每个阶段下拉分配 Agent。
- 设置页新增高级配置，可查看用户配置路径并编辑默认 Agent、Fallback Agent、重试 / 超时和执行策略；环境页改为运行 / 调用 tab，关于页改为中文版本与兼容性说明。
- 运行详情将总览和阶段合并为详情；Workflow Flow 与产物流补齐耗时、尝试次数和生成时间，并统一压缩节点上下留白；左侧切换运行时右侧回到详情。
- 手册重分为快速开始、安装使用、核心概念、操作指南、架构设计、数据与参考、排障，补齐介绍、安装指引、组件说明和架构内容。
- 前端移除中英文切换与 i18n 语言层级，保留固定中文文案表；Vite 前端拆分 vendor / Mantine chunk，避免 chunk size warning。
- App Server 新增高级设置、Preset 创建、项目目录选择和 Agent 模型发现 API；项目目录选择由 CLI / Studio / Desktop 入口注入，App Server 不再直接 spawn 本机脚本。
- 自审修复：项目目录模式下 Workflow / Preset 命令回显不再误带 `--config`；Agent 保存会用实时工具模型发现结果解析模型，避免下拉可选但提交失败；写侧测试隔离 `$HOME` 和 `AGENTMESH_CONFIG`，避免用户本机配置污染测试。

### 验证

- `npm run build`
- `node --test dist-node/tests-node/agent-registration.test.js dist-node/tests-node/studio.test.js dist-node/tests-node/studio-ui.test.js dist-node/tests-node/package-structure.test.js dist-node/tests-node/flow-dispatch.test.js dist-node/tests-node/flow-retry-resume.test.js dist-node/tests-node/release-check-flow.test.js`：121/121 通过
- `npm test`：458/458 通过
- `git diff --check`

## 18:22 - 收紧 Agent ID 与 Preset 关联规则

- Agent 创建改为由 runtime/CLI 统一生成短 ID，CLI 不再接受位置参数指定 Agent ID；Studio 创建和编辑只展示 ID，不允许用户修改。
- Agent 列表和资源卡片改为优先展示名称，工具和模型作为次级信息展示，避免短 ID 占据主标题。
- Preset 的默认 Agent 和阶段分配继续通过下拉展示名称，但底层写入和导入校验只接受 Agent ID；名称仅用于展示和记忆。
- Studio Agent 编辑区分不可变 ID 与可读名称，ID 由系统生成且不允许用户修改。
- 已同步文档、AgentMesh skill 和本机用户配置中的现有 Agent ID；本机配置修改前备份为 `config.toml.bak.20260519171029`。

### 验证

- `node --test dist-node/tests-node/studio-ui.test.js dist-node/tests-node/flow-preset-run.test.js`：20/20 通过
- `npm run build:studio-frontend`
- `npm test`：461/461 通过
- `git diff --check`

## 19:00 - 移除 Agent 别名引用层

- Agent 配置、Runtime、SDK、App Server、Studio 和 CLI 全面移除 Agent 级 `aliases` 字段；Agent 只能通过系统生成的 ID 被实际引用，名称仅用于用户识别和展示。
- `resolveAgent`、`agentmesh call`、`doctor --agent`、`agents show` 和 Preset 阶段分配都收紧为只接受 Agent ID；旧配置里写 `agents.<id>.aliases` 会 fail fast。
- Studio Agent 创建 / 编辑表单不再展示或提交别名，资源列表和下拉继续展示名称，底层请求与 TOML 写入保持 ID。
- 文档、手册、AgentMesh skill 和本机用户配置同步移除 Agent 别名说明；本机配置修改前备份为 `config.toml.bak.20260519185617`。

### 验证

- `npm run build:node`
- `npm run build:studio-frontend`
- `node --test dist-node/tests-node/config-layering.test.js dist-node/tests-node/cli-surface.test.js dist-node/tests-node/studio.test.js dist-node/tests-node/studio-ui.test.js dist-node/tests-node/sdk-read.test.js dist-node/tests-node/flow-preset-run.test.js dist-node/tests-node/readiness.test.js`：147/147 通过
- `npm test`：461/461 通过
- `git diff --check`

## 22:21 - 优化 Agent 模型下拉预加载

- Studio 创建 / 编辑 Agent 弹窗改为打开时并发预拉全部内置工具的模型列表，并在弹窗内缓存结果；切换工具只读缓存，不再反复触发模型发现请求。
- Runtime 模型发现补齐 Codex、Claude Code 和 Gemini：Codex 使用 `codex debug models --bundled` 的 JSON catalog；Claude Code 从本机 `claude` 可执行文件 catalog 中提取模型；Gemini 从本机 `@google/gemini-cli` 包内 catalog 提取模型。
- Codex catalog 过滤 `hidden` / `hide` 可见性模型，避免把内部或隐藏项展示给用户；Studio server 的 unsupported 分支测试改为使用 generic `command` adapter，避免依赖本机 AI CLI 安装状态。
- 本机实测五个工具均可发现模型：Codex 5 个、Claude Code 50 个、Cursor 7 个、Gemini 10 个、OpenCode 18 个。

### 验证

- `npm run build:node`
- `node --test dist-node/tests-node/agent-registration.test.js dist-node/tests-node/studio-ui.test.js`
- `node --test dist-node/tests-node/studio.test.js`
- `npm run build:studio-frontend`
- `npm test`：468/468 通过
- `git diff --check`

## 22:42 - 统一 Studio 下拉搜索体验

- Studio 全局 Mantine theme 为 `Select` 和 `MultiSelect` 默认启用 `searchable`，统一普通下拉和多选下拉的搜索体验。
- 自动刷新间隔控件从 `NativeSelect` 切换为 Mantine `Select`，同步纳入全局 searchable 行为，并更新对应样式选择器。
- Studio UI 测试同步覆盖全局 searchable 默认值、自动刷新控件 Select 化，以及可搜索下拉不再作为只读输入渲染。

### 验证

- `npm run build:node`
- `node --test dist-node/tests-node/studio-ui.test.js`：14/14 通过
- `npm run build:studio-frontend`
- `git diff --check`
