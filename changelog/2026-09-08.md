# 2026-09-08

## 16:35 - 准备 AgentMesh 0.2.1 发布

- 修复第三方 Provider/API Key 可用但 Codex 官方账号未登录时 Agent 添加失败的问题，改为在登录态检查失败后执行临时、只读的真实模型探测。
- 增加无效 Key、模型不可用、网络错误、空响应和超时场景的回归覆盖，并将 Codex 默认探测超时调整为 60 秒。
- 将 root、全部 workspace、内部 exact dependencies、Tauri/Cargo、updater 示例、运行时版本常量和版本测试同步到 `0.2.1`。
- 更新 README 当前发布入口，新增 `docs/distribution/v0.2.1-release-notes.md`；历史 changelog 和 v0.2.0 发行说明保持不变。

### 验证

- `npm test`：716 passed，0 failed；覆盖第三方 Codex Provider 成功探测与失败分类。
- `npm run check:boundaries`、`cargo check --manifest-path apps/studio-desktop/src-tauri/Cargo.toml` 和 `git diff --check` 通过。
- `npm run release:assets -- --notes-file docs/distribution/v0.2.1-release-notes.md` 通过，包含桌面 sidecar 验证、dev distribution smoke 和 Tauri app/DMG 构建。
- 本地 CLI tgz、Apple Silicon DMG、updater archive/signature、`latest.json`、Skill markdown 与 `SHA256SUMS` 已生成；`shasum -a 256 -c dist-release/SHA256SUMS` 全部通过。
