[Must Fix] packages/sdk/src/index.ts - `appendCallAdoption` directly modifies `adoption.jsonl` and `call.json` using `appendFileSync` and `writeFileAtomic`. The SDK must be a stable public read-only surface. This bypasses the CLI mutation contract and violates the read-only boundary; it should be moved to `@agentmesh/runtime` and invoked via CLI like other mutations.

[Should Fix] apps/studio/src/catalog.ts & apps/studio/src/agent-lifecycle.ts - Both files use `spawnSync` to execute `agentmesh agents list` and `agentmesh workflows list` CLI commands. Since P1.3 explicitly promoted `listAgents` and `listWorkflows` to the `@agentmesh/sdk`, the App Server should use these direct, stable SDK methods for reads instead of spawning expensive subprocesses.

[Nit] scripts/check-boundaries.mjs:21 - The regex `/@agentmesh\//` in the Studio frontend boundary check is overly strict if the frontend actually needs to import type-only contracts from `@agentmesh/core` or `@agentmesh/sdk`. Because `import type` is stripped during compilation, the script should ideally allow `import type` to support sharing type-only contracts.

Summary: 1 Must, 1 Should, 1 Nit
