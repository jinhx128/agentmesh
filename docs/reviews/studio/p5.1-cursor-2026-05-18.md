Unable to run `git diff` in this environment (command was rejected), so this review is against the **current contents** of the scoped paths you listed, aligned with your stated P5.1 intent.

## Must Fix

No blocking factual contradictions found between: PATH-visible `agentmesh` for Skill/entry-agent use, DMG-only = Desktop Studio only, optional future DMG CLI install (confirm + show existing PATH target), per-host Skill install (no blanket auto-install), and split paths (`.agents/...` vs `.claude/...`). The docs consistently frame DMG CLI and Skill flows as **opt-in / future** where applicable, and **`dist-node/packages/cli` is not bundled in the desktop app** (`studio-macos.md`), which avoids implying DMG ships the npm-style CLI surface.

## Should Fix

1. **Post-install verification examples default to Codex** — In `README.md`, the “CLI 安装后至少验证” block uses `agentmesh skill verify --target codex --json`. That reads like Codex is the required acceptance host; teammates who only install `--target cursor` (etc.) should verify **the same host(s) they installed**. Prefer `<host>` or explicit wording (“对你已安装的 target 执行 verify”). Same idea applies wherever “验收” is codex-only without saying why.

2. **Ambiguous “runtime/CLI entrypoint” on macOS app updates** — In `docs/distribution/studio-macos.md` under **App-Managed Updates**, “runtime/CLI entrypoint” can be read as “the global `agentmesh` CLI channel.” The next sentence clarifies npm globals must not be mutated, but tightening the phrase (e.g. **sidecar / app runtime entrypoint**, explicitly **not** the PATH `agentmesh` tarball) would reduce the risk of misreading DMG updates as touching the npm CLI.

3. **Pinned tarball filename in `cli-command-install.md`** — `npm install -g /tmp/agentmesh-0.1.0.tgz` will drift when `package.json` `version` changes. Prefer a wildcard or a placeholder (`agentmesh-<version>.tgz`) plus “match `npm pack` output,” consistent with `README.md`’s `./agentmesh-*.tgz` style.

## Nit

1. **Time-stamped registry note in `README.md`** (“截至 2026-05-18 …”) — Accurate today but will stale quickly; consider removing the calendar date or pointing at `package.json` / release process without a fixed day.

2. **`README.md` does not link `docs/distribution/cli-command-install.md`** — Only `package-shape.md` points there; a single link from the new PATH/CLI section would help teammates find the canonical English spec.

3. **`npm install -g` vs “installer must confirm before replace”** — `package-shape.md` / human-run snippets describe **team/installer discipline** and future DMG behavior; `npm install -g` itself does not prompt. A half-sentence clarifying that avoids implying npm enforces confirmation.

4. **Smoke test brittleness** — In `tests-node/package-structure.test.ts`, `verifyPayload.files[0]` assumes ordering; low risk but fragile if report shape changes.

## Summary

The scoped docs and `package.json` / tests **do close P5.1** in substance: they separate **Desktop (DMG / app-bundled runtime)** from **PATH `agentmesh`**, state that **Skills resolve the CLI via PATH** (no embedded private CLI), document **per-target** `skill install` / `skill verify`, spell out **shared vs Claude paths**, and spell **future DMG “Install Command Line Tool”** as **user-confirmed with inspection of any existing PATH binary**. `npm run cli:install-smoke` and the expanded pack-install test mirror the documented smoke commands (`--help`, `doctor --json`, `skill show`, `skill install --target codex --force`, `skill verify --target codex --json`), so automation matches the written gate.

Residual risk is mostly **reader confusion**, not silent DMG auto-install: tighten **verify examples** so they do not imply Codex-only acceptance, clarify **app update wording** so “CLI” cannot be mistaken for the global command, and avoid **hardcoded tarball versions** in `cli-command-install.md`.

---

_used_: mode=coding-mode · skills=无 · tools=Read, Grep, Shell (diff rejected)
