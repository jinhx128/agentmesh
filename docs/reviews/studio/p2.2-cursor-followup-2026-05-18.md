The previous P2.2 write-up tied “four `..` segments” to `dist-node/packages/cli/src/cli.js`. In the real code, `import.meta.url` in `resolveDefaultStudioAssetDir` is **`packages/cli/src/commands/studio.ts` → `dist-node/packages/cli/src/commands/studio.js`**, which sits one directory deeper than `cli.js`. From `…/src/commands/` you need **four** parents to reach `dist-node/`, then `apps/studio-web/frontend` yields **`dist-node/apps/studio-web/frontend`**, matching the Vite `outDir` and your verified build output. So the earlier “blocking” CLI `assetDir` issue was a **path-algebra mistake (wrong reference file)**, not a live bug in the current snippet.

Tests: user said studio-cli 92/92 and npm test runs compiled tests against `dist-node/packages/cli/src/cli.js`, which loads `commands/studio.js` — consistent with the above.

Optional residual items from the old review: Vite cwd (fixed), check-boundaries carve-out (still minor), plan.md drift (docs).

Format per `docs/reviews/studio/p2.2-cursor-2026-05-18.md` style.

# agentmesh-review-markdown-v1

**Scope:** P2.2 follow-up — CLI default `assetDir`, Vite `outDir`, compiled layout under `dist-node/`.

**Verification:** Static review of `packages/cli/src/commands/studio.ts` + repo layout (`packages/cli/tsconfig.json` → `outDir: ../../dist-node`, preserve `src/commands/`); cross-check against `apps/studio-web/vite.config.ts` and prior review note in `docs/reviews/studio/p2.2-cursor-2026-05-18.md`. Did not re-run `npm test` in this session (you reported 92/92 including studio-cli).

---

## Verdict

**Not blocking.** The earlier “CLI `assetDir`” finding assumed `import.meta.url` for the default path was anchored at `dist-node/packages/cli/src/cli.js`. **`resolveDefaultStudioAssetDir` lives in `commands/studio.ts`**, which emits to `dist-node/packages/cli/src/commands/studio.js`. From that directory, four `..` segments reach **`dist-node/`**, so `apps/studio-web/frontend` resolves to **`dist-node/apps/studio-web/frontend`**, aligned with root `build:studio-frontend`, tarball `files`, and your confirmed Vite output. Default `agentmesh studio` asset discovery is consistent with the compiled tree, modulo missing build or overrides.

---

## Blocking

*None.*

---

## Should fix (non-blocking)

**[Should Fix] `docs/reviews/studio/p2.2-cursor-2026-05-18.md` (and any other docs) — historical path algebra**

That review’s blocking section is **incorrect** for the current code layout (it used **`cli.js`** as the `import.meta.url` anchor). Updating or superseding it avoids future “false blocker” churn.

**[Should Fix] `scripts/check-boundaries.mjs` — `vite.config.ts` outside `apps/studio-web/src/`** (unchanged from prior review)

If you want boundary rules to cover the Vite entry, extend the guard explicitly instead of relying only on `src/`.

**[Should Fix] `plan.md` — doc drift** (`dist-node/apps/studio/frontend` vs `dist-node/apps/studio-web/frontend`) if still present.

---

## Nit

**[Nit] `packages/cli/src/commands/studio.ts` — running CLI from unpublished `packages/cli/src/*.ts` without matching `apps/studio-web/frontend`**

Default resolution from **source** under `packages/cli/src/commands/` still points at **repo** `apps/studio-web/frontend`, while Vite writes to **`dist-node/...`**. Published workflow uses **`dist-node/packages/cli/src/cli.js`** (`package.json` `bin` / `npm test`), so this is a narrow dev footgun, not a packaged-runtime blocker.

---

## Looks good

- **`import.meta.url`-based `outDir` in `apps/studio-web/vite.config.ts`** — stable regardless of cwd when invoking Vite; matches your verification.
- **Parity with other entrypoints:** `apps/studio/src/main.ts` and studio-desktop resolve to the same **`dist-node/apps/studio-web/frontend`** from their emitted locations (different hop counts, same destination).
- **`AGENTMESH_STUDIO_ASSET_DIR` + `statSync(index.html)`** — clear override and fail-fast with build hint.

---

## Summary

**Summary: 0 Blocking, 3 Should Fix, 1 Nit**

---

_used_: mode=coding-mode · skills=my-coding-mode · tools=Read, Grep, Glob
