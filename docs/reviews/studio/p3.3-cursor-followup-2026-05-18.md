## Verdict: **PASS / LGTM**

Cross-check against the prior **Should fix** list in `docs/reviews/studio/p3.3-cursor-2026-05-18.md`:

| Item | Status |
|------|--------|
| **1. Lock details: `pid` + `operation_id`** | **Resolved** — `formatLockDetails` in `SafeActionsPanel.tsx` includes both; `studio-ui.test.ts` asserts them in the locked output. |
| **2. `workspace_refused` mutation API tests** | **Resolved** — `studio.test.ts` has `"Studio mutation endpoint surfaces refused compatibility..."` with `error_code === "workspace_refused"`, `409`, `retryable: false`. |
| **3. Settings/About refused UI test** | **Resolved** — `studio-ui.test.ts` `"React settings about renders compatibility refused state"` covers copy beyond a bare `/refused/` match. |
| **4. Stable codes vs English message prefixes** | **Resolved** — `WorkspaceCompatibilityError` + `isWorkspaceCompatibilityError()` in `compatibility.ts`; `mutations.ts` maps via `error.code` (no `compatibilityMutationErrorCode` / prefix parsing left in app-server). |
| **5. Contract doc** | **Resolved** — `docs/contracts/app-server.md` documents `409` + `workspace_read_only` / `workspace_refused` + `retryable: false`. |

**Must fix:** none seen in this pass.

**Should fix:** none new; the earlier **nit** about packet/schema skew still surfacing as `mutation_failed` remains a product/contract choice, not a regression from these fixes.

Your reported **73/73** test run was not re-executed here; the tree matches what those tests would exercise.

---

_used_: mode=coding-mode · skills=my-coding-mode · tools=Read, Grep
