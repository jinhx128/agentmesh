## Verdict

**LGTM / PASS for the core P3.3 wire-up** (App Server status ↔ `error_code` / `retryable`, lock payload plumbed to mutations, `/api/compatibility` + Settings/About, desktop `entrypoint: "desktop"`). I could not run `git diff` in this environment (tool rejected), so this is based on reading the current tree and greps, plus your note that **71/71** tests pass locally.

---

### Must fix

**None** that clearly violate the stated contracts in codeReview *given* “workspace compatibility” is interpreted as `.agentmesh/compatibility.json` policy (not packet `schema_version` skew).

---

### Should fix

1. **Safe Actions lock text omits fields you called out as required**
   `formatLockDetails` prints operation, entrypoint, runtime, command, heartbeat, expires but **not `operation_id` or `pid`**, though both are on `StudioMutationLock` / `RunMutationLockDetails` and are part of the “recoverable” story.

2. **`workspace_refused` is never exercised in tests**
   There is coverage for `workspace_read_only` (e.g. `studio.test.ts`) and for desktop lock + newer packet attach (`studio-distribution-coexistence.test.ts`), but **no test asserts `error_code === "workspace_refused"`** (e.g. `min_read_runtime_version` newer than current, invalid compatibility file + mutation, etc.).

3. **Settings/About only has a React test for `read_only`, not `refused`**
   Refused mode has stronger UX (“before reading or mutating”); a static render test would lock that behavior in.

4. **Stable error codes for workspace mutations are coupled to English `Error.message` prefixes** (`compatibilityMutationErrorCode` in `mutations.ts`)
   Any rewording of `assertWorkspaceWritable` / `assertWorkspaceReadable` breaks Studio’s `error_code` without a compile-time check. A typed error or sentinel substring/code from runtime would be harder to regress.

5. **Contract doc gap**
   `docs/contracts/app-server.md` documents `423` / `run_locked` but **does not spell out `409` + `workspace_read_only` / `workspace_refused` + `retryable: false`** for mutation JSON, which you listed as part of the API semantics.

---

### Nits

- **Packet / status skew** (e.g. “unsupported packet schema version”) still surfaces as **`mutation_failed` + `409`**, not `workspace_refused`. That can be correct if “workspace compatibility” is only the compatibility file — but Studio cannot key off a stable code for that class; worth a one-line product note or a dedicated `error_code` if you want one UX path for all “upgrade / don’t write” cases.
- **Reason list keys** in `SettingsAboutPanel` use `key={reason}` — duplicate reasons would be awkward for React (rare).
- **Residual verification**: I did not re-run tests here; **no full `git diff` review**. If you need line-accurate review of unstaged-only hunks, re-run diff locally or allow shell in this session.

---

_used_: mode=无 · skills=无 · tools=Grep, Read, Glob
