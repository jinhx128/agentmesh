## P4.Z closeout review (read-only)

### Verdict: **PASS / LGTM — no code-level blockers to closing P4.Z and proceeding to P5**

Implementation matches the stated stage expectations: `@agentmesh/skills` centralizes the template, version-metadata injection, install/verify, and the shared-project vs Claude path split; the CLI is a thin wrapper over that API; `packages/runtime` no longer exports or contains `skill/verify`; tests and `check:boundaries` encode the intended dependency direction; tarball whitelist and import rewrite include `packages/skills` as described in `docs/distribution/package-shape.md` and `tests-node/package-structure.test.ts`.

**Evidence from the tree (not a re-run of your commands):**

- **Ownership / API:** ```1:1:packages/skills/src/index.ts``` re-exports `./verify.js` only; ```315:339:tests-node/package-structure.test.ts``` asserts no root `agentmesh-skill/`, no `runtime/src/skill`, CLI imports `@agentmesh/skills` only.
- **Target contract:** ```225:238:packages/skills/src/verify.ts``` — shared hosts → `.agents/skills/agentmesh/SKILL.md`; Claude → `.claude/skills/agentmesh/SKILL.md`; legacy Cursor rule only in `legacyFilesForTarget` for verify diagnostics.
- **CLI consumption:** ```1:74:packages/cli/src/commands/skill.ts``` — `show` / `export` / `install` / `verify` all delegate to `@agentmesh/skills`; install logging uses **`report.files.filter((item) => item.expected)`**, so successful install does not echo `legacy_only` paths (aligned with your tests and prior review follow-up).
- **Runtime:** ```1:15:packages/runtime/src/index.ts``` — no skill exports; grep shows no `packages/runtime/**/skill/**`.

**Process note (not a blocker):** `plan.md` still has the P4 stage gate and P4.Z checkbox unchecked until someone records P4.Z and ticks them—expected paperwork after this review, not a product defect.

---

### Must fix

**None** for “P4 skill package + install-path modernization” as scoped. No missing package, wrong import boundary, or contradictory install/verify behavior turned up in active code paths.

---

### Should fix

1. **`docs/contracts/skill-output.md` wording** — The contract still says the metadata block is “generated from the **runtime**” (around lines 25–28). After P4, that text is **misleading**; it should describe generation from **`@agentmesh/skills` / `agentmeshSkillMarkdown`** (or equivalent) so architects do not assume `packages/runtime` owns Skill output.

---

### Nit

1. **`agentmeshSkillMarkdown` fallbacks** — ```104:108:packages/skills/src/verify.ts``` still tries `path.join(projectRoot, "agentmesh-skill", "SKILL.md")`; tests assert the repo no longer uses a root `agentmesh-skill/`. Harmless for odd layouts, slightly redundant.
2. **`expectedSkillFilesForTarget` adopters** — Exported and tested for path contract + `homeDir` non-use; Studio/App Server do not appear to call it yet. Fine for P4 if the goal was to **publish** the contract; optional follow-up if P5 wants “single source” in those UIs.
3. **`VerifyOptions` is internal** — Public functions take it, but the interface type is not exported; IDE hints for `cwd` / `homeDir` are only as good as JSDoc on the public entrypoints (already partially documented on `homeDir`).

---

**Bottom line:** With the **contract doc** tweak above as the only substantive “should fix,” you can treat **P4.Z as closed** and move to **P5** on engineering merit; remaining work is mostly **checkbox + changelog/process** in `plan.md` / team workflow, not functional gaps in the skill package.

---
_used_: mode=coding-mode · skills=无 · tools=Glob, Grep, Read, Shell
