## Verdict

**PASS / LGTM** — 与 P4.2 描述一致：共享 host 写入当前项目 `.agents/skills/agentmesh/SKILL.md`，`claude` 写入 `.claude/skills/agentmesh/SKILL.md`；`verifySkillInstall` 的 `ok` 只由**预期**安装文件决定，`legacy_only` 不拉低 `ok`；`installSkill(..., force: true)` 只覆盖安装目标，不会删除 `.cursor/rules/agentmesh.mdc`。`expectedSkillFilesForTarget` + 已导出的 `SkillExpectedFile` 提供只读路径契约，且不依赖用户 home 下的旧路径（`homeDir` 在路径计算中不起作用，测试也锁了这一行为）。

---

## Must Fix

- 无（在声明需求与当前 `verify.ts` / `readiness.test.ts` 语义下未见阻断性问题）。

---

## Should Fix

- **Should Fix** `packages/skills/src/verify.ts` — `VerifyOptions` 仍包含 `homeDir?`，但 `installPathsForTarget` / `expectedSkillFilesForTarget` 完全忽略它；对外使用者容易误以为 home 参与解析。建议在 `VerifyOptions` 或 `expectedSkillFilesForTarget` 上用简短 JSDoc 写明：**项目级路径仅由 `cwd` 决定，`homeDir` 预留/不参与当前契约**（或将来用途），避免误用。
- **Should Fix** `packages/cli/src/commands/skill.ts`（约 65–68 行）— `skill install` 在 `verifySkillInstall` 后遍历 **全部** `report.files` 打日志；对 `--target cursor` 且存在 legacy rule 时会在成功安装后再打印一条 `legacy_only: ...`，容易让人以为 install 写动了 legacy。更贴近期望的 UX 是只打印 `expected: true` 的条目，或区分 “installed” vs “diagnostic”。

---

## Nit

- **Nit** `packages/skills/src/verify.ts` — `SkillFileReport` 里 `status` 与 `classification` 始终同步，JSON 里重复；若希望诊断载荷更紧凑，可考虑长期只保留其一或 deprecate 其一（非本 slice 必需）。
- **Nit** `packages/skills/src/verify.ts` — `legacyFilesForTarget` 仅用 `existsSync`，若 legacy 路径存在但不可读，不会得到 `unreadable`（当前设计接受为兼容性信号即可）。
- **Nit** `docs/contracts/skill-output.md` — 已描述 install/verify 路径与 legacy 行为；若要把 `expectedSkillFilesForTarget` 列为**稳定公开契约**，可加一句指向该 API（属于文档增强，不在本次 uncommitted diff 里也行）。

---

## Scope note

按会话初始 **git status**，本次未提交变更集中在 `packages/skills/src/verify.ts` 与 `tests-node/readiness.test.ts`；README / `docs/contracts/skill-output.md` / `packages/skills/agentmesh-skill/SKILL.md` 中的路径与 legacy 说明与当前实现**口径一致**（grep 核对），未强求算作本 diff 的一部分。

你列的验证命令我未在本环境复跑（只读 review）；若与本地不一致，以你机器为准。

---

`_used_: mode=coding-mode · skills=my-coding-mode · tools=Read, Grep, Glob`
