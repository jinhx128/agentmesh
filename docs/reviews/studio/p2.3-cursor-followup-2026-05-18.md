Prior review concerns are addressed in the tree: `catalog.ts` no longer touches `child_process` or CLI paths (`readStudioCatalog` only delegates to `./mcp-diagnostics.js`). The boundary script narrows its allowlist to `packages/app-server/src/mcp-diagnostics.ts`, extends the forbid pattern with `spawn`/`spawnSync`/`exec`/`execSync`/`execFile`/`execFileSync`/`fork`/`forkSync` (via `\b(?:spawn|exec|execFile|fork)(?:Sync)?\b`), and still blocks `process.execPath`, `node:child_process`, bare `child_process`, `@agentmesh/cli`, and `packages/cli`. Tests mirror file lists and probe `scripts/check-boundaries.mjs`. Under `packages/app-server/src/` the only `child_process` import is still `mcp-diagnostics.ts`, so the enforcement story matches mutation surfaces using `@agentmesh/runtime`.

---

# agentmesh-review-markdown-v1

## Blocking findings

**None.** The earlier **whole-file carve-out on `catalog.ts`** and **`child_process` API gap (non-`spawn*`)** are remediated by isolating MCP CLI diagnostics in `mcp-diagnostics.ts`, allowlisting **only that file**, and widening detection to `\b(?:spawn|exec|execFile|fork)(?:Sync)?\b` alongside `process.execPath`, `node:child_process`/`from 'child_process'`, `@agentmesh/cli`, and `packages/cli`. For the scoped question **“does the App Server write-path release gate still have blocking holes?”**, `mutations.ts` (and the rest of `app-server/src` except the allowlisted file) still has **no** `child_process` / CLI substring matches in the workspace state reviewed.

## Remaining findings (non-blocking)

1. **[Should fix] Misleading CI message** (`scripts/check-boundaries.mjs` ~37–39): Violation copy still says *“only catalog read diagnostics may spawn the CLI”* while enforcement is keyed to **`mcp-diagnostics.ts`**, not catalog. Updating the message avoids future confusion during gate failures.

2. **[Residual policy risk / Should clarify] Whole-file carve-out is smaller but still coarse.** Anything added to `mcp-diagnostics.ts` that matches forbidden tokens is exempt. That is much tighter than exempting all of `catalog.ts`, but the **policy ↔ enforcement** pairing is still “one file”, not symbol-scoped.

3. **[Nit] Latent heuristic brittleness for `\bexec\b`.** The pattern can match **`RegExp`’s `.exec(`** (`… .exec`) if such code appears under `packages/app-server/src` later. There are **no** current `.exec` matches in app-server sources; acceptable as lint noise risk only.

4. **[Nit] Redundant narrower assertion** (`tests-node/package-structure.test.ts` ~424–429): Extra checks on mutation-related files still only mention `\bspawn(?:Sync)?\b|process\.execPath`; the broader loop above already forbids **`exec*` / `fork*`**. Harmless redundancy, small **drift** risk if someone edits one block without the other.

## Verdict

**PASS for P2.3 App Server write-path / CLI-spawn gate** — no blocking issues remain relative to the previous review once this diff lands; residuals are hygiene (copy), policy clarity (single-file carve-out), and heuristic edge cases.

`Summary: 0 Must, 2 Should, 2 Nit`

---

_used_: mode=coding-mode · skills=my-coding-mode · tools=Read, Grep (Shell invocation rejected — boundary script / tests not re-run locally; aligns with your 74/74 P2.3 run)
