## Review (read-only)

**LGTM** for landing this diff as **test-stability-only** work: both changes are plausibly load-sensitive (`spawnSync` budget for a failing CLI run; sidecar process readiness under a contended runner), and your verification set is strong for this scope. **Residual risk:** higher ceilings can hide real slowdowns until they exceed 5s/15s; if sidecar startup regresses into the 10–15s band you would still be green while UX degrades.

### Findings

[Should Fix] *commit message / plan snippet* — P2.Z 清单里的提交说明是 `refactor(studio): complete app server and web split`，而当前未提交 diff **只调了两个测试超时**；若仍用那句当本次 commit message，会和真实变更不符，也不利于以后用 `git blame` / `git log` 做拆包相关排查。建议本次提交用语义上贴近 “test flake / timeout under full suite” 的 subject（计划里那句更适合概括 **已在 master 的 P2.1–P2.3** 或单独的大 refactor commit）。

[Nit] `tests-node/studio-desktop-distribution.test.ts:323` — `launchSidecar` 就绪超时 5s→15s 会拉长**失败路径**耗时；在 CI 里通常可接受，但若以后出现 “侧车偶发卡死”，定位会多等最多 10s。

[Nit] `tests-node/studio-cli.test.ts:221` — `runCli` 用 `spawnSync` 的 `timeout` 只作用在该用例；**缺少 assets 时应快速退出**，1s 在轻载下足够，5s 更多是调度/负载缓冲。若日后仍偶发超时，值得再查是否 CLI 在错误路径上做了意外慢操作，而不是继续放宽。

### P2.Z / 门禁

- 与 **“拆包是否正确”** 相关的证据主要在 **已提交的 P2.1–P2.3** 与 **`npm run check:boundaries`**；本 diff **不增加也不削弱** 架构约束，只降低测试脆性。
- 计划中的 **外审 “可进入 Desktop 打包”** 属于流程确认：从代码层面，这份 diff 本身不阻断进入 P3，前提是 reviewer 已独立认可 master 上的拆包结果（你为本次说明的 launcher / `studio-web` / `app-server` / desktop 职责）。

---

_used_: mode=无 · skills=无 · tools=Read, Grep
