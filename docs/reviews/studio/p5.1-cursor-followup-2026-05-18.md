## Verdict: **PASS / LGTM** (Should Fix → all addressed)

Cross-checked against `docs/reviews/studio/p5.1-cursor-2026-05-18.md` **Should Fix** list:

### 1. Post-install verification not Codex-default (`README.md`)
The “CLI 安装后至少验证” block states verify should run against **targets you installed** and uses a generic host placeholder:

```244:250:/Users/zz/Documents/WebStorm/agentmesh/README.md
CLI 安装后至少验证；Skill verify 要对你已安装的 target 执行：

```bash
agentmesh --help
agentmesh doctor --json
agentmesh skill verify --target <host> --json
```
```

Codex-only verify remains only in the **explicit Codex subsection** (“如果你当前主要用 Codex”, lines 291–296), which matches the accepted intent.

### 2. macOS app update wording (`docs/distribution/studio-macos.md`)
**App-Managed Updates** now names the sidecar/runtime entrypoint and explicitly separates it from the PATH tarball CLI:

```124:127:/Users/zz/Documents/WebStorm/agentmesh/docs/distribution/studio-macos.md
`AgentMesh.app` updates the app-bundled Studio host, App Server, UI assets, and
sidecar app-runtime entrypoint together. That entrypoint is not the
PATH-visible `agentmesh` tarball command. An app update must not mutate or
replace a global npm install of `agentmesh`.
```

### 3. Tarball filename in `cli-command-install.md`
Tarball step uses `agentmesh-<version>.tgz`, ties naming to `npm pack` / root `package.json`, and separately documents that **`npm install -g` does not prompt**:

```58:67:/Users/zz/Documents/WebStorm/agentmesh/docs/distribution/cli-command-install.md
npm run build
npm pack --pack-destination /tmp
npm install -g /tmp/agentmesh-<version>.tgz
...
Use the exact tarball filename printed by `npm pack`; the version placeholder
must match the packaged root `package.json`.
```

```97:99:/Users/zz/Documents/WebStorm/agentmesh/docs/distribution/cli-command-install.md
`npm install -g` itself does not prompt before changing a global command. Any
team installer, app action, or rollout wrapper that switches the PATH-visible
command must do the inspection and confirmation step before invoking it.
```

### Acceptance nits / tests (outside “Should Fix” but you listed them)
- **README** links `docs/distribution/cli-command-install.md` (line 258) and avoids the pinned registry date (no `2026-05-18` registry line in README from search).
- **`package-shape.md`** clarifies confirmation is not enforced by raw `npm install -g` (lines 97–99).
- **`tests-node/package-structure.test.ts`** resolves the verified file via `find(...)` instead of `files[0]` (lines 304–311).

### Remaining **Must** / **Should**
None from this review pass.

### Optional **Nit**
Codex stays in intentional **smoke** doc blocks (`package-shape.md`, `cli-command-install.md`, coexistence smoke) and pack-install tests—that’s coherent with anchored automation.

---

_used_: mode=无 · skills=无 · tools=Read, Grep
