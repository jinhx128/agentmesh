## agentmesh-review-markdown-v1

### Must Fix

_No new issues that clearly fail P5.2 DMG acceptance, optional-integrations boundary, or app/runtime vs PATH CLI separation._

---

### Should Fix

_No open items from `docs/reviews/studio/p5.2-cursor-2026-05-18.md` **Should Fix** list remain unaddressed in the intended spirit; closure details below._

| # | Original item | Closure check |
|---|----------------|---------------|
| 1 | CLI `requires_confirmation` vs target replace / preview for effective `bin_dir` | Server: `requires_confirmation` now includes `target_file.exists && target_file.different` for the **effective** `bin_dir` used in each report/install (`commandLineToolReport(..., binDirOverride)` plus `beforeReport = commandLineToolReport(options, binDir)` before POST). UI: treats **non-default** `bin_dir` as requiring confirmation (`customBinDir`). GET still previews `target_file` only for **default** install dir; non-default rows show install-time check language—aligned with earlier “preview **or** conservative UI”. |
| 2 | Confirmation UX / PATH shadowing copy | Checkbox label references replacement + PATH shadowing; contextual `integration-warning` explains PATH precedence or different file at install target or custom-bin caveat; tests assert copy. |
| 3 | Partial skill-install copy | `resultText` emits `installed N; failed: …` when any `ok === false`. |
| 4 | Desktop skill prefers bundle/module over workspace `cwd/...` | `agentmeshSkillMarkdown(..., { preferModuleSource })` plus `desktopSkillMarkdown`; regression test asserts workspace stray `# Wrong Workspace Skill` is **not** written. |
| 5 | Wrapper absolute-path durability in docs | `docs/distribution/cli-command-install.md`, `docs/distribution/studio-macos.md`, `README.md`, `docs/distribution/studio-coexistence-smoke.md` call out moves/updates and re-run install. |

---

### Nit

1. **`AgentIntegrationsPanel` — overlapping reasons** When `requires_confirmation` is true for **both** PATH shadowing **and** a different-on-disk target, the banner only surfaces the PATH branch (`commandConfirmationReason` ordering). Rare; UX could mention both.

2. **GET vs custom `bin_dir` preview** Status row still omits server-side `target_file`/`requires_confirmation` for the user’s typed directory until POST; mitigation is mandatory confirmation + copy. If product ever drops `customBinDir` conservatism, reopen Should-level alignment.

3. **Shared targets grid** Codex/Cursor/Gemini/OpenCode/Copilot still share one SKILL path; unchanged from prior Nit (still accurate, still slightly misleading for “six separate installs” expectations).

*(Original Nit about `isExecutableFile` narrowness is effectively handled: `(stats.mode & 0o111) !== 0`. Original Nit about hints/paths in skill rows is improved via `hint ?? expected_path` in the subtitle.)*

---

### Summary

The follow-up **closes all five prior P5.2 Should Fix items**: server semantics for confirmation now cover **different content at install target**, the web panel explains **PATH shadowing vs replacement**, skill results surface **partial failure**, bundled skill Markdown is **preferred over workspace clones** (`preferModuleSource` + test), and **wrapper fragility after app move/update** is documented in distribution/README/smoke docs. Automated coverage includes desktop integration/API tests (`studio-desktop-options.test.ts`, extended UI assertions). Claimed verification (118 tests + packaging smoke + boundaries) matches what would be expected for **no regressions**.

**LGTM for P5.2 closeout** — no remaining **Must Fix** / **blocking Should Fix** surfaced in this scoped review.

---

_used_: mode=无 · skills=无 · tools=Read, Grep
