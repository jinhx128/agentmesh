Unable to run `git diff` in this environment (command was rejected), so this review is based on the current working-tree contents of the touched areas (`apps/studio-desktop`, `packages/app-server`, `plan.md`, docs, tests) and targeted searches—not a line-by-line diff against `HEAD`.

## Verdict: **PASS**

Nothing in the reviewed paths rises to a **BLOCK** for P3.2: sidecar argv stays token-free, the secret crosses the boundary on **stdin** as one-line JSON, Tauri sets **`HttpOnly` + `SameSite::Strict`** on `127.0.0.1` before **`navigate`**, navigation targets a **no-query `http://127.0.0.1:<port>/`** shape, the App Server **`authorizeRequest`** path uses **Bearer + cookie only** (no query auth), loopback **Host/Origin** gating and **401/403** behavior are exercised in tests, and **`serializeStudioDesktopLaunchEvent`** omits the token while tests assert stdout JSON does not contain it.

---

### Must fix

- **None** from code review (remaining gaps are **process/docs hygiene**, not a clear security defect in the reviewed implementation).

---

### Should fix

1. **`plan.md` P3.2 slice still staged as not done** — The `[ ] P3.2` block and “进度记录：待执行” under P3.2 conflict with an implemented slice; when you close P3.2 you should align checklist state, evidence, and the “next step” footer so the plan matches reality (see `plan.md` around the P3.2 section and line ~1084 “当前下一步：`P3.2`…”).

2. **`docs/contracts/app-server.md` vs optional-auth server** — The contract says a per-launch token is required for UI traffic, while `authorizeRequest` effectively allows **unauthenticated** access when `authToken` is unset (developer / `agentmesh studio` modes). Worth a short clarification so “desktop bundle” vs “dev server without token” is not contradictory (`docs/contracts/app-server.md` “Security Boundary” / responsibilities).

3. **Re-run full `npm test` after review edits** — You noted a prior stale doc assertion; until the full suite is green again, treat “all tests pass” as **unverified** for this branch tip.

---

### Nits

1. **Stale “P3.1” wording in DMG distribution metadata / tests** — `apps/studio-desktop/distribution/macos.json` (`shell_spike.webview_smoke` still mentions P3.1) and `tests-node/studio-desktop-distribution.test.ts` (asserts `webviewSmoke` matches `/P3\.1/`) are phase-label drift; harmless but confusing when P3.2 owns cookie/no-query bootstrap.

2. **Naming collision across docs** — `docs/decisions/studio-frontend-stack-proposal.md` uses “P3.2” for “don’t move logic into Rust,” which is **not** the same as monorepo `plan.md` P3.2 (token/security). Consider renaming in that decision doc to avoid cross-link confusion.

3. **`studio-web` bootstrap still understands legacy `?token=` in the page URL** — `apps/studio-web/src/api/bootstrap.ts` strips `token` from history for cleanup; that’s fine for old bookmarks, but it’s worth remembering it’s **legacy UX**, not the Desktop transport (no conflict with “server won’t accept query token for auth,” which holds).

---

### Requirement checklist (concise)

| Requirement | Assessment |
|---------------|------------|
| Token not in argv / URL query for Desktop | **Met** — Tauri: `args(["--launch-json"])`; URL validation rejects query/fragment; sidecar tests assert `spawnargs` and `webview_url` shape (`apps/studio-desktop/src-tauri/src/lib.rs`, `tests-node/studio-desktop-distribution.test.ts`). |
| Per-launch token, stdin JSON handshake, cookie before navigate | **Met** — Handshake JSON written to child stdin; cookie set then `navigate` (`lib.rs`). |
| App Server loopback + Host/Origin/CORS | **Met** — `startStudioServer` defaults `127.0.0.1`; `enforceLocalRequestBoundary` + tests for hostile Host/Origin/OPTIONS (`packages/app-server/src/server.ts`, `tests-node/studio-desktop-options.test.ts`). |
| Bearer + cookie auth; no query token | **Met** — `authorizeRequest` only inspects `Authorization` and `Cookie`; tests cover `?token=` → 401. |
| Old/stale/missing auth explicit | **Met** — 401 `unauthorized` / 403 host-origin; restart token test for stale cookie (`studio-desktop-options.test.ts`). |
| Launch JSON / logs / argv without token | **Met** — `serializeStudioDesktopLaunchEvent` has no token; CLI log line uses `redactStudioUrlForLog`; ready JSON test rejects embedded secret (`host.ts`, `main.ts`, tests). |

---

### Plan update (P5 DMG-only vs CLI/skills)

**PASS** for the stated intent: `plan.md` **§ P5** now states DMG-only is **Desktop Studio only**, entry agents need a **PATH-visible `agentmesh`**, and **user-selected** skill targets including **`codex` / `cursor` / `gemini` / `opencode` / `copilot` / `claude`** with the expected **shared vs Claude paths** (see `plan.md` ~934–989, ~1037–1079).

---

### Risk assessment

**Low** for the intended **local threat model**: binding to loopback, strict host/origin posture, cookie + optional Bearer, and automated rejection of query-token auth remove the largest practical leak classes (argv, shareable URLs, naive cross-origin browser use). **Residual**: anyone with **local code injection / ptrace / memory inspection** can still observe stdin or process memory; **Secure** cookie is absent as expected for **`http://127.0.0.1`**. **Documentation/plan drift** is the main practical risk until P3.2/P5 sections and phase strings are aligned with what ships.

---
_used_: mode=无 · skills=无 · tools=Read、Grep
