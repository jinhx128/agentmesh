结合仓库内容与验收清单，给出 **agentmesh-review-markdown-v1** 风格结论如下。

---

## agentmesh-review-markdown-v1

### Must Fix

| 严重程度 | 位置 | 结论 |
|---------|------|------|
| Must Fix | `plan.md:L934-L936`、`plan.md:L1005-L1021`、`plan.md:L1082-L1084` | **P5 总门禁仍为 `[ ]`，`P5.Z 项目收尾校准` 仍为 `[ ]`，进度为「待执行」**；与同文件内已勾选的 **P5.1 / P5.2**、「当前下一步：P5.Z」并存。若以仓库为准，形式上 **还不能宣称 P5 / P5.Z 已收口**；要宣布阶段完成需在完成审查与日志后再勾选，并写明证据（与你们在 P5.Z 自检里跑的命令对齐）。 |
| Must Fix | `changelog/2026-05-18.md`（末尾条目止于 P5.2） | **缺少对应的 P5.Z 收尾条目**（你要求把该文件纳入范围）。与 `plan.md` 中 P5.Z 仍未完成一致；若自检已全部通过，应补一条 changelog，记录 P5.Z 验证命令与结论，再在 `plan.md` 勾选。 |
| Must Fix | 验证可复现性（本轮会话） | 你列出的 `npm run build` / 全量 `npm test` 等在本环境 **未能独立复跑 Shell 核对**（工具侧拒绝）。**发布结论应在你本地或 CI 再执行一次同款命令存档**，避免仅以对话声明为准。（这不是代码缺陷，是**审查证据缺口**。） |

### Should Fix

| 严重程度 | 位置 | 结论 |
|---------|------|------|
| Should Fix | `plan.md` P5.2 进度小节 | 仍存在 **「commit：待提交」**一类与后续「已实现/已日志」不一致的占位表述；收尾时应改成实际 commit hash 或与 `changelog` 一致，避免后人误判 P5.2 未入库。 |
| Should Fix | 自动化 vs 门禁文字 | **P5.Z** 写明 `DMG command-line tool smoke`、`至少一个 shared target + Claude` 等；桌面侧已有 **`desktop command-line tool install`**（含 PATH 冲突替换确认）与 **`desktop skill install`（codex + claude）**。从实现看 **`gemini` / `opencode` / `copilot` 与同一路径写入逻辑共享**，风险低，但若要以「字面门禁」压得最满，可考虑补 **循环/参数化测试** 或注明「shared target 等价类仅测代表 target」。 |

### Nit

| 严重程度 | 位置 | 结论 |
|---------|------|------|
| Nit | `tests-node/studio-desktop-options.test.ts`（CLI wrapper `--help` 断言） | `execFileSync(..., "--help")` **期望 stdout 为空**；若日后 CLI `--help` 默认向 stdout 打说明，会破坏测试。**低概率、纯属脆性**。 |
| Nit | 用语 | UI 为 **Install Selected Skills**，`plan.md` 部分写 **Install Agent Skill / skill install**；含义一致但文档可再统一用词。 |

### Summary

- **Must：** 共 **3**：`plan.md` 中 **P5 / P5.Z 仍为未完成勾选**、`changelog` **缺 P5.Z 条目**、本轮 **无法用命令在本环境复跑** 所形成的证据缺口。  
- **Should：** **2**：`plan.md` P5.2 **commit 占位** outdated、门禁与测试覆盖的「字面完全对齐」可再收紧或说明等价类。  
- **Nit：** **2**：`--help` 断言脆性、文档/UI 用词微差。

**实现对验收项的对照（基于已读源码/测试）：**

- **DMG-only 仅供 Desktop Studio、入口依赖 PATH-visible `agentmesh`：** README、`docs/distribution/cli-command-install.md`、`docs/distribution/studio-coexistence-smoke.md` 与 `@agentmesh` 宿主 skill 契约一致（DMG ≠ 可调入口 CLI）。  
- **Desktop mutation 不走 CLI：** `integrations` 路径为 **App Server 内 `fs` + `@agentmesh/skills` 的 `installSkill`**，**未见**通过 `spawn` 调 CLI 实现集成安装；共存类测试仍以 **runtime API** 与 PATH 哨兵区分 app / CLI（见 `changelog` / `studio-coexistence-smoke.md` 描述）。  
- **Install Command Line Tool：** **先报告 PATH/`target`，冲突时 `requires_confirmation`，未确认返回 409**；UI 勾选 **Confirm replacement or PATH shadowing**；与 `integrations.ts`、`AgentIntegrationsPanel.tsx` 一致。  
- **Install Agent Skill / 勾选宿主：** **`codex`/`cursor`/`gemini`/`opencode`/`copilot`** 走 **`.agents/skills/agentmesh/SKILL.md`**，**`claude`** 走 **`.claude/skills/agentmesh/SKILL.md`**（见 `packages/skills/src/verify.ts`）；桌面测试 **仅选 codex+claude** 并已断言两路径内容与 **非 workspace 顶替 skill**。  
- **Tauri：** `targets: ["dmg"]`，`bundle.resources` 含 **CLI / skills 模板 / sidecar**，与可选集成一致。  

**Release verdict：** **needs_decision**  

理由：从你提供的自检命令看，**工程行为与门禁高度吻合 `ready`**，但仓库内 **`plan.md` / `changelog` 尚未反映 P5.Z 收口**，且本轮审查 **无法在环境中复跑你那组命令**；是否宣布 **「P5 阶段完成」和对外 `ready`**，取决于是否先完成 **Must Fix** 的流程与文档同步，以及你们是否坚持把 **第二轮人审** 记在 `plan` 的证据里。**若你只问「技术上能否完成」：可以，只差收尾勾选与 changelog / 可选补测。**  

（你指定的 **41ae106 / a138a99** 在本会话中 **无法用 `git show` 拉出 diff**（工具拒绝）；若审稿必须绑定这两条 commit，请本地 `git log -2`、`git show` 自行核对是否覆盖 P5.2/Z。）

---

_used_: mode=coding-mode · skills=my-coding-mode · tools=Glob, Read, Grep
