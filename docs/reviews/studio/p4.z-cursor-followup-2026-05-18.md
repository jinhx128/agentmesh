## P4.Z follow-up — documentation

**Accepted issue (prior Should Fix #1): resolved**

In `docs/contracts/skill-output.md`, the “runtime” wording is gone. The contract now states that the Version Metadata block comes from **`@agentmesh/skills`** via **`agentmeshSkillMarkdown`**, and that it is **not** hand-maintained in each host wrapper, with a clear requirement that wrappers embed the same canonical markdown:

```25:28:docs/contracts/skill-output.md
The metadata block is generated by `@agentmesh/skills` through
`agentmeshSkillMarkdown`, not hand-maintained in each host install wrapper.
Host-specific wrappers must embed the same canonical markdown so exported and
```

That matches the implementation: `agentmeshSkillMarkdown` is defined in `packages/skills/src/verify.ts` and used by the CLI `skill` command, so the doc no longer implies `packages/runtime` owns Skill output.

**Must Fix / Should Fix (for this closeout)**

- **Must Fix:** None identified relative to the prior P4.Z review scope; nothing new contradicts the skill-package boundaries or install/verify behavior from that pass.
- **Should Fix:** The single substantive Should Fix from `docs/reviews/studio/p4.z-cursor-2026-05-18.md` (contract doc accuracy) is **addressed** by the current contract text.

The items listed there under **Nit** (legacy `agentmesh-skill` fallback, `expectedSkillFilesForTarget` adoption, internal `VerifyOptions`) were explicitly optional/process notes, not Must/Should gates for P4.Z.

**Verdict:** **PASS / LGTM** for the doc follow-up and for treating the prior Should Fix as cleared, assuming your reported **`npm run build`**, **69/69 node tests**, and **`git diff --check`** remain green (I did not re-run those here).

---
_used_: mode=无 · skills=无 · tools=Grep, Read
