LGTM

The P1.5 slice meets all intent requirements perfectly:
- `CURRENT_PACKET_SCHEMA_VERSION` is correctly synchronized to `CURRENT_SCHEMA_VERSION` (1) in `packages/core/src/index.ts`.
- The test helper has been properly renamed from `packetStatusV2` to `currentPacketStatus`, removing the `v1`/`v2` dual-version strategy naming in tests (`tests-node/helpers/current-packet-status.ts` and downstream usages in `tests-node/packet-validate.test.ts`).
- Documentation (`README.md`, `index.html`, and TOML examples like `docs/workflows/review-gate.toml`) have been successfully updated to collapse the narrative to the "current packet schema" and completely stripped of "v2" references. 
- Internal `schema_version` bounds tests correctly remain intact and continue validating forward-compatibility safeguards (e.g., intentionally rejecting newer unsupported schemas like `99`). 

No test gaps or regressions were found. Beautiful execution.

---
_used_: mode=无 · skills=无 · tools=grep_search, read_file
