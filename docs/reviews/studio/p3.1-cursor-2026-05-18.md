## Verdict: **PASS**

P3.1 goals are met in code: desktop host + bundled Node sidecar, explicit Tauri `resources`/`externalBin` without `packages/cli`, no App Server `child_process` / CLI spawn path, MCP catalog via `loadConfigWithSources`, mutations/lifecycle via `@agentmesh/runtime`, and smoke/tests lock the launcher layout and `dist-node` → `Resources` mapping (including `runtime-node_modules` → packaged `dist-node/node_modules` for Node resolution from `apps/studio-desktop/src/main.js`). Compiled artifacts use **relative** `../../../packages/...` imports (e.g. `dist-node/apps/studio-desktop/src/host.js`), so skipping `link: true` workspace entries in the sidecar `node_modules` copy is consistent: third‑party deps land under `dist-node/node_modules`, workspace code comes from the explicit `dist-node/packages/*` resource trees.

---

## Findings (by severity)

**None — blocking**

**Should fix**

1. **`README.md` (L34–L36)** — Still says L8 Studio mutates flows “通过 **CLI subprocess** 触发 dispatch/retry/resume/attach”. App Server / desktop path is **HTTP + runtime APIs** (`mutations.ts` uses `dispatchFlowStage` / etc., not a subprocess). This is user-facing doc drift vs the new contract, not a packaging bug.

2. **`packages/app-server/src/catalog.ts` (L9–L13)** + **`tests-node/studio.test.ts` (L1473–L1477)** — `StudioCatalogOptions.commandTimeoutMs` remains on the type and in a test call but is **unused** after removing CLI MCP listing. Misleading API; remove or wire it (e.g. into future timeouts on SDK/config reads).

**Nice to have**

3. **Stale internal docs** — `changelog/2026-05-18.md` and `docs/reviews/studio/p2.3-*.md` still describe `mcp-diagnostics.ts` and allowlists; `mcp-diagnostics.ts` is deleted and `check-boundaries.mjs` is now a flat forbid under `packages/app-server/src/**` (`package-structure.test.ts` already asserts no allowlist). Updating those avoids wrong mental model for reviewers.

4. **`apps/studio-desktop/src/sidecar-bundle.ts` (`isTypeScriptSourceFile`, L163–L165)** — Only `.ts` / `.tsx` (excluding `.d.ts`). Rare deps could ship `.mts`/`.cts` as sources; low probability but easy hardening.

5. **`bundleRuntimeNodeModules` (L143–147)** — If `package-lock.json` lists a non-dev package but `existsSync(sourcePath)` is false, the loop **skips** without failing. Incomplete installs could slip through until runtime; optional strictness.

6. **`scripts/check-boundaries.mjs` (L31)** — Regex word-boundary `fork` could theoretically false-positive on identifiers like `forkSomething` (unlikely in practice).

---

## Plan checklist (concise)

| Area | Assessment |
|------|------------|
| (1) CLI packaged/invoked for Desktop/App Server | **OK** — Tauri resources exclude `dist-node/packages/cli`; `distribution-smoke` rejects that path; App Server grep shows no CLI/`child_process` usage; `coexistence` test still spawns CLI only to prove **CLI Studio vs desktop**, not desktop bundling. |
| (2) Tauri resource map vs sidecar layout | **OK** — `tauri.conf.json` L35–43 matches smoke L216–226; launcher `PACKAGED_APP_DIR`/`PACKAGED_ENTRYPOINT` in `sidecar-bundle.ts` L105–112 aligns with `Resources/dist-node/apps/studio-desktop/...`; `runtime-node_modules` → `dist-node/node_modules` matches Node lookup from `src/main.js`. |
| (3) `.ts`/.tsx` filtering | **Adequate** — `cpSync` filter + throw/ smoke if any `.ts`/`.tsx` remain (sidecar L156–159, `distribution-smoke` L431–436); edge cases above under Nice to have. |
| (4) MCP: CLI list → config | **OK** — `catalog.ts` L87–116 uses `loadConfigWithSources`; `studio.test.ts` adds coverage for catalog without CLI (L1467–1483); behavior may differ from old `mcp list` CLI in edge cases—no test evidence of a gap. |
| (5) Test/doc gaps | **Tests strong** for distribution/sidecar/boundary/catalog; **README** bullet stale (Should fix); **historical** changelog/review text stale (Nice to have). |

---

### Separate buckets

- **Should fix:** README L8 Studio mutation wording; dead `commandTimeoutMs` on catalog + test.  
- **Nice to have:** Refresh changelog/review mentions of `mcp-diagnostics`; `.mts`/`.cts` filter; stricter missing-path behavior in sidecar bundle; trivial regex doc note.

---

_Summary: 0 blocking, 2 should-fix (docs/API dead field), 4 nice-to-have._

---  
_used_: mode=coding-mode · skills=my-coding-mode · tools=Read, Grep, Glob
