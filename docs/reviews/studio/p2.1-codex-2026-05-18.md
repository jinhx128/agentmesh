```yaml
format: agentmesh-review-markdown-v1
schema_version: 1
subject: "P2.1 App Server extraction (uncommitted diff)"
verdict: "LGTM - no blocking issues"
blocking_findings: []
```

[Should Fix] [packages/app-server/src/calls-browser.ts](/Users/zz/Documents/WebStorm/agentmesh/packages/app-server/src/calls-browser.ts:112) — Call adoption lost CLI-side validation for metadata after moving to direct runtime calls. `related_run_id`, `superseded_by_call_id`, `reason`, and `related_commit` now go through `optionalString()` and into `appendCallAdoptionEvent()` without the old CLI `safeToken` / null-byte checks. This can persist invalid related IDs that `agentmesh calls adopt` would reject. Add validation in app-server or, better, the runtime adoption API, plus a Studio adoption regression test.

[Should Fix] [packages/app-server/src/agent-lifecycle.ts](/Users/zz/Documents/WebStorm/agentmesh/packages/app-server/src/agent-lifecycle.ts:110) — If the runtime lifecycle call throws, the operation remains stored as `running` and the API returns a plain `400` error instead of a failed lifecycle operation. This can happen on config parse/load failures or filesystem write errors. The old subprocess path converted CLI failures into `{ status: failed, exit_code, stderr }`. Wrap `runRuntimeAgentLifecycle()` and update the operation to `failed` before returning/rethrowing.

## Boundary Check

No remaining App Server -> CLI write path found in `mutations.ts`, `agent-lifecycle.ts`, or `calls-browser.ts`. The remaining `child_process` use in `packages/app-server/src/catalog.ts` is the allowed read-only MCP diagnostic path.

## Residual Risks

`cliPath` is still threaded through some App Server options for compatibility/diagnostic display, but production write paths no longer use it. I did not rerun the full suite; this was a static diff review with targeted source tracing.

Summary: 0 Must, 2 Should, 0 Nit

---
_used_: mode=coding-mode · skills=using-superpowers,my-coding-mode · tools=exec_command,multi_tool_use.parallel
