[Should Fix] tests-node/studio-cli.test.ts:219 — 测试中直接通过 `renameSync` 物理重命名全局的构建产物目录 `defaultStudioAssetDir`。由于 Node.js 的 `--test` 在跑多文件时默认并行执行，这会引发竞态条件，极易导致正在并发执行的其他依赖该目录的测试文件（如 `studio.test.ts`）随机失败（如找不到资源报错 500）。建议改为通过注入环境变量或内部参数来 mock `assetDir`，以避免对全局共享构建目录产生破坏性的物理副作用。

（补充确认项：
1. `cli` 和 `studio/main.ts` 中基于 `import.meta.url` 的相对层级回溯指向 `dist-node/apps/studio/frontend/` 均是正确的。
2. `server.ts` 在缺 `index.html` 资源时返回 500 且停止回退旧页面的逻辑十分合理，兜底了运行中资源丢失的情况。
3. 测试覆盖场景全面，仅此一处并发执行的隐患问题。）

---
_used_: mode=coding-mode · skills=无 · tools=无
